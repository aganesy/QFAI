{
  "$comment": [
    "The job this repository EXPECTS to carry a required status context, and what that job",
    "must be performing for the expectation to mean anything.",
    "",
    "Why a checked-in file at all: which checks branch protection actually requires is a",
    "repository setting, and a pull request cannot read it. So the expectation is declared",
    "here and the workflow hygiene lane checks the tree against this file. Reading live",
    "settings is forbidden, because a check that cannot run on a pull request blocks nothing.",
    "",
    "This file states an expectation. It cannot prove the setting agrees with it. What it can",
    "do — and what the lane does — is make a change that would invalidate the expectation fail",
    "before it merges.",
    "",
    "IF THE SETTING IS CHANGED TO MATCH THIS FILE, the required context must be `ci-pass` and",
    "not `build`. That is the point of the entry below, and it is a repository-settings change",
    "no pull request can make."
  ],
  "contexts": [
    {
      "workflow": "ci.yml",
      "job": "ci-pass",
      "why": [
        "`ci-pass` and not `build`, which is where this pointed until review finding [28] on",
        "PR #794 measured what that cost: `build` declares no `needs` at all, so requiring it",
        "and nothing else means every Vitest lane, `lint`, `check-types` and",
        "`scanner-coverage` can fail while the merge condition is satisfied. The one job whose",
        "result depends on theirs is the aggregate verdict, so that is the one worth requiring.",
        "",
        "The earlier entry argued the opposite from a real constraint: the verdict carries",
        "`if: always()`, and the hygiene lane's property 2 rejected any condition on a declared",
        "job, because a skipped job reports SUCCESS to branch protection. That approximation is",
        "right for every condition except this one — `always()` is what guarantees the job",
        "runs. The lane now says so, and a condition that is not `always()` is still fatal.",
        "",
        "Its dependencies DO carry conditions (`check-types`, `test` and the rest run on",
        "`needs.detect.outputs.full`), and that is the arrangement rather than a hole: the",
        "verdict is derived by iterating the serialized `needs` map, and its accepting set",
        "includes `skipped`. A documentation-only pull request skips those lanes, the verdict",
        "runs anyway and passes; a failing lane makes it fail. Neither is reachable while the",
        "context sits on a job that never looks at them.",
        "",
        "The executed-instance ceiling in the spec is written as 'at most four job instances",
        "while the job named `build` carries the required status context, and at most three",
        "once that context moves'. The context has now moved. The third instance is not yet",
        "realised, because `build` still carries no condition and so still runs on a",
        "documentation-only pull request; making it conditional is a separate change with its",
        "own measurement, and is deliberately not bundled here."
      ],
      "verificationSet": [
        "Derive the verdict from the serialized needs map",
        "Run build & pack verification",
        "Sanity grep — no internal spec IDs or version markers leak (post-build)",
        "QFAI self-validate this repo (dogfooding — TDD gates)",
        "QFAI self-validate this repo (dogfooding — SDD gates)",
        "QFAI self-validate this repo (dogfooding — full profile)",
        "Run qfai validate gate (fail on error)"
      ],
      "verificationSetNote": [
        "Step names, because a step name is what a reader of the workflow sees and what a",
        "reviewer of a diff notices moving. The lane accepts an item performed by the declared",
        "job OR by a job it depends on, so relocating work into a dependency is legal and",
        "relocating it out of reach is not.",
        "",
        "The six build-side items are unchanged from when this entry named `build`: `build` is",
        "inside `ci-pass`'s `needs`, so they remain in reach and are still enumerated. The",
        "verdict's own step joins them, because the property this context carries is that the",
        "verdict was actually derived rather than that the job merely existed.",
        "",
        "The optional report upload is deliberately absent: it ends in a tolerated failure and",
        "cannot fail the job, so it is not a verification however it is named."
      ],
      "verificationBodies": {
        "Derive the verdict from the serialized needs map": "aa561732d2c46923",
        "Run build & pack verification": "825e27ef8e9e3c0e",
        "Sanity grep — no internal spec IDs or version markers leak (post-build)": "da88ff96bc74267f",
        "QFAI self-validate this repo (dogfooding — TDD gates)": "0a974493b6af80f8",
        "QFAI self-validate this repo (dogfooding — SDD gates)": "40b1a3e874db39de",
        "QFAI self-validate this repo (dogfooding — full profile)": "01092c148c823b66",
        "Run qfai validate gate (fail on error)": "17bff3325ca1a4e3"
      },
      "verificationBodiesNote": [
        "A digest of each verification step's `run` / `uses` / `with` / `env`, and of the package",
        "scripts that `run` invokes — what the step DOES, with its name deliberately excluded so",
        "the pin cannot agree with itself.",
        "",
        "Membership in `verificationSet` is decided by the step's NAME, and review finding [03] on",
        "PR #794 measured what that alone is worth: renaming nothing and replacing",
        "`run: pnpm ci:build-verify` with `run: true` left the hygiene lane green while the required",
        "context verified nothing at all. The name says which obligation a step claims; this says it",
        "still discharges it.",
        "",
        "The INVOKED SCRIPTS are in the digest because `run: pnpm ci:build-verify` is a reference, and",
        "pinning a reference pins the pointer rather than the work. Review finding [36]: deleting",
        "`node ./scripts/check-publish-dry-run.mjs` from that script in the root manifest left the",
        "step's digest and this declaration in perfect agreement while the pack verification stopped",
        "happening. Resolution is transitive, so a script that calls another script does not move the",
        "problem one hop.",
        "",
        "Editing one of those step bodies — or a script one of them runs — is expected to fail this",
        "lane. Recompute with `node scripts/pin-verification-bodies.mjs` and land the new digest in",
        "the same change: that is the point, a change to a required verification is a change a",
        "reviewer reads."
      ]
    }
  ]
}
