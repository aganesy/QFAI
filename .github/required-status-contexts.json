{
  "$comment": [
    "The job this repository EXPECTS to carry a required status context, and what that job",
    "must be performing for the expectation to mean anything.",
    "",
    "Why a checked-in file at all: which checks branch protection actually requires is a",
    "repository setting, and a pull request cannot read it. So the expectation is declared",
    "here and the workflow hygiene lane checks the tree against this file. Reading live",
    "settings is forbidden, because a check that cannot run on a pull request blocks nothing.",
    "",
    "This file states an expectation. It cannot prove the setting agrees with it. What it can",
    "do — and what the lane does — is make a change that would invalidate the expectation fail",
    "before it merges.",
    "",
    "IF THE SETTING IS CHANGED TO MATCH THIS FILE, the required context must be `ci-pass` and",
    "not `build`. That is the point of the entry below, and it is a repository-settings change",
    "no pull request can make."
  ],
  "contexts": [
    {
      "workflow": "ci.yml",
      "job": "ci-pass",
      "why": [
        "`ci-pass` and not `build`, which is where this pointed until review finding [28] on",
        "PR #794 measured what that cost: `build` declares no `needs` at all, so requiring it",
        "and nothing else means every Vitest lane, `lint`, `check-types` and",
        "`scanner-coverage` can fail while the merge condition is satisfied. The one job whose",
        "result depends on theirs is the aggregate verdict, so that is the one worth requiring.",
        "",
        "The earlier entry argued the opposite from a real constraint: the verdict carries",
        "`if: always()`, and the hygiene lane's property 2 rejected any condition on a declared",
        "job, because a skipped job reports SUCCESS to branch protection. That approximation is",
        "right for every condition except this one — `always()` is what guarantees the job",
        "runs. The lane now says so, and a condition that is not `always()` is still fatal.",
        "",
        "Its dependencies DO carry conditions (`check-types`, `test` and the rest run on",
        "`needs.detect.outputs.full`), and that is the arrangement rather than a hole: the",
        "verdict is derived by iterating the serialized `needs` map, and its accepting set",
        "includes `skipped`. A documentation-only pull request skips those lanes, the verdict",
        "runs anyway and passes; a failing lane makes it fail. Neither is reachable while the",
        "context sits on a job that never looks at them.",
        "",
        "The executed-instance figure in the spec is written as 'at most 5 executed instances,",
        "4 once the job named `build` may carry a condition'. The fourth is not yet realised:",
        "`build` still carries no condition, so it still runs on a documentation-only pull",
        "request, and making it conditional is a separate change with its own measurement.",
        "The fifth instance is the lane that runs the agent-integration mirror guards, which",
        "took a runner of its own and which lane selection may never skip."
      ],
      "dependencies": [
        "detect",
        "lint",
        "mirror-surface",
        "check-types",
        "check-types-future",
        "node-floor",
        "test",
        "scanner-coverage",
        "build"
      ],
      "dependenciesNote": [
        "The EXACT set of jobs the aggregate depends on, and the reason it is pinned here rather",
        "than left to the workflow: `${{ toJSON(needs) }}` contains only the jobs still listed in",
        "`needs:`, so deleting a name from that list removes a whole lane from the verdict while",
        "the lane keeps running and keeps failing. Review finding [80] measured the consequence —",
        "`lint`, `check-types`, `test` and `scanner-coverage` all deleted, all still red, and the",
        "required context green, with the topology test that would notice sitting inside the very",
        "`test` job that had been removed from the aggregate.",
        "",
        "Equality, in both directions. A missing name is the attack; an EXTRA name is a lane whose",
        "result now gates the merge and whose addition nobody declared, and the discipline in this",
        "file is that a change to what the required context means is a change a reviewer reads.",
        "Adding a lane is two edits on purpose."
      ],
      "unconditionalDependencies": ["lint", "mirror-surface"],
      "unconditionalDependenciesNote": [
        "These jobs host the lint lanes that run on every pull request.",
        "Each must remain in needs, carry no condition, and stay absent from dependencyConditions.",
        "The topology test independently resolves every exempt lane to its host and checks this list."
      ],
      "dependencyConditions": {
        "check-types": "${{ needs.detect.outputs.full == 'true' }}",
        "check-types-future": "${{ needs.detect.outputs.full == 'true' }}",
        "node-floor": "${{ needs.detect.outputs.full == 'true' }}",
        "test": "${{ needs.detect.outputs.full == 'true' }}",
        "scanner-coverage": "${{ needs.detect.outputs.full == 'true' }}"
      },
      "dependencyConditionsNote": [
        "Which dependencies may be SKIPPED, and on exactly what. The verdict's accepting set",
        "includes `skipped`, because a documentation-only pull request legitimately runs none of",
        "these lanes — so the skip decision is load-bearing, and every part of it has to be pinned",
        "",
        "VERBATIM, `${{ }}` included. This lane evaluates no GitHub expression, and the literal is what a reviewer reads in the diff — so an equivalent rewriting is still a change to the skip topology and is still reported.",
        "or the acceptance of `skipped` becomes the hole.",
        "",
        "A job listed here must carry that condition verbatim. A job NOT listed must carry no",
        "condition at all. Review finding [81]: `if: false` on `test`, or any narrowing of the",
        "expression, skips the lane, the verdict accepts the skip, and nothing else in the tree",
        "notices.",
        "",
        "`detect`, `lint`, `mirror-surface` and `build` are deliberately absent, which is the assertion",
        "that they run on every pull request. `mirror-surface` cannot be skipped because the surface it",
        "checks — the rule mirror, the skill links, the reviewer vocabulary — is what a change to those",
        "documents breaks, and such a change is exactly the one a detection-derived condition would",
        "route past it. `BR-0017-0011` binds that on review: a lane moved into a job of its own gains",
        "neither a condition nor an entry above.",
        "a dependency condition."
      ],
      "dependencyMatrices": {
        "test": {
          "slice": [
            "core",
            "validators",
            "integration",
            "e2e",
            "cli",
            "unit",
            "scripts",
            "pr-fix",
            "pr-merge"
          ]
        },
        "node-floor": {
          "slice": [
            "core",
            "validators",
            "integration",
            "e2e",
            "cli",
            "unit",
            "scripts",
            "pr-fix",
            "pr-merge"
          ]
        }
      },
      "dependencyMatricesNote": [
        "The matrix values a gated lane expands over, pinned exactly.",
        "",
        "Review finding [97]: `Run tests (${{ matrix.slice }})` is digested with the EXPRESSION in it,",
        "so the digest says nothing about what the expression expands to. Replacing a value with",
        "`unit || true #` hands the shell `pnpm -C packages/qfai test:unit || true #`, which succeeds",
        "whatever the tests did — and removing values from the list drops whole slices — while the",
        "pinned body and every other property stay exactly as they were.",
        "",
        "Equality, in both directions and in order. A dropped value is a slice that stopped running; an",
        "added one is a leg whose check name branch protection has never seen; and a REWRITTEN one is",
        "shell the digest cannot see. This is the same obligation `verificationBodies` carries for a",
        "step's text, applied to the other half of what the step actually runs.",
        "",
        "TWO lanes are sliced now, and each is pinned separately. `node-floor` runs the same nine",
        "slices, so a value rewritten in its list reaches its shell exactly as one rewritten in the",
        "`test` job's reaches that one — and a value dropped from it stops a slice being exercised on",
        "the engines floor while the `test` job still runs it and the lane still reports green. That",
        "the two lists are identical is the point and not a reason to share one entry: they are two",
        "declarations in the workflow, so they are two declarations here."
      ],
      "gateOutputs": {
        "detect": {
          "full": "${{ steps.classify.outputs.full }}",
          "reason": "${{ steps.classify.outputs.reason }}"
        }
      },
      "gateOutputsNote": [
        "The wiring the conditions above read, pinned expression by expression. Review finding",
        "[81]: rewiring `detect.outputs.full` to `${{ false }}` — or to a step output that does not",
        "exist — skips every gated lane without touching a single condition or a single line of the",
        "classifier, and `skipped` is accepting. The classifier's own tests are in `test`, which is",
        "one of the lanes that skips.",
        "",
        "Equality again, and one more property the lane checks: an expression of the form",
        "`${{ steps.<id>.outputs.<name> }}` must name a step that job actually declares with that",
        "`id`. A mapping that reads a step nobody defines is a mapping that always answers empty,",
        "which is the same skip by a quieter route.",
        "",
        "The classifier's BODY is pinned separately, by its membership in `verificationSet` and its",
        "digest in `verificationBodies`: the wiring and the program are two edits and both are read.",
        "",
        "That claim was FALSE when it was written, and an audit of this file found it: the classifier",
        "step was in neither list, so the wiring was pinned and the program behind it was not. Two",
        "literal edits inside its heredoc — `emit(false, 'docs-only')` — skipped five of the eight lanes",
        "into an accepting `skipped`, including the lane whose tests execute that very heredoc. It is a",
        "member of `verificationSet` now, and `detect` carries no condition, so it belongs to the",
        "unconditional set rather than to `gatedVerifications`."
      ],
      "commandFiles": ["GITHUB_ENV", "GITHUB_PATH"],
      "commandFilesNote": [
        "The workflow command files, pinned here and READ from `.github/command-files.txt`.",
        "",
        "Two readers need the same list — the hygiene lane, which refuses a step inside this context's",
        "closure whose surface reaches one, and `scripts/check-toolchain-action.sh`, which refuses a",
        "composite action that reaches one before any action runs — so the list is a file both read",
        "rather than a literal in each.",
        "",
        "And that file is pinned here, because it is the one input neither reader's own pin covers: the",
        "pre-flight step's body digest hashes the script, not the data it opens at runtime. Dropping",
        "`GITHUB_PATH` from the file alone would leave one name behind, both readers narrowed, and every",
        "check still reporting PASS. Equality against this list makes that a finding; changing both is a",
        "change a reviewer reads."
      ],
      "installLifecycle": {
        "package.json": { "preinstall": "node ./scripts/check-not-a-dependency.mjs" },
        "packages/qfai/package.json": {}
      },
      "installLifecycleNote": [
        "The install lifecycle scripts of every manifest the toolchain installs from, verbatim.",
        "",
        "Review finding [105]: `pnpm install --frozen-lockfile` runs `preinstall`, `install`,",
        "`postinstall` and `prepare` from the root manifest — in EVERY job, inside the composite action,",
        "before every verification in that job — and no verification digest reaches them. They are",
        "invoked by the package manager rather than by a step body, so the resolution that follows",
        "`pnpm ci:lint` into its script never sees them.",
        "",
        "Pinned verbatim rather than by digest, because these are short and a reviewer should read the",
        "command itself in this file. An empty object for a manifest that declares none is the claim",
        "that it declares none, and adding one is a change to this file.",
        "",
        "The KEY SET is checked too, against the manifests the workspace installs. Review finding [107]:",
        "the loop walked the manifests this object names, so deleting a manifest's key here and adding a",
        "`prepare` to that manifest reported nothing — and `pnpm install --frozen-lockfile` runs a",
        "workspace package's lifecycle exactly as it runs the root's."
      ],
      "pinnedBytes": {
        ".github/actions/setup/action.yml": "c60bf87cda546431c66af0a2b47df1a2a3664540d3fd40b9663ee6f4cf8553d7",
        ".github/actions/setup/dependency-builds.txt": "0788adad7ac485ca2c340c2c75ed5bf581a1ba3bb17ea6fe0d0994f666dabf2f",
        ".github/actions/setup/verify-rebuild-sources.mjs": "0816918db6c8833c8d1c4f0cc6218b9eeae7b16a718a0b3113898ae026e4127f",
        "scripts/check-atdd-annotation-ledger.mjs": "1891bdeb48e1503a810001f6c917f8f2c5e4430124792f721c8c4bda86f4413f",
        "scripts/check-bidi.mjs": "b006fcaaa399a3718a55f6e85f12fb3e4f87f22e067d159f6ef05c0f9739e9f4",
        "scripts/check-build-warnings.mjs": "f07d6df10d28a9ffcb4cdf1130e379c4aadfc2a89329e5f66ae23fd117000077",
        "scripts/check-conflict-markers.mjs": "9759695e9cf3f2590a046c45640124a09ae4ce9146cf6cf24b2dfaf01e31b0da",
        "scripts/check-doc-clarity.mjs": "ca7eed5283b49a844140cbb1ed611a19a1ac979e3fe062f25f5bb17e83d6bd6f",
        "scripts/check-dogfood-backlog.mjs": "35e6b8e27a397d0184305afc05ef7c65d408b079ea8838fb8d8a7a97b1fefb2c",
        "scripts/check-instructions-size.mjs": "edc55807a4bfdaf48ae985992d2785057ce7cf130afab34ec934cc6f740b46f4",
        "scripts/check-lifecycle-manifests.mjs": "1c701af6c814c2a9e5e6d5a5c8070a58d378f35d5caec24ba0152b95b7f28786",
        "scripts/check-mdschema.mjs": "4900c69ba7e51c2766ee41579e4b41dd7edc3aaf0915f22b1432a74d8f15bebe",
        "scripts/check-mermaid.mjs": "25ee8a44086d4e2aac81db2e2049363043096ab5865ca0575d4ca184c386222a",
        "scripts/check-not-a-dependency.mjs": "ce41f9a1e9e1b63c581c3896cac3998a22cc578abfcec616abd6ecbcf032e84e",
        "scripts/check-prompt-scanner-pair.mjs": "922b13a3f93ec173b9dd34836c426585db2504d939ec0a99e93d0b80096b40d6",
        "scripts/check-publish-dry-run.mjs": "b668f5756bdc362faee523800f1e2997f03115f7905af782d88d526578f9de72",
        "scripts/check-readme-alignment.mjs": "8d150ea7bc9487c6029c578ddd4766433bf8a456a4a6f7cc87a767d446253a61",
        "scripts/check-release-notes.mjs": "249f8b5bbd828e5f04d7342f09186f99a2511ea4d3e2f41a45b976d7649cff21",
        "scripts/check-review-profile-consistency.mjs": "f12212dd069051cd19934b42c5fde751d6b6391637ef96484762a3b8dab08cb0",
        "scripts/check-scanner-coverage.mjs": "0f5d0f6d9c7a3db50ead67bbc104cf958883ec74ac523b6def4557a42ec64ed8",
        "scripts/check-shipped-ci-parity.mjs": "3340742941c90d1204bbca2b2f54baa0e329d7edbf58d7965809566256d0500d",
        "scripts/check-simplification-ledger.mjs": "e891758c0d379cb9bf2f11346330ab131a3ab1dbea8e800249a51ae8ac9a1a67",
        "scripts/check-toolchain-action.sh": "8831fd822ecc4fffebbc7b154cc4e203810c072b4d3b621e81532e9469549682",
        "scripts/check-tracked-readmes.mjs": "236bd086eb503a37747b00a6d0e98bdd7818bec054a78527e27ca20a873aa449",
        "scripts/check-tracked-scratch.mjs": "a57ff7c6e1c885c233218bbbc15188ea40b56eb8d245c689dcd0ca981d8ca923",
        "scripts/check-tracked-symlinks.mjs": "1769fe9351bb11fa3c2a1e11a45e665cf46fec970cb6b5abebd63519ba2136b2",
        "scripts/check-tsc-compiler.mjs": "14811d9aa79ca3988a07f3ee51aeac3a4ee825e793e77af69ff05cf55e39e1f1",
        "scripts/check-types-future.mjs": "c5db610e56abad0fe7962384460760915ae1f084936a3c9684656364beb50203",
        "scripts/check-workflow-hygiene.mjs": "58a6fd7de5fa5f97bd6ac703a52eed5fbac0a9388204baa55eb1366ff9830cd7",
        "scripts/derive-cli-message-allowlist-count.mjs": "6c67359c0073519a0fa5b3be5fc9866c49b1ec5bd4bce3feff985ba2fd962d87",
        "scripts/derive-e2e-callsites.mjs": "0185bc5abdcfa2fef223c02362aea44838a3c8dac53157c050d5c3285448c6d3",
        "scripts/dogfood-backlog.json": "59929472bbdb01b65ad7ed3c0709fab038584bf882daac97d5179016e7b8c62f",
        "scripts/fresh-init-findings.json": "28bf7de140686f8df16666c3d2a9e5a8dd7deaa30d5bbbfcb8eda39494b9ecfe",
        "scripts/fresh-init-findings.mjs": "032493d612d6999edef2233ef94718a84d7c21843722c4c5050f2b879d165331",
        "scripts/gen-agent-catalog.mjs": "3a4ca269aabfd40dbebdf8579bee203d5a3ead0ad49c76647b593818d0b27da7",
        "scripts/gen-codex-agents.mjs": "732b21f9b368195dafe1138b288bb755fc19f3c692f1a92c521a01ce88b36136",
        "scripts/gh-budget.mjs": "7d63b0909ea4afa667cdcf699fee0565716b8775919f99426d8436974f0dca5e",
        "scripts/lib/bounded-read.mjs": "1ca91026cd7016db2ed89d31b397985d5e18fe00a3d018b7cc0cedbbb2578444",
        "scripts/lib/pin-cost.mjs": "68c1c68351fce620f61d73b550ca3b92beecd5a150f720723730bab931c0ab43",
        "scripts/lib/ts-specifier-hook.mjs": "b6501b31043719b5bb131994fce0aa3ed28b8bf6b4e8d77035610778681dad24",
        "scripts/lib/write-declaration.mjs": "cfdd1a0a2dc37ce06a183338e3550e9d6bd169ef633cc0cfbd1110d19d5e9dc9",
        "scripts/link-assistant-tree.mjs": "75e4742e36f847622eac505365ea28f700295b2699d2d3faf591b8036600bc0e",
        "scripts/pin-cli-message-allowlist-count.mjs": "635753d2a837eba74efc62b11dcc6b2174da26988f899317f4c547b07733d71e",
        "scripts/pin-code-path-cost.mjs": "c6c8b7868e98f5acc30522485745055225ec2a04e587cc4d7b9670e6aeb11ba5",
        "scripts/pin-documentation-only-cost.mjs": "1160b750f6e44c2a9e41eabcd9f7216eb72f71a6542f7cd71ce36702e5ea2eed",
        "scripts/pin-guard-bytes.mjs": "4c914d40d0dc75cf623fe0cab2110a0106a5f519d4a258fa05891b612f929c97",
        "scripts/pin-stage-evidence-counts.mjs": "9cf2034c8ce05387e50b7b22c0cd33f2e5ff4544f3e5913e469786044a201099",
        "scripts/pin-verification-bodies.mjs": "ada370be9bd4d331a705d5a02e01ba4d4f378c862ddc62680af9ad831eb881cf",
        "scripts/run-lint-checks.sh": "8f9e06c267d2bfa5771c54e723555189e55877aa38cb482783ce1d9d5d3642d2",
        "scripts/sync-init-to-root.mjs": "f053272f5a47c1e08fd53112255b52366b4da3bd62718c1ca57d30f7787d3af3",
        "scripts/verify-pack.mjs": "98fe8d88a5b7a130737a0870393d5c69b6a5fe7136903cfc79fd6c9092e62b40"
      },
      "pinnedBytesNote": [
        "The bytes of every local composite action and every guard program, pinned by path.",
        "",
        "Review finding [95] put the actions here: refusing a command-file NAME refuses one capability",
        "out of many, and enumerating what a step may DO is the losing side of that argument. What the",
        "action IS can be pinned.",
        "",
        "Review finding [108] put the guards here, and it is the sharper of the two. `pnpm ci:lint` ran",
        "`pnpm lint` before the hygiene lane, and `eslint.config.js` is top-level code a pull request",
        "controls — so it could replace `scripts/check-workflow-hygiene.mjs` with a program that exits 0",
        "before the lane ever ran, and ESLint would report success over the replacement. The lane is now",
        "the first member of `ci:lint`, and more to the point its own bytes are verified by the",
        "pre-flight before any of that runs.",
        "",
        "Read from `.github/pinned-bytes.txt`, in `sha256sum -c` format, so the pre-flight needs no",
        "parser. Pinned here as well for the reason `commandFiles` is: the pre-flight step's body digest",
        "hashes the script, not the data it opens at runtime.",
        "",
        "The lane checks three things: the file holds what this declares, the tree hashes to what the",
        "file says, and no file under either pinned root is missing from it."
      ],
      "closureActions": [
        "actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09",
        "actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02"
      ],
      "closureActionsNote": [
        "The EXTERNAL actions a step in this context's closure may invoke directly, as",
        "`owner/repo@<40-hex sha>`, exactly.",
        "",
        "Review finding [106]: `nestedActions` covers what a LOCAL composite action reaches, and the",
        "command-file scan opens an action's contents only when the reference starts with `./` — so",
        "`uses: attacker/action@<sha>` added as a step in the closure was examined by nothing. A SHA pin",
        "makes a reference immutable and says nothing about what it does, and an action running before",
        "the verdict step can write a workflow command file.",
        "",
        "An allow-list rather than a ban, because the closure legitimately needs two: the checkout every",
        "job starts with, and the artifact upload the aggregate uses. Pinned to the SHA, so a bump is a",
        "change read here as well as in the workflow."
      ],
      "nestedActions": ["actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444"],
      "nestedActionsNote": [
        "The EXTERNAL actions a local composite action reached from this context's closure may invoke,",
        "as `owner/repo@<40-hex sha>`, exactly.",
        "",
        "Review finding [88]: the scan that reads a local action's steps followed `./` references only,",
        "so `uses: attacker/action@<sha>` inside `.github/actions/setup` was passed over without a",
        "word. `action-pin` checks that such a reference is SHA-pinned and says nothing about what it",
        "does; the pre-flight refusal reads the checkout, which is this repository's files and not the",
        "action's. So the toolchain preamble every job runs first could invoke code nothing in this",
        "repository looks at, and that code can write a workflow command file.",
        "",
        "An allow-list rather than a ban, because the preamble legitimately needs one: `setup-node` is",
        "how the toolchain gets a Node. Pinned to the exact SHA, so a bump is a change a reviewer reads",
        "here as well as in the action — which is the same discipline `action-pin` establishes one level",
        "up, applied where the reference is otherwise invisible.",
        "",
        "Steps in the WORKFLOWS themselves are not governed here: those references sit in the diff a",
        "reviewer already reads, and `action-pin` and the shipped third-party rule cover them. This",
        "closes the level where the reference is inside a file the digest does not cover."
      ],
      "preflight": {
        "job": "lint",
        "step": "Verify the toolchain action before running it",
        "mayPrecede": ["actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09"]
      },
      "preflightNote": [
        "The step that must run before any local composite action does, and the job it runs in.",
        "",
        "Review finding [82]: `lint` invokes `./.github/actions/setup` before it runs any guard, and a",
        "pull request can edit that action. A step at the top of it appending `BASH_ENV=<a script that",
        "exits 0>` to the environment file makes every later `shell: bash` step exit 0 without running",
        "its body — the hygiene lane among them. The lane reports that write and cannot report it in",
        "that scenario, because it is one of the steps that never runs.",
        "",
        "So the refusal is moved ahead of the action, and this entry is what keeps it there. The lane",
        "checks three things: the named job declares the named step; every step before it carries",
        "neither `run:` nor a local `uses:`, so nothing can have set an environment file first; and the",
        "step is in `verificationSet`, so its body is pinned like every other required verification.",
        "",
        "`mayPrecede` is the closed set of `uses:` references allowed AHEAD of it. Review finding [94]:",
        "the order check treated `run:` and local `uses:` as work and everything else as inert, so",
        "`uses: attacker/action@<sha>` could sit before the refusal — `action-pin` proves such a",
        "reference is immutable and says nothing about what it does. An external action ahead of the",
        "pre-flight can write a workflow command file, or replace the very script the pre-flight is",
        "about to run, out of the checkout it produced.",
        "",
        "`actions/checkout` is on it because the pre-flight has to read this repository's files to",
        "refuse anything, and pinned to the SHA for the same reason every other reference here is."
      ],
      "verificationSet": [
        "Classify the change against the enumerated directory lists",
        "Verify the toolchain action before running it",
        "Derive the verdict from the serialized needs map",
        "Run build & pack verification",
        "Sanity grep — no internal spec IDs or version markers leak (post-build)",
        "QFAI self-validate this repo (dogfooding — TDD gates)",
        "QFAI self-validate this repo (dogfooding — SDD gates)",
        "QFAI self-validate this repo (dogfooding — full profile)",
        "Run qfai validate gate (fail on error)",
        "Run lint gate"
      ],
      "verificationSetNote": [
        "Step names, because a step name is what a reader of the workflow sees and what a",
        "reviewer of a diff notices moving. The lane accepts an item performed by the declared",
        "job OR by a job it depends on, so relocating work into a dependency is legal and",
        "relocating it out of reach is not.",
        "",
        "The six build-side items are unchanged from when this entry named `build`: `build` is",
        "inside `ci-pass`'s `needs`, so they remain in reach and are still enumerated. The",
        "verdict's own step joins them, because the property this context carries is that the",
        "verdict was actually derived rather than that the job merely existed.",
        "",
        "The optional report upload is deliberately absent: it ends in a tolerated failure and",
        "cannot fail the job, so it is not a verification however it is named."
      ],
      "gatedVerifications": {
        "Run type check": "check-types",
        "Run future TypeScript compatibility check": "check-types-future",
        "Run the package test suite on the floor (${{ matrix.slice }})": "node-floor",
        "Run tests (${{ matrix.slice }})": "test",
        "Run scanner coverage gate": "scanner-coverage"
      },
      "gatedVerificationsNote": [
        "The work of the lanes that are ALLOWED to skip, pinned by body without being required to run.",
        "",
        "Review finding [89]: `scanner-coverage` was a declared dependency by name and condition only,",
        "so replacing its `run` with `true` left the hygiene lane silent, the job green and `ci-pass`",
        "green — while nothing else in the repository runs `pnpm ci:coverage`, so the 90% floor and the",
        "scanner coverage report stopped being checked at all. Being IN the aggregate is not the same",
        "claim as still doing the work.",
        "",
        "Separate from `verificationSet` because the property is different, not because the obligation",
        "is weaker. A `verificationSet` item must be performed UNCONDITIONALLY, and these five sit in",
        "jobs that carry `needs.detect.outputs.full` — which is the arrangement rather than a hole: a",
        "documentation-only pull request legitimately runs none of them, and the verdict accepts the",
        "skip. What must not change without a reviewer reading it is WHAT THEY DO when they run.",
        "",
        "So the lane checks each one's body digest against `verificationBodies`, requires the step to",
        "carry no condition and no `continue-on-error` of its own — the JOB's declared condition is the",
        "only one allowed, and it is pinned in `dependencyConditions` — and requires every step sharing",
        "the name to agree, for the reason `verificationBodiesNote` gives: a name is not a step.",
        "",
        "`Run lint gate` is NOT here. `lint` carries no condition, so `pnpm ci:lint` is an ordinary",
        "unconditional verification and joins `verificationSet` with the rest."
      ],
      "verificationBodies": {
        "Classify the change against the enumerated directory lists": "ff046bcf7d3956e0",
        "Verify the toolchain action before running it": "79730605f0982d18",
        "Derive the verdict from the serialized needs map": "b700f852a69f52d2",
        "Run build & pack verification": "211656e97a2f3714",
        "Sanity grep — no internal spec IDs or version markers leak (post-build)": "f98ff7751afa6bbb",
        "QFAI self-validate this repo (dogfooding — TDD gates)": "266464e881b38c15",
        "QFAI self-validate this repo (dogfooding — SDD gates)": "9efc82ef75f7a622",
        "QFAI self-validate this repo (dogfooding — full profile)": "3537948e49d1d967",
        "Run qfai validate gate (fail on error)": "64fb079758255e4c",
        "Run lint gate": "c6c25d583bf94392",
        "Run type check": "51c5eeee942920dc",
        "Run future TypeScript compatibility check": "c58cfcd12f363478",
        "Run the package test suite on the floor (${{ matrix.slice }})": "cdce2fa04dd9f130",
        "Run tests (${{ matrix.slice }})": "cdce2fa04dd9f130",
        "Run scanner coverage gate": "dbb04c7e4d66e944"
      },
      "verificationBodiesNote": [
        "A digest of each verification step's `run` / `uses` / `with` / `env`, and of the package",
        "scripts that `run` invokes — what the step DOES, with its name deliberately excluded so",
        "the pin cannot agree with itself.",
        "",
        "Membership in `verificationSet` is decided by the step's NAME, and review finding [03] on",
        "PR #794 measured what that alone is worth: renaming nothing and replacing",
        "`run: pnpm ci:build-verify` with `run: true` left the hygiene lane green while the required",
        "context verified nothing at all. The name says which obligation a step claims; this says it",
        "still discharges it.",
        "",
        "The INVOKED SCRIPTS are in the digest because `run: pnpm ci:build-verify` is a reference, and",
        "pinning a reference pins the pointer rather than the work. Review finding [36]: deleting",
        "`node ./scripts/check-publish-dry-run.mjs` from that script in the root manifest left the",
        "step's digest and this declaration in perfect agreement while the pack verification stopped",
        "happening. Resolution is transitive, so a script that calls another script does not move the",
        "problem one hop.",
        "",
        "Editing one of those step bodies — or a script one of them runs — is expected to fail this",
        "lane. Recompute with `node scripts/pin-verification-bodies.mjs` and land the new digest in",
        "the same change: that is the point, a change to a required verification is a change a",
        "reviewer reads."
      ],
      "documentationOnlyCostPin": {
        "jobs": ["build", "ci-pass", "detect", "lint", "mirror-surface"],
        "timeoutMinutesSum": 45
      },
      "documentationOnlyCostPinNote": [
        "What a documentation-only pull request costs, pinned as the jobs it executes and the sum of",
        "their declared `timeout-minutes`.",
        "",
        "A job executes on that path when nothing can prevent it: no condition at all, or a condition",
        "whose whole expression is `always()`. The aggregate verdict is in the set for that second",
        "reason — `always()` is what makes it run when its needs are skipped, which is this path.",
        "",
        "The unit is runner-minutes rather than job instances. Instances charges +1 for a change that",
        "shortens the run and adds no work, and the measure this replaced was a set equality over four",
        "job names, which refused exactly such a change.",
        "",
        "This is NOT a cost bound, and the distinction is load-bearing. Enforcement is equality against",
        "a value recomputed from the same tree, so a clause forbidding a higher cost could not fail:",
        "once the pinner has run, no state of the tree violates it. What the pin refuses is a change",
        "that did not re-pin — which is what makes a raise deliberate and puts the new figure in a diff.",
        "Whether the figure is acceptable is a cost claim, and a cost claim arrives with measured",
        "before-and-after numbers rather than on argument.",
        "",
        "`timeout-minutes` is the declared worst case, not a measurement, so the sum sits far above what",
        "the path really costs. It still catches a runaway job and a forgotten raise, which is the class",
        "a declared ceiling can catch.",
        "",
        "Re-pinned by `node scripts/pin-documentation-only-cost.mjs`, which imports the lane's own",
        "computation rather than restating it. `dependencies` and `dependencyConditions` above carry the",
        "membership half of the same rule, scoped to the jobs this context depends on — a narrower set,",
        "because the verdict executes and is not one of its own dependencies."
      ],
      "codePathCostPin": {
        "instances": 26,
        "timeoutMinutesSum": 350,
        "installInstances": 24,
        "buildJobs": ["build", "node-floor", "test"]
      },
      "codePathCostPinNote": [
        "What a code-path pull request costs, pinned as four figures recomputed from the workflow tree.",
        "",
        "Every job runs on that path, so the cost is a property of the tree rather than of a condition.",
        "`instances` is a job's matrix legs summed over the tree — what a check-name list and a bill both",
        "count. `timeoutMinutesSum` is the declared ceiling per instance summed the same way.",
        "`installInstances` is the instances that run the toolchain action, one frozen-lockfile install",
        "each.",
        "",
        "`buildJobs` names JOBS and not instances, and that is a limit stated rather than hidden. Two of",
        "the three condition their build step on the matrix leg, so an instance figure would need",
        "`matrix.slice == 'e2e' || matrix.slice == 'integration'` evaluated, and this lane evaluates no",
        "GitHub expression. The jobs figure still moves when a build is added to or removed from a job,",
        "which is the change a pin has to catch.",
        "",
        "This is NOT a cost bound. Enforcement is equality against a value derived from the same tree, so",
        "a clause forbidding a higher cost could not fail: once the pinner has run, no state of the tree",
        "violates it. What the pin refuses is a change that did not re-pin — which is what makes a raise",
        "deliberate and puts the new figure in a diff. Whether the figure is acceptable is a cost claim,",
        "and a cost claim arrives with measured before-and-after numbers rather than on argument.",
        "",
        "Nor does it compare the pin with a RUN. Nothing here reads what a run actually cost: that needs",
        "the forge's API and a finished run, which no lint lane has. A tree that was always more",
        "expensive than it declared is therefore outside this rule, and that gap is deliberate.",
        "",
        "`timeout-minutes` is the declared worst case, so the sum sits far above what the path really",
        "costs. It still catches a runaway job and a forgotten raise, which is the class a declared",
        "ceiling can catch.",
        "",
        "Re-pinned by `node scripts/pin-code-path-cost.mjs`, which imports the lane's own computation",
        "rather than restating it. Its sibling above pins the documentation-only path, where the subject",
        "is the jobs nothing can prevent from running rather than the whole tree."
      ]
    }
  ]
}
