# The bytes of every local composite action, pinned.
#
# `scripts/check-toolchain-action.sh` verifies this list BEFORE any action runs, and the
# workflow-hygiene lane verifies it again and compares it with the tree. Review finding [95]:
# the pre-flight refused a command-file NAME and nothing else, so a step added to the toolchain
# action could `printf 'process.exit(0)' > scripts/check-workflow-hygiene.mjs` — replacing the
# lane itself before it ran, and rewriting any verification source in every job that uses the
# action.
#
# Enumerating what a step may do is the losing side of that argument. What the action IS can be
# pinned instead: an edit to any of these files is refused before the action runs, and landing
# one means landing a new digest here, in the diff, next to the change.
#
# `sha256sum -c` format, so the pre-flight needs no parser: two spaces between the digest and
# the path, paths relative to the repository root.
7ab9263cfe42a3b50469ea79afbc41fdbd4f53e3a1dccd72ad743f339396d0f6  .github/actions/setup/action.yml
