# Change Request

- ID: `CR-20260925-0003`
- Title: `The active pivot rule names a function the product does not implement`
- Raised by: `qfai-implement cross-spec re-review`
- Raised at: `2026-09-24T21:10:00Z`
- Class: `intent`
- Status: `approved`
- Approved by: `user (Codex interactive decision)`
- Approved at: `2026-09-24T21:13:46Z`
- Approved option: `2`
- Applied at: `2026-09-25T22:32:07Z`
- Superseded by: `-`

## Context

`spec-0012/03_Acceptance-Criteria.md#AC-0012-0026` and `06_Test-Cases.md#TC-0012-0332` require
`computePivotDirective(history)` to choose `pivot` when three recent iterations have low information
architecture and the latest has a layout anti-pattern. `TDD-0342` is `done` on that TC, but its
selector `TC-0012-0332` selects zero tests in `evaluatorReview.test.ts`, and the current
`packages/qfai/src/core/prototyping/evaluatorReview.ts` has no `computePivotDirective`. The spec
states an active product behavior absent from current runtime and tests. This is a product
obligation choice, not a title-only repair.

## Proposed change

Adopt the selected option below and rerun `/qfai-sdd spec-0012` in `re-derive` mode. Do not mark
`TDD-0342` re-reviewed until the selected obligation has either executable proof or an approved
retirement with its evidence retained.

## Options (at least 3) and recommendation

| #   | Option                                                                                                                        | Cost                              | Risk                                                                           | Recommended |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------ | ----------- |
| 1   | Preserve AC-0012-0026 and TC-0012-0332; implement `computePivotDirective` and its focused tests. Reset `TDD-0342` for proof.  | Code, tests and review            | Restores a branch the current runtime does not use; may add needless behavior. |             |
| 2   | Remove the unused pivot obligation from its active story/rule/example/case chain; retire `TDD-0342` with historical evidence. | SDD impact cascade and retirement | A previously intended pivot behavior is formally dropped.                      | ✅          |
| 3   | Defer the product decision; keep the CR open and `TDD-0342` blocked from completion.                                          | No immediate code change          | Cross-spec closure and PR completion remain pending.                           |             |

Recommendation 2 follows the current runtime, which has no consumer for `computePivotDirective`;
approval is needed because this changes an active obligation. If a current caller is found in the
owner rerun, stop and revisit the recommendation before editing.

## Blocked downstream items

| Item                 | Kind       | Why it depends on the artifact                                                                |
| -------------------- | ---------- | --------------------------------------------------------------------------------------------- |
| `spec-0012/TDD-0342` | ledger-row | Its active TC requires the absent pivot function and its stored selector executes zero tests. |

- Not blocked by this CR: the other 49 spec-0012 rows in the selector audit. They have their own
  draft CRs.
- Overlapping open CRs: `CR-20260912-0003` mentions `TDD-0342` as a matrix blocker for a potential
  later 2a rerun, but does not authorise its retirement or split.

## Impact scope

- Specs: `spec-0012`
- Plans: `.qfai/specs/spec-0012/10_Plan.md` if it binds the pivot obligation
- Tests: `packages/qfai/tests/core/prototyping/evaluatorReview.test.ts` if option 1 is selected; no
  test deletion under option 2 without ownership review
- Contracts: none identified
- Schema: none
- Upstream paths edited under this CR: `.qfai/specs/spec-0012/02_User-stories.md`,
  `.qfai/specs/spec-0012/03_Acceptance-Criteria.md`, `.qfai/specs/spec-0012/04_Business-Rules.md`,
  `.qfai/specs/spec-0012/05_Examples.md`, `.qfai/specs/spec-0012/06_Test-Cases.md`,
  `.qfai/specs/spec-0012/tdd/test-list.md`, `.qfai/specs/spec-0012/09_delta.md`,
  `.qfai/specs/spec-0012/10_Plan.md`

## Decision needed from user

Choose whether to restore the active pivot behavior, retire that unused obligation, or defer its
resolution. Option 2 is recommended because the current runtime has no `computePivotDirective`
consumer.

## Approved actions (owner skill rerun plan)

1. After explicit approval, record the selected option, approver and time in canonical
   `.qfai/decisions/CR-20260925-0003-spec-0012-pivot-rule.md`; run `/qfai-sdd spec-0012` in
   `re-derive` mode. Stage 1 Triage is `UPDATE/MODIFY` for option 1's traceability repair,
   `UPDATE/REMOVE` for option 2's active pivot obligation, or no owner write for option 3. Phase 4
   records the applied CR in `spec-0012/09_delta.md`; UI-bearing review gates apply.
2. Option 1 resets `spec-0012/TDD-0342` to `todo`, recording this CR in `DR-ID`, then
   `/qfai-implement` adds a focused falsifiable test and implementation. Option 2 retires
   `spec-0012/TDD-0342`; its current Evidence cell verbatim is
   `computePivotDirective rules (pivot / continue / refine)`. The named `evaluatorReview.test.ts`
   remains for other TCs, and the present selector selects no test. The ID is never reused. Option 3
   leaves the row and CR unresolved.

## Resolution

Option 2 applied. No caller of `computePivotDirective` exists in `packages/qfai/src`, so the
recommendation stood.

- **Removed as `UPDATE/REMOVE`:**
  - `AC-0012-0026` and `AC-0012-0027`, the `pivot` and `continue` / `refine` criteria;
  - `BR-0012-0021`, the rule both criteria state;
  - `BR-0012-0022`, `ordinalIndex`, whose only criterion was `AC-0012-0027` and which no source
    defines;
  - `EX-0012-0111`, the `pivot` example, and `EX-0012-0119`, the `ordinalIndex` example, whose
    only case was `TC-0012-0333`;
  - `TC-0012-0332` and `TC-0012-0333`, the two cases.

- **Cases that also cited `EX-0012-0111`** now cite `EX-0012-0118`, the lap-catalog example of
  their own rule: `TC-0012-0320`, `TC-0012-0331` and `TC-0012-0349`. `TC-0012-0341`, the
  `pivotDirective` value-set case, keeps `AC-0012-0023` and cites no example: the only other example
  stating the value set, `EX-0012-0117`, is superseded.
- **Kept:** `US-0012-0099` and `AC-0012-0023`. The reviewer still writes `pivotDirective`, and the
  validator still checks its value set. Only the rule deriving it is gone.
- **Ledger:** `spec-0012/TDD-0342` is retired under `## TDD-ID reservations` with its `Evidence`
  kept verbatim. Its selector selected no test, so no test is deleted. Its two carrier lines in
  `tests/integration/qfai-traceability.md` and its entries in the carrier-only backlog are struck.
- **Not edited:** `10_Plan.md` binds no pivot obligation.
