# 07 Constraints

## Technical Constraints

| ID    | Constraint                                                                                                                                                                         | Rationale                               | Impact                                                   |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | -------------------------------------------------------- |
| TC-01 | Node.js >= 18.0.0                                                                                                                                                                  | current runtime baseline                | 実行環境の制約                                           |
| TC-02 | TypeScript validators は deterministic async 関数 `(root, config) => Promise<Issue[]>` に従う                                                                                      | validate repeatability                  | バリデータ実装の制約                                     |
| TC-03 | downstream execution skills は discussion pack を直接読まない                                                                                                                      | contract-first downstream を維持する    | `/qfai-sdd` 以降は specs + contracts only                |
| TC-04 | `/qfai-sdd` は discussion UIUX sidecar を `.qfai/contracts/design/**` と `.qfai/contracts/ui/**` に正規化する                                                                      | downstream truth source を固定する      | SDD preflight / generation の制約                        |
| TC-05 | repo-root `qfai validate` は contract-first validator 群を primary path にする                                                                                                     | current code posture に一致させる       | discussion direct-pack validation と分離                 |
| TC-06 | `runCanonicalUixValidators` は direct discussion-pack validation 専用                                                                                                              | latest discussion pack の暗黙解決を防ぐ | repo-root downstream validate では primary path にしない |
| TC-07 | declared screen ごとの screenshot / HTML snapshot は両方 mandatory                                                                                                                 | fail-closed evidence gate               | `QFAI-UIE-001/002` の blocking 条件                      |
| TC-08 | design contracts の canonical file set は `exploration-brief.yaml`, `evaluation-rubric.yaml`, `evaluator-calibration.yaml`, `selected-direction.yaml`, `design-system.yaml` とする | exploration-first downstream を固定する | `QFAI-DCON-*` validator 対象                             |
| TC-09 | UI contracts は `.qfai/contracts/ui/*.yaml` を canonical set とする                                                                                                                | screen contract truth source を固定する | downstream evaluator / validator の入力制約              |
| TC-10 | `qfai prototyping` CLI と removed runtime/full-harness engine を public contract として再導入しない                                                                                | skill-first posture を維持する          | public interface の制約                                  |
| TC-11 | breakthrough trigger は `allReviewerAxesPerfect100`, score delta, diff lines を使う deterministic な機械判定とする                                                                 | 再現性と監査性を保つ                    | `.qfai/evidence/breakthrough.json` が必要                |
| TC-12 | design system は discussion 初期入力ではなく、prototyping の winner direction から抽出する                                                                                         | 早期収束を防ぐ                          | discussion / SDD / prototyping の責務分離                |

## Operational Constraints

| ID    | Constraint                                                                                                              | Rationale                        | Impact                                   |
| ----- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ---------------------------------------- |
| OC-01 | `/qfai-prototyping` は唯一の public prototyping path とする                                                             | 誤用防止                         | CLI surface の単純化                     |
| OC-02 | `/qfai-verify` は `qfai validate --fail-on error` と review artifact を completion gate に含める                        | reviewer gate を必須化           | verify の制約                            |
| OC-03 | downstream validate は discussion artifact の存在で contract 欠落を代替してはならない                                   | fail-open 防止                   | contract 欠落は error                    |
| OC-04 | historical wording は `07_Decisions.md`, `09_delta.md`, `_policies/08_Decisions.md`, `_policies/10_delta.md` にのみ残す | active layer の誤読防止          | active 01..06/10 の整合制約              |
| OC-05 | prototyping は初期 funnel `5->3->2->1` と polish loop を持ち、停滞時は breakthrough branch 2 本を強制比較する           | exploration-first quality system | branch planner / plateau detector の制約 |

## Business Constraints

| ID    | Constraint                                                | Rationale           | Impact                          |
| ----- | --------------------------------------------------------- | ------------------- | ------------------------------- |
| BC-01 | drift recovery は upstream owner skill rerun を必須とする | SSOT 修正責任を保つ | downstream での場当たり修正禁止 |
| BC-02 | breaking changes は delta と migration expectation を伴う | ユーザー説明責任    | 文書管理の制約                  |

## CHG-003 (Assistant-layer Recut + Work-log Surface, 2026-05-22)

### Technical Constraints

| ID    | Constraint                                                                                                                                | Rationale                                                                     | Impact                                                                                                                  |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| TC-65 | `.qfai/assistant/` の top-level entry は `constitution/` / `manifest/` / `catalog/` / `process/` / `agents/` / `skills/` の 6 つに限定    | layer 概念を構造で強制                                                        | `assistantAssets.ts` が enumerated 以外を reject                                                                        |
| TC-66 | `.qfai/steering/` は project-root に置き `.qfai/assistant/` 配下に置かない                                                                | work-log は per-project surface であり global asset ではない                  | distributed surface (`package.json#files`) に含めない / `.gitignore` 既定 exclude                                       |
| TC-67 | `packages/qfai/src/core/paths/assistantPaths.ts` を assistant-path SSOT module として独占                                                 | path 漂流防止                                                                 | 文字列 literal `assistant/(steering\|manifest\|instructions\|catalog\|constitution\|process)/` を SSOT 外で lint reject |
| TC-68 | 旧 layout (`instructions/`, `steering/`, `manifest/`) は 1 minor release の間のみ validator が受理し `D-DEPRECATED-PATH` warning を発する | 既存 consumer の breaking を回避                                              | sunset version を migration memo に明記し、`qfai validate --report` の warning text に含める                            |
| TC-69 | 全 `qfai-*` SKILL.md が `project_memory:` block を持ち、読む layer を宣言する                                                             | skill ごとの load 範囲を構造化                                                | `constitution/` は always-loaded で列挙不要、宣言外の path read は validator が reject                                  |
| TC-70 | `kind: decision` entry の promotion は manual (自動 promote しない)                                                                       | per-spec `07_Decisions.md` SSOT の dilution 防止                              | promote 完了は (a) `07_Decisions.md` row 追加 + (b) entry archive + (c) `promoted-to` back-ref の 3 条件                |
| TC-71 | Reviewer Gate `R-*` finding は `justification:` 非空が必須                                                                                | heuristic な drift 検査を advisory-failing にする (mute と hard-block の中間) | `justification:` 欠落 finding は `qfai validate` が reject                                                              |

### Operational Constraints

| ID    | Constraint                                                                                                                                                                   | Rationale                                       | Impact                                                               |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------- |
| OC-50 | implementing PR は file relocate (25+ files) + path SSOT module + validator/CLI 更新 + SKILL.md 更新 + tests + migration memo を atomic に commit                            | 部分移行はバリデータ曖昧化を招く                | CI は partial moves を reject                                        |
| OC-51 | `.qfai/steering/` は `.gitignore` 既定 exclude。project は override で opt-in 可能                                                                                           | accidental secret commit 防止                   | seeded README で commit-by-default vs opt-out 選択を案内             |
| OC-52 | 配布物 (`packages/qfai/package.json#files`) には `assets/init/.qfai/steering/{README.md,.gitkeep,_templates/entry.md}` のみ含め、それ以外の `.qfai/steering/*.md` は含めない | user data を distributed surface に混入させない | layer 3 smoke test (`distributedSurfaceLeakage.test.ts`) が enforces |
| OC-53 | migration memo (`process/migrations/v<X.Y.Z>-assistant-layer-recut.md`) は commit 後 edit せず、修正は follow-up memo で対応                                                 | ADR-style immutability practice                 | post-merge 編集は CI 警告 (hard-fail ではない)                       |

## CHG-005 (qfai-prototyping defect remediation, 2026-05-24)

### Operational Constraints

| ID    | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Rationale                                                                                                                                                                                                                                                                                          | Impact                                                                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OC-60 | One-minor-release deprecation window: validators / probes / readers MUST accept BOTH the old form (legacy `playwright-cli` wrapper, legacy `prototyping.json` shape, legacy `.qfai/output/validate.json` path, legacy `iterate-plan.json` without Capture Contract) AND the new form during the window opened in qfai 1.9.1. Sunset = qfai 1.10.0; from sunset onwards, old forms emit `error` instead of `warning`.                                                                                     | NFR-0111: downstream consumers on qfai@1.8.10 (the four reporter projects in SRC-0001..0004) MUST be able to upgrade incrementally without hand-patching every artifact in a single commit. CHG-003's path-recut precedent already established the one-minor cadence as the minimum viable window. | All `D-DEPRECATED-PROBE` / `D-DEPRECATED-PATH` / `D-DEPRECATED-SCHEMA` finding severities are window-aware. CI lint stays green on legacy artifacts during 1.9.x; CI must flip to error-fail at the 1.10.0 boundary. Implementers MUST not delete the legacy code paths inside the window — only mark them deprecated. |
| OC-61 | Migration memo immutability: once `.qfai/assistant/process/migrations/<pinned-version>-prototyping-defect-remediation.md` is committed, edits trigger a Reviewer-Gate `warning` (mirrors the ADR-style precedent established by the `v1.4.27-atdd-alignment.md` memo and re-affirmed by CHG-003 OC-53). Substantive corrections MUST be authored as a follow-up memo (`<later-version>-prototyping-defect-remediation-followup.md`) rather than rewriting the original.                                  | NFR-0109: migration memos are referenced by external recovery instructions handed to downstream consumers; their content must remain stable as a citation target. Rewriting the original silently breaks any quoted recovery step in downstream worklogs.                                          | Reviewer-Gate gains an advisory finding for post-merge edits to `process/migrations/*.md`. Hard-fail is not introduced (typo-fix latitude preserved); the warning forces explicit `justification:` per existing TC-71.                                                                                                 |
| OC-62 | Documentation drift bound after sunset: `qfai validate` MUST emit `W-SKILL-DOC-BROKEN-REF` (warning during 1.9.x, error from 1.10.0) when any `qfai-*` SKILL.md / `references/*.md` references a path, finding code, exit code, schema field, or CLI flag that no longer exists in the current layout. Coverage MUST include the four references that triggered CHG-005 (`/qfai-verify` reference in `qfai-prototyping/SKILL.md` Step H, `.qfai/output/validate.json` in `references/handoff.md`, etc.). | NFR-0108: the four reporter projects each independently lost time chasing documentation that named non-existent paths and obsolete skills. Without an automated drift check, the same class of defect will reappear with every CLI rename.                                                         | `qfai validate` lane scans skill markdown for known finding/path/flag/exit-code tokens and cross-checks against the live validator / CLI surface. Stale references that are inside the deprecation window stay as warning; after sunset they hard-fail builds.                                                         |

## CHG-006 (Second-Wave Defect Remediation, v1.9.2, 2026-05-27)

### Operational Constraints

| ID    | Constraint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Rationale                                                                                                                                                                                                                                | Impact                                                                                                                                                                                                               |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OC-63 | One-minor-release deprecation window (REQ-0169): every path or schema change in this pack ships with a backwards-compatible adapter. Validators / probes / readers MUST accept BOTH old and new forms during the window opened in qfai 1.9.2 and emit `D-DEPRECATED-*` (warning) as appropriate. Specific applications: legacy ad-hoc handoff files (REQ-0161, `D-HANDOFF-LEGACY-FORMAT`), pre-mutation-log iterate emit (REQ-0165), `surface_type`-absent specs (REQ-0163, `D-SURFACE-TYPE-MISSING`), `QFAI-AUD-020` string-only `primary_tasks` (REQ-0164). Sunset = qfai 1.10.0; from sunset, old-form acceptance is removed and warnings promote to error. Pre-emit-skeleton cycle-0 behavior (REQ-0150) is opt-in; default unchanged. | NFR (REQ-0169) mirrors the v1.9.1 `OC-60` precedent exactly: downstream consumers on qfai@1.9.1 must upgrade incrementally without hand-patching every artifact in one commit.                                                           | All `D-DEPRECATED-*` finding severities are window-aware. CI lint stays green on legacy artifacts during 1.9.x; flips to error-fail at the 1.10.0 boundary. Legacy code paths MUST NOT be deleted inside the window. |
| OC-64 | Migration memo immutability (REQ-0170): once `.qfai/assistant/process/migrations/1.9.2-second-wave-defect-remediation.md` is committed, edits trigger a Reviewer-Gate `warning` (mirrors OC-61 / CHG-003 OC-53). Substantive corrections MUST be authored as a follow-up memo (`<later-version>-second-wave-defect-remediation-followup.md`) rather than rewriting the original.                                                                                                                                                                                                                                                                                                                                                           | Migration memos are citation targets in downstream recovery instructions; rewriting silently breaks any quoted recovery step in downstream worklogs.                                                                                     | Reviewer-Gate gains an advisory finding for post-merge edits to `process/migrations/*.md`. Hard-fail is not introduced (typo-fix latitude); the warning forces explicit `justification:` per TC-71.                  |
| OC-65 | Pack-location CI lane (REQ-0167): `packages/qfai/scripts/check-pack-locations.mjs` MUST lint staged/changed dirs for `review-*/` or `discussion-*/` directories outside the allowed roots (`tmp/` per `.agents/rules/temporary-files.md`, `.qfai/review/<ts>/`, `.qfai/discussion/<ts>/`) and be wired into `pnpm ci:lint`. Misplaced packs are rejected with `R-PACK-LOCATION-DRIFT` referencing `.agents/rules/root-additions-policy.md` and the corrected path.                                                                                                                                                                                                                                                                         | NFR (REQ-0167): structural enforcement of `root-additions-policy.md` rule 6, which was textual-only. Review packs landing at repo root or under arbitrary dirs (the recurring drift) are caught at PR time rather than in manual review. | CI lint job gains the pack-location lane. Scope = staged/changed dirs only (not a full-tree walk) so legacy packs on unrelated PRs are not re-flagged (per DR-0274).                                                 |
| OC-66 | Envelope-deviation audit record + autopilot governance (REQ-0158 / REQ-0160): `AskUserQuestion` invocations naming a four-context envelope-deviation (skill envelope / architectural decision / rejected-option re-adoption / scope expansion, per DR-0270) MUST write `.qfai/evidence/decisions/<ISO8601-ts>.json`; every SKILL.md MUST carry a `## Default Autopilot Policy` section (DR-0269) and `/qfai-sdd` MUST NOT widen the auto-decide bucket beyond the policy prototype. `R-AUTOPILOT-POLICY-MISSING` (error) enforces presence.                                                                                                                                                                                                | Removes the 3–6 avoidable `AskUserQuestion` per session while preserving an audit trail for envelope deviations; auto-deciding triage / destructive / version / scope operations stays forbidden.                                        | `qfai validate` / Reviewer Gate scan SKILL.md for the policy section; `.qfai/evidence/decisions/` is git-ignored by default (mirrors `.qfai/evidence/prototyping/`).                                                 |
