# 10 Plan

## Goal

- Keep prototyping SSOT aligned to the current skill-first architecture.

## Current State

- CLI/runtime orchestration is removed from the active public contract.
- Validate/verify hold the machine gates.
- Skill/reference documents define the actual iteration workflow.

## Next Maintenance Steps

1. Keep `spec-0012` aligned with `qfai-prototyping` SKILL.md and its references.
2. When validator-only legacy slices change, update this spec without reintroducing CLI/runtime wording.
3. Keep `tdd/test-list.md` aligned with real remaining tests only.
