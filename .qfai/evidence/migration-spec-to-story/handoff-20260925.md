# Story-tree migration handoff

## Work location and pull request

- Working tree: `C:\Users\pc\Documents\GitHub\QFAI\.claude\worktrees\qfai-specs-restructure-ec45fd`
- Branch: `claude/qfai-specs-restructure-ec45fd`
- Pull request: `https://github.com/aganesy/QFAI/pull/2261`, targeting `feature/v2.0.0`
- This task's separate Codex checkout: `C:\Users\pc\.codex\worktrees\710c\QFAI`. The implementation commits are in the working tree above.
- The user requested an immediate stop and a committed, pushed handoff. The pull request has **not** been merged.

## Authoritative inputs

- Discussion pack: `.qfai/discussion/discussion-20260923063306456/`, including `99_delta.md` for changed direction.
- Inherited Claude Code session log: `C:\Users\pc\.claude\projects\C--Users-pc-Documents-GitHub-QFAI--claude-worktrees-qfai-specs-restructure-ec45fd\f0bdcc77-8dc1-4976-973b-d9ca34906cef.jsonl`.
- The accepted decisions are four business flows, explicit `DG-NNNN` entries in policy or contract files, and D12–D17 correction with one confirmation review. `plan.yaml`, `id-map.json`, and the disposition records in this evidence directory give the actual mapping.
- The user manually removed the 11 remaining old `.qfai/spec/spec-*` packs after an automated deletion was refused. Their sources are retained under `retired/`.
- This repository's `AGENTS.md`, `.agents/rules/`, and the target branch's `REVIEW.md` govern follow-up work. Source changes to assistant assets belong under `packages/qfai/`, not the generated `.qfai/assistant/` mirror.

## Completed and verified

- The package, CLI, shipped assets, and this repository's own artifacts use the story tree. `plan.yaml` maps 241 active stories to four flows (190/23/16/12); `id-map.json` fixes the converted IDs. The architecture reviewer checked 447 AC, 514 BR, 76 old EX provenance entries, and 14 archived pack hashes.
- Migration step 7 rejects conflicting BR content before writing. Step 4 can rerun with only retired packs present. Step 6 protects a manually corrected AC reference on rerun.
- The complete initial reports and a later set of 30 measured dry/real/rerun reports are tracked under `reports/`. All 30 later invocations reported no operations. Initial measured exit codes were not retained; `initial-run/expected-exit-codes.csv` labels inferred values and one unknown value. Do not present them as measured.
- Full validation on the old-pack-free tree reports 615 `QFAI-STORY-006` missing test annotations and **zero layout or chain errors**. `reports/full-validation-20260925.log` lists every gap. The ratchet carries inherited debt; this is not an ATDD PASS.
- Latest local checks before the handoff changes: package build, `format:check`, ESLint, `check-types`, `check-types:future`, Markdown lint/schema, `verify-pack`, and tracked scratch check passed. Test suites were run only in CI, as instructed by the user.

## Current blockers

1. CI run `36061758509` on commit `573fa1fd6` failed test jobs. The failures are in `tests/assets/assets.test.ts`, `tests/cli/usageExitCodes.test.ts`, `tests/e2e/bf0001DevelopVerifyFlowE2E.test.ts`, `tests/e2e/bf0004MigrationCutoverE2E.test.ts`, `tests/e2e/bf0004MigrationOutcomesE2E.test.ts`, `tests/e2e/spec0018MigrationJourneyE2E.test.ts`, and `tests/integration/bf0004Acceptance.test.ts`. Failed logs are in ignored `tmp/ci5-failed.log`. Build, lint, type, core, unit, validators, and scripts jobs passed. Do not run local Vitest suites; use CI for test execution.
2. The completion reviewer returned **REVISE** on the P7 migration gate. The final step 8 report has 34 `For a person` annotations. Seventeen are intentional old-format migration test inputs and need explicit row-by-row disposition. Seventeen are active test annotations and must be mapped to current BF/AC/EX only where the assertion proves them, or removed with a retirement reason. `reports/final-rerun/step08-real.md` lists each location. Two interrupted workers began this classification, but no committed changes were present at handoff.
3. Step 5's `TC-0005-0009` row in `step05-step08-tc-disposition.csv` still says `requires_sdd_and_implementation`. Current `src/core/report.ts` and `tests/integration/storyTreePrototypingReport.test.ts` already appear to implement and assert screenshot/HTML coverage and rerun guidance. Verify against the accepted AC/EX and close the disposition accurately. Other step 5 rows have dispositions; some still describe explicit test debt.
4. Step 4 reported one archived work-log scope spanning three flows. The work-log was changed to `scope: global` with BF-0001/BF-0002/BF-0004 links and the reason was added to `step04-step07-manual-resolution.md` in this handoff commit. Rerun all ten migration steps, refresh measured reports, and confirm step 4 no longer reports this item and full validation no longer emits its work-log schema warning.
5. The final architecture mapping reviewer was waiting for that step 4 rerun. The completion reviewer needs the step 5/8 dispositions and new evidence before a new verdict. CI and reviewer PASS are required before merge.

## Resume sequence

1. Confirm the branch and clean tracked status in the working tree. Inspect `tmp/ci5-failed.log`, fix the seven CI failure files against the current contracts, and use CI for all test suites.
2. Resolve the active step 8 annotations and write a row-level disposition for all 34 final findings. Close `TC-0005-0009` from observed source, behavior, and tests.
3. Build the package, rerun the ten migration steps on the final tree, refresh `reports/final-rerun/`, `p7-cutover-result.md`, and full validation evidence. Preserve the honest first-run exit-code limitation.
4. Run local static and package gates, push, wait for CI green, and obtain independent architecture/completion PASS on the final tree. Update the PR body, which currently says the latest CI and final review are pending.
5. Merge PR #2261 into `feature/v2.0.0` only after its gates pass. On that pinned branch, set the package version to 2.0.0, roll the CHANGELOG heading, and commit `chore(release): qfai 2.0.0`. Then create and validate a PR into `main` and merge it. Do not tag, publish, force-push, or amend.
