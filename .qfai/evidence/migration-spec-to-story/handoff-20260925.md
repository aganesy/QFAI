# Story-tree migration handoff

## Work location and pull request

- Working tree: `C:\Users\pc\Documents\GitHub\QFAI\.claude\worktrees\qfai-specs-restructure-ec45fd`
- Branch: `claude/qfai-specs-restructure-ec45fd`
- Pull request: `https://github.com/aganesy/QFAI/pull/2261`, targeting `feature/v2.0.0`
- This task's separate Codex checkout: `C:\Users\pc\.codex\worktrees\710c\QFAI`. The implementation commits are in the working tree above.
- The user requested an immediate stop and a committed, pushed handoff. The pull request has **not** been merged.

## Authoritative inputs

- Discussion pack: `.qfai/discussion/discussion-20260923063306456/`, including `99_delta.md` for changed direction.
- Inherited Claude Code session log: `C:\Users\pc\.claude\projects\C--Users-pc-Documents-GitHub-QFAI--claude-worktrees-qfai-specs-restructure-ec45fd\f0bdcc77-8dc1-4976-973b-d9ca34906cef.jsonl`.
- The accepted decisions are four business flows, explicit `DG-NNNN` entries in policy or contract files, and D12–D17 correction with one confirmation review. `plan.yaml`, `id-map.json`, and the disposition records in this evidence directory give the actual mapping.
- The user manually removed the 11 remaining old `.qfai/spec/spec-*` packs after an automated deletion was refused. Their sources are retained under `retired/`.
- This repository's `AGENTS.md`, `.agents/rules/`, and the target branch's `REVIEW.md` govern follow-up work. Source changes to assistant assets belong under `packages/qfai/`, not the generated `.qfai/assistant/` mirror.

## Completed and verified

- The package, CLI, shipped assets, and this repository's own artifacts use the story tree. `plan.yaml` maps 241 active stories to four flows (190/23/16/12); `id-map.json` fixes the converted IDs. The architecture reviewer checked 447 AC, 514 BR, 76 old EX provenance entries, and 14 archived pack hashes.
- Migration step 7 rejects conflicting BR content before writing. Step 4 can rerun with only retired packs present. Step 6 protects a manually corrected AC reference on rerun.
- The complete initial reports and a later set of 30 measured dry/real/rerun reports are tracked under `reports/`. All 30 later invocations reported no operations. `reports/final-rerun/` was refreshed on the build of commit `8c0d01e0f`, and repeated on the build of `cb4b3e669`, the last migration source change, with the same reports and exit codes. The exit codes are unchanged (3 for steps 5 and 8, 0 elsewhere), the working-tree hash was the same before and after, and only the line numbers in the step 8 report moved. Initial measured exit codes were not retained; `initial-run/expected-exit-codes.csv` labels inferred values and one unknown value. Do not present them as measured.
- Full validation on the old-pack-free tree reports 613 `QFAI-STORY-006` missing test annotations and **zero layout or chain errors**. `reports/full-validation-20260925-101327.log`, on the build of `8c0d01e0f`, lists every gap. The earlier `reports/full-validation-20260925.log` reported 615. The ratchet carries inherited debt; this is not an ATDD PASS.
- Local checks on `f375da0a4`, before the resumed work: package build, `format:check`, ESLint, `check-types`, `check-types:future`, Markdown lint/schema, `verify-pack`, and tracked scratch check passed. Test suites were run only in CI, as instructed by the user.

## Current blockers

1. **Resolved.** CI run `36061758509` on commit `573fa1fd6` failed test jobs in `tests/assets/assets.test.ts`, `tests/cli/usageExitCodes.test.ts`, `tests/e2e/bf0001DevelopVerifyFlowE2E.test.ts`, `tests/e2e/bf0004MigrationCutoverE2E.test.ts`, `tests/e2e/bf0004MigrationOutcomesE2E.test.ts`, `tests/e2e/spec0018MigrationJourneyE2E.test.ts`, and `tests/integration/bf0004Acceptance.test.ts`. Commit `092dfaee2` fixed them: the step 9 link repair in `src/cli/commands/init.ts`, and tests updated to the accepted contracts. `bf0004Acceptance.test.ts` was not edited and passes on the fixed source. Commit `d4e03350a` resealed the pinned digests after the dogfood backlog moved. CI on `d4e03350a` concluded `success`. Test suites still run only in CI.
2. **Resolved.** The completion reviewer returned **REVISE** on the P7 migration gate because 34 step 8 `For a person` annotations had no disposition. Commit `092dfaee2` added `step08-for-a-person-disposition.md`, with one row per annotation. Seventeen are migration inputs, kept. Of the 17 active ones, one is remapped to `QFAI:AC-0001-0167-01` and 16 are removed with a reason. The refreshed `reports/final-rerun/step08-real.md` lists 18 lines, all migration input. A new completion verdict is still needed; see item 5.
3. **Resolved.** Commit `092dfaee2` closed step 5's `TC-0005-0009` row against EX-0001-0067-01 to 03, with no SDD change. `src/core/report.ts` implements the report section, and `QFAI:EX-0001-0067-02` was added to the case that proves it. Commit `6c81cc7a3` narrowed that case's EX-0001-0067-02 assertion to the prototyping section of the report. EX-0001-0067-01 and 03 still lack a test (Follow-up in `step08-for-a-person-disposition.md`). Other step 5 rows have dispositions; some still describe explicit test debt.
4. **Resolved.** The refreshed `reports/final-rerun/step04-*.md` report no item and exit 0, and `reports/full-validation-20260925-101327.log` has no `W-WORKLOG-SCHEMA` warning for the `spec-0003` work-log. Step 4 reported one archived work-log scope spanning three flows. The work-log was changed to `scope: global` with BF-0001/BF-0002/BF-0004 links and the reason was added to `step04-step07-manual-resolution.md` in commit `f375da0a4`. Both results were read from the rerun on the build of `8c0d01e0f`.
5. **Open, owned by resume step 4.** The step 4 rerun is in `reports/final-rerun/` (build of `8c0d01e0f`), and the step 5 and step 8 dispositions are in commit `092dfaee2`. The final verdicts, the P5 GO and CI on the final head are tracked there.

## Open proposals for the SDD owner

The implementation reviewer proposed two contract changes. Neither is made in this PR; each needs a change request.

| Proposal                       | Where                                                                   | Change                                                                                                                                                |
| ------------------------------ | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Package resolution             | BR-0457 in `.qfai/spec/03_contract/cli/qfai-migration-spec-to-story.md` | BR-0457 requires `node_modules/qfai` from the project root. The contract prose and `_step.mjs` use Node's module resolution. Align BR-0457 with them. |
| Exit code for an edited source | The `## Exit codes` table of the same contract                          | Step 7 now refuses a source edited after a partial run. List that refusal under exit 2.                                                               |

## Resume sequence

1. Done in `092dfaee2` and `d4e03350a`: the seven CI failure files are fixed (blocker 1).
2. Done in `092dfaee2`: the 34 step 8 findings have row-level dispositions and `TC-0005-0009` is closed (blockers 2 and 3).
3. Done on the build of `8c0d01e0f` and repeated on the build of `cb4b3e669`: the ten steps were rerun on the final tree, and `reports/final-rerun/`, `p7-cutover-result.md` and the full validation log were refreshed. The first-run exit-code limitation is kept.
4. The P5 independent reviewer GO is recorded as Met in `p7-execution-checklist.md`: the implementation-reviewer returned PASS at `3cde5f941` after the last fix (`cb4b3e669`, a respelled link rewritten through a hold). Remaining before merge: CI green on the final head and the completion and architecture PASS on the final tree. Then update the PR body, which currently says the latest CI and final review are pending. Run the local static and package gates before any further push; each later commit needs its own CI run. The two proposals above are for the SDD owner and do not block the merge.
5. Merge PR #2261 into `feature/v2.0.0` only after its gates pass. On that pinned branch, set the package version to 2.0.0, roll the CHANGELOG heading, and commit `chore(release): qfai 2.0.0`. Then create and validate a PR into `main` and merge it. Do not tag, publish, force-push, or amend.
