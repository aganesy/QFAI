# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                       | Expected                                                                                                                                                                              |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0004-0008-01 | AC-0004-0008-01 | After step 4: a `06_Test-Cases.md` row whose EX-Ref is `—`, citing one criterion, with Steps and Expected cells; step 5 run | That criterion's story's `03_Example.md` holds an EX row under the ID the map gives the case, its AC-Ref the criterion's new ID, carrying the Steps and Expected cells                |
| EX-0004-0008-02 | AC-0004-0008-01 | The same input as EX-0018-0047                                                                                              | `## Cases to examples` lists one entry: the old TC ID and the new EX ID                                                                                                               |
| EX-0004-0008-03 | AC-0004-0008-02 | After step 4: a row whose EX-Ref is `—` and whose AC-Refs is empty; step 5 run                                              | No EX row is written for it; `## For a person` lists it with its file and the reason; the row is still in the retired archive's `06_Test-Cases.md`                                    |
| EX-0004-0008-04 | AC-0004-0008-02 | After step 4: a row whose EX-Ref is `—` and whose AC-Refs names two criteria; step 5 run                                    | No EX row is written for it; `## For a person` lists it with its file and the reason                                                                                                  |
| EX-0004-0008-05 | AC-0004-0008-02 | After step 4: five rows whose EX-Ref is `—`, three citing one criterion, one citing none, one citing two; step 5 run        | `## Cases to examples` holds three entries and `## For a person` two test-case rows; 3 + 2 = 5                                                                                        |
| EX-0004-0008-06 | AC-0004-0008-03 | After step 5: an EX cited by two test-case rows that both name the same criterion; step 6 run                               | The EX's AC-Ref is that criterion's new ID                                                                                                                                            |
| EX-0004-0008-07 | AC-0004-0008-03 | After step 3: an EX whose only citing test-case row names no criterion; steps 4 to 6 run                                    | The EX stays in its pack's `05_Examples.md` with no new ID and no `id-map.json` entry; step 4's `## For a person` lists it with its file and the reason; step 6 sets no AC-Ref for it |
| EX-0004-0008-08 | AC-0004-0008-03 | After step 3: an EX cited by two test-case rows naming different criteria; steps 4 to 6 run                                 | The EX stays in its pack's `05_Examples.md` with no new ID and no `id-map.json` entry; step 4's `## For a person` lists it with its file and the reason; step 6 sets no AC-Ref for it |
| EX-0004-0008-09 | AC-0004-0008-03 | After step 3: an old example that no test-case row cites; step 4 run                                                        | The example stays in its pack's `05_Examples.md` with no new ID and no `id-map.json` entry; `## For a person` lists it with its file and the reason; exit 3                           |
