# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                     | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0004-0012-01 | AC-0004-0012-01 | The shipped `SKILL.md` of the migration skill                                                                             | It directs, in this order: write the plan; for each step, run it with `--dry-run` and then without; keep every report as evidence; after step 3, remove any paraphrased duplicate in the five merged files; after step 10, run `qfai validate`. It also directs that on a project already on the story tree with no ID map, once every step prints `none` and exits 0, the AI reports that there is nothing to migrate                                |
| EX-0004-0012-02 | AC-0004-0012-01 | A project already on the story tree with no ID map; each step run                                                         | Every step prints `none` under every section and exits 0, and the tree hash is unchanged                                                                                                                                                                                                                                                                                                                                                              |
| EX-0004-0012-03 | AC-0004-0012-02 | The fixture migrated by all ten steps, with every `## For a person` item resolved by a prepared edit; `qfai validate` run | No layout error and no chain error; the test-obligation findings name exactly the BF, AC and EX left without a test at their layer                                                                                                                                                                                                                                                                                                                    |
| EX-0004-0012-04 | AC-0004-0012-03 | The package's shipped `references/migration-guide.md` of the migration skill                                              | The file exists; it names the release that brings the story tree as `2.0.0` with no `v` before it; it states that 2.x does not read the spec-pack layout, that a project keeping that layout stays on a pinned 1.x release, and that a project upgrading runs the migration first; it gives the command that runs a step, and has a section on resolving each `## For a person` item; every example ID in it is from the sample band `0001` to `0009` |
