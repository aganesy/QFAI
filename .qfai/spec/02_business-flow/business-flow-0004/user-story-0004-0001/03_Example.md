# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                        | Expected                                                                                                                                                                                                                                             |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0004-0001-01 | AC-0004-0001-01 | Run `qfai init` in an empty directory, and separately on a project whose `.qfai/specs/` holds a `spec-*/` directory                                          | On both, `qfai-migration-v1-to-v2/` with its `SKILL.md` and `scripts/` is installed in the assistant skill tree, and its entry in each of `.claude/skills/`, `.agents/skills/`, `.codex/skills/` and `.github/skills/` is a link that resolves to it |
| EX-0004-0001-02 | AC-0004-0001-02 | Inspect the package implementation of migration step 9, which repoints the host integration links, and step 10, which updates the managed `.gitignore` block | Step 9 calls init's integration-directory writer and step 10 calls init's managed-block writer. Neither contains a link-creation, link-removal or `.gitignore` write call of its own                                                                 |
