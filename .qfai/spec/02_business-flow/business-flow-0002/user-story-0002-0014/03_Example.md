# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                       | Expected                                                                                                                                                                                                                                                                                      |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0014-01 | AC-0002-0014-01 | Two fixture workflows: one job with no job-level `permissions:` inside a workflow that declares one, and one job with neither                               | The reachability counter accepts the first and rejects the second. A declaration-only counter rejects both, which is how the two measurements are told apart — 4 of 12 reachable against 2 of 12 declared at the baseline                                                                     |
| EX-0002-0014-02 | AC-0002-0014-01 | Every job across `.github/workflows/**` after the own-tree hardening                                                                                        | Zero jobs have no reachable permission block, and every job this spec adds carries both a reachable permission block and a `timeout-minutes`                                                                                                                                                  |
| EX-0002-0014-03 | AC-0002-0014-01 | Every `permissions:` block across `.github/workflows/**` at workflow and job level, reduced to file, job and value, plus a fixture that adds a fourth block | The set of blocks whose parsed value is not exactly `{ contents: read }` equals exactly `ci.yml` `ci-pass` with `{}`, `release.yml` `github-release` with `{ contents: write }` and `release.yml` `publish` with `{ contents: read, id-token: write }`; the fixture makes the comparison fail |
| EX-0002-0014-04 | AC-0002-0014-02 | One job with both its job-level and its workflow-level permission block removed, then each of the two restored in turn                                      | Exit 1 naming the workflow and the job while both are absent; exit 0 after restoring either one. Restoring the workflow-level block alone is sufficient                                                                                                                                       |
| EX-0002-0014-05 | AC-0002-0014-03 | Every checkout step across `.github/workflows/**`, then a copy with `persist-credentials: false` deleted from exactly one step                              | The count of steps not setting the flag is 0 on the clean tree; the planted tree exits 1 and names the offending step's file and job                                                                                                                                                          |
| EX-0002-0014-06 | AC-0002-0014-03 | The two jobs that legitimately need full history, plus every workflow-level checkout default                                                                | Full-history checkout is requested on those two jobs only; no workflow-level default requests it                                                                                                                                                                                              |
| EX-0002-0014-07 | AC-0002-0014-04 | Every `uses:` value across `.github/workflows/**` and `.github/actions/**`, then a copy with one value replaced by a floating major-version reference       | All values resolve to a forty-hex commit SHA on the clean tree; the planted tree exits 1 naming that reference                                                                                                                                                                                |
| EX-0002-0014-08 | AC-0002-0014-04 | A conventional readable version trailer beside an own-CI pin, plus the diff of the distributed-surface guard set for the same change                        | `check-no-internal-version-leakage.sh` exits 0 because its root resolves to the package directory, and the guard's pattern set, pragma handling and allow-list show a zero-line diff                                                                                                          |
| EX-0002-0014-09 | AC-0002-0014-05 | Every durable repository artifact searched for a named action-pin bump owner                                                                                | DR-0017-0003 in `07_Decisions.md` names the owner as a maintainer role and binds the obligation to release preparation. A bump owner stated only in a pull-request description is not accepted, because no gate can read one                                                                  |
| EX-0002-0014-10 | AC-0002-0014-05 | The repository root after this spec's changes, plus the recorded reason for the absence of a bump configuration                                             | No automated action-bump configuration was created by any agent, and the bump owner is recorded anyway. The two halves are independent — the owner is recordable today, the configuration is not                                                                                              |
