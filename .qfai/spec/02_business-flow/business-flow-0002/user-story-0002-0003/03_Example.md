# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                        | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0003-01 | AC-0002-0003-01 | 配布 `.github/` に `actions/` ディレクトリを planted し、workflows に `ci.yml` を planted                                                                                                                                                    | `pnpm verify:pack` が `actions/` で throw する。`^qfai-[a-z0-9-]+\.yml$` に一致しない `ci.yml` は命名 assertion で reject される。planted を戻すと配布 set 全体で exit 0                                                                                                                                                                                                                                                                                |
| EX-0002-0003-02 | AC-0002-0003-01 | 配布 set の各ファイル本文から他配布ファイル名を検索し、orchestrator の job 一覧を抽出                                                                                                                                                        | 他配布ファイル名の参照が 0 件。set のファイル数は 2 以上。orchestrator は layer ごとに 1 job または 1 matrix leg を宣言している                                                                                                                                                                                                                                                                                                                         |
| EX-0002-0003-03 | AC-0002-0003-02 | Run `qfai init` for an adopter that declares no layer-named script, and separately for one that declares only `test:unit`, on a change that selects the full lane set                                                                        | With no script, every test lane of the shipped orchestrator is declared and its false condition evaluates to skipped, so zero test lanes run. With only `test:unit`, the unit lane's condition evaluates to run and every other lane stays skipped. Each condition references the presence of a layer-named script, never a credential attribute                                                                                                        |
| EX-0002-0003-04 | AC-0002-0003-02 | Over one `qfai init` output tree, count the job declarations that install dependencies, the executing installing instances each event expands them to, and every secret reference in the set                                                 | Three job declarations install dependencies: document checks, the test lane and validation. Change detection, document scope and each aggregate install nothing and carry their own timeout. With the test lane at five layer legs, installing instances run nine times on a pull request and eight times on a push. Secret declarations, secret-context references and `secrets: inherit` are 0.                                                       |
| EX-0002-0003-05 | AC-0002-0003-03 | Read the delivered document and validation workflows out of a fresh `qfai init` tree and enumerate, per file, the matrix legs, the `fail-fast` setting, the command or profile each leg selects, and the event each event-selected leg names | Each file declares its independent checks as matrix legs of one job with `fail-fast: false`. Every checker command and validation profile the file ran before appears in exactly one leg, none dropped and none duplicated. The drift profile's leg names the pull-request event                                                                                                                                                                        |
| EX-0002-0003-06 | AC-0002-0003-03 | Run each delivered aggregate job's body with its dependency result set to `success`, `failure`, `cancelled`, `skipped` and unset, and separately with the result binding removed from the job; and read each aggregate job's declaration     | Each aggregate declares an always-run condition, a `needs` list naming exactly the job it aggregates (plus the scope job on the document aggregate), and an empty permission map. Only `success` exits 0. Every other value exits 1. A job whose result binding is removed exits 1 rather than passing on an empty value. On the document aggregate, `skipped` exits 0 when the scope output is exactly `false`, and exits 1 with any other scope value |
