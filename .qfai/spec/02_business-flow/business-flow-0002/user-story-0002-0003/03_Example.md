# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                        | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0003-01 | AC-0002-0003-01 | 配布 `.github/` に `actions/` ディレクトリを planted し、workflows に `ci.yml` を planted                                                                                                                                                    | `pnpm verify:pack` が `actions/` で throw する。`^qfai-[a-z0-9-]+\.yml$` に一致しない `ci.yml` は命名 assertion で reject される。planted を戻すと配布 set 全体で exit 0                                                                                                                                                                                                                                                                                                                           |
| EX-0002-0003-02 | AC-0002-0003-01 | 配布 set の各ファイル本文から他配布ファイル名を検索し、orchestrator の job 一覧を抽出                                                                                                                                                        | 他配布ファイル名の参照が 0 件。set のファイル数は 2 以上。orchestrator は layer ごとに 1 job または 1 matrix leg を宣言している                                                                                                                                                                                                                                                                                                                                                                    |
| EX-0002-0003-03 | AC-0002-0003-02 | layer 名スクリプトを宣言していない adopter に `qfai init` を実行                                                                                                                                                                             | 配布された orchestrator の全 test lane が宣言済みかつ false 条件で skip 判定になり、実行される test lane は 0 件。条件式が参照しているのは layer 名スクリプトの存在で、credential 属性ではない                                                                                                                                                                                                                                                                                                     |
| EX-0002-0003-04 | AC-0002-0003-02 | Over one `qfai init` output tree, count the job declarations that install dependencies, the executing installing instances each event expands them to, and every secret reference in the set                                                 | Two job declarations install dependencies: the document checks and the validation profiles. Detection, verdict and both aggregate jobs install nothing, and each still declares its own timeout. After matrix expansion the executing installing instances are four on a pull request (two document checks, two validation profiles) and three on a push (two document checks, one validation profile). Secret declarations, secret-context references and `secrets: inherit` are 0 across the set |
| EX-0002-0003-05 | AC-0002-0003-03 | Read the delivered document and validation workflows out of a fresh `qfai init` tree and enumerate, per file, the matrix legs, the `fail-fast` setting, the command or profile each leg selects, and the event each event-selected leg names | Each file declares its independent checks as matrix legs of one job with `fail-fast: false`. Every checker command and validation profile the file ran before appears in exactly one leg, none dropped and none duplicated. The drift profile's leg names the pull-request event                                                                                                                                                                                                                   |
| EX-0002-0003-06 | AC-0002-0003-03 | Run each delivered aggregate job's body with its dependency result set to `success`, `failure`, `cancelled`, `skipped` and unset, and separately with the result binding removed from the job                                                | Only `success` exits 0. Every other value exits 1 and names the concluded result. A job whose result binding is removed exits 1 rather than passing on an empty value. On the document aggregate, `skipped` exits 0 when the scope output is exactly `false`, and exits 1 with any other scope value                                                                                                                                                                                               |
