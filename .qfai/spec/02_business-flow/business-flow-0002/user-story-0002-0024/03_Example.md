# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                  | Expected                                                                                                                                                                                                                                                                                            |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0024-01 | AC-0002-0024-01 | The own-CI workflow's job on `windows-latest`, and the suite list of the `test:windows-parity` script in `packages/qfai/package.json`                  | The job runs `pnpm -C packages/qfai test:windows-parity` and no other test command. The list equals the declared set compared both ways: `tests/unit/workflow/`, `tests/integration/workflow/`, `tests/cli/init`, `tests/integration/init/` and the named provenance, gitignore and migration tests |
| EX-0002-0024-02 | AC-0002-0024-01 | The Windows job's steps up to its first test step                                                                                                      | One earlier step creates a directory whose name contains a space, and the test step sets both `TEMP` and `TMP` to it                                                                                                                                                                                |
| EX-0002-0024-03 | AC-0002-0024-01 | Each entry of the `test:windows-parity` suite list, matched against the tracked files the workspace `include` globs collect                            | Every entry matches at least one collected test file. An entry for a suite that has not landed matches none and fails, naming the entry                                                                                                                                                             |
| EX-0002-0024-04 | AC-0002-0024-02 | The Windows job and `ci-pass` in the own-CI workflow                                                                                                   | The job's `needs` is `detect`, its `if:` equals the `test` job's, and it is a key of `ci-pass`'s `needs`                                                                                                                                                                                            |
| EX-0002-0024-05 | AC-0002-0024-02 | The `ci-pass` verdict body over its `needs` map with every other need `success`: once with the Windows job concluded `failure`, once with it `success` | The verdict exits 1, then 0                                                                                                                                                                                                                                                                         |
| EX-0002-0024-06 | AC-0002-0024-03 | The Windows job's steps                                                                                                                                | A step running `pnpm -C packages/qfai build` precedes the first test step                                                                                                                                                                                                                           |
| EX-0002-0024-07 | AC-0002-0024-03 | The Windows job's `needs` and steps                                                                                                                    | `needs` holds no `build`, and no step uses a download-artifact action                                                                                                                                                                                                                               |
