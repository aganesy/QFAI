# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                          | Expected                                                                                                                                                                                                                                         |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EX-0002-0016-01 | AC-0002-0016-01 | The matrix legs that build the bundle today, and whether any of them downloads it instead                      | While none downloads, the rule is satisfied and the legs that would change are named; the moment one downloads, the recorded invocation count MUST have fallen against the captured baseline and the two pack-lifecycle builds MUST be unchanged |
| EX-0002-0016-02 | AC-0002-0016-05 | A cost, wall-clock or parallelism change proposed with no captured before-and-after numbers                    | Rejected. The accepting shape is numbers written to the evidence tree **and** quoted in the pull-request description **and** in `07_Decisions.md`, because the evidence tree is ignored by git                                                   |
| EX-0002-0016-03 | AC-0002-0016-02 | A captured before-and-after measurement showing a wall-clock regression from artifact reuse                    | The rebuilds are kept and the measurement is recorded as the reason. That record satisfies the requirement; a second comparison run to obtain a different answer is forbidden                                                                    |
| EX-0002-0016-04 | AC-0002-0016-03 | The own-CI workflow after the producer and verifier split, read for the job named `build`                      | A job of that exact name exists, carries no condition of its own, no job it depends on carries one, and every item of its enumerated verification set is still performed by it or by a job it needs                                              |
| EX-0002-0016-05 | AC-0002-0016-03 | Every step of the `build` job that belongs to its enumerated verification set                                  | No such item carries `continue-on-error`. An item that keeps running while losing the ability to fail the job is a weakened item, not a preserved one                                                                                            |
| EX-0002-0016-06 | AC-0002-0016-04 | The report upload step, plus fixture steps with retention 7 and retention 8 and an unconditional run condition | The real step skips on cancellation, tolerates a missing report file and sets retention to at most 7 days; the retention-8 fixture and the unconditional fixture are both rejected                                                               |
