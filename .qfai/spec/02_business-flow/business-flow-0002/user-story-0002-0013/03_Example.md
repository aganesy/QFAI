# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                     | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0013-01 | AC-0002-0013-01 | The aggregate verdict job's body, plus a tree in which one job has been wired into its `needs` and nothing else edited                                                                                    | The body iterates the serialized needs map and holds no hand-written list of need names; the added need is evaluated with a zero-line diff to the verdict body                                                                                                                                                                                                                                                                                                                                    |
| EX-0002-0013-02 | AC-0002-0013-02 | Two synthetic needs maps: one where every need concluded `success`, one where every need concluded `skipped`                                                                                              | The verdict exits 0 for both. "Nothing needed running" and "everything passed" are the two accepting states                                                                                                                                                                                                                                                                                                                                                                                       |
| EX-0002-0013-03 | AC-0002-0013-01 | Synthetic needs maps holding `failure` and holding `cancelled`                                                                                                                                            | Exit 1 for both failed and cancelled needs                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| EX-0002-0013-04 | AC-0002-0013-02 | A synthetic needs map holds a state token the verdict does not recognize                                                                                                                                  | Exit 1; the accepting state set is closed and unknown is not silently mapped to success                                                                                                                                                                                                                                                                                                                                                                                                           |
| EX-0002-0013-05 | AC-0002-0013-01 | A proposed own-CI change adds a job to `ci-pass.needs` and replaces derived iteration with a hand-written list; the current verdict also receives that new need as failed                                 | The current verdict exits 1 naming the new need. The workflow hygiene lane rejects the proposed tree and names the changed verdict body.                                                                                                                                                                                                                                                                                                                                                          |
| EX-0002-0013-06 | AC-0002-0013-03 | The current test matrices, a documentation-only run and an approved retirement of a test suite                                                                                                            | Every current leg stays declared during change selection. Excluded jobs report `skipped` under their bare names and consume no runner minutes. The retired suite loses its project, script and leg on every matrix surface in the same change.                                                                                                                                                                                                                                                    |
| EX-0002-0013-07 | AC-0002-0013-03 | The jobs with no condition plus the verdict, whose condition is `always()`; their `timeout-minutes`; the committed pin beside the value recomputed from the tree; the declaration's unconditioned entries | That executing set, and the sum of its members' declared timeouts, equal the recomputed values; of the verdict's dependencies, the executing ones are exactly the `dependencies` entries absent from `dependencyConditions`, each named in that file's note with the reason it cannot be skipped. Where the committed pin and the recomputed value disagree, the hygiene lane exits 1 naming both, and re-pinning in the same change returns it to exit 0 with `BR-0017-0030`'s numbers beside it |
| EX-0002-0013-08 | AC-0002-0013-04 | The change-detection step run against (a) a full-history clone with a reachable base ref and (b) a shallow clone and an unreachable base ref                                                              | (a) selects the narrowed lane set and emits no annotation; (b) emits an explicit warning annotation naming the reason and selects the full lane set                                                                                                                                                                                                                                                                                                                                               |
| EX-0002-0013-09 | AC-0002-0013-05 | A change list whose only entry is a path inside no recognized directory                                                                                                                                   | The full lane set is selected. An unclassified path is never read as "nothing to run"                                                                                                                                                                                                                                                                                                                                                                                                             |
| EX-0002-0013-10 | AC-0002-0013-05 | Two change lists: one touching only Markdown under the assistant catalog tree, one touching only the agent-integration mirrors                                                                            | The assistant-tree change selects the full lane set because it alters validate output; the mirrors change classifies as documentation-only                                                                                                                                                                                                                                                                                                                                                        |
| EX-0002-0013-11 | AC-0002-0013-06 | A detection output that selects zero test lanes, plus each lint-aggregate lane's host job and the declaration's `dependencyConditions`                                                                    | Every lint-aggregate lane executes, whichever job hosts it, so the formatter, the Markdown linter, the leakage guard, the branch-version-pin guard and the agent-integration mirror guards all run on a documentation-only pull request. A lane moved into a job of its own carries no condition and appears in no `dependencyConditions` entry; a job carrying both is rejected even though the executing set still matches the declaration                                                      |
| EX-0002-0013-12 | AC-0002-0013-06 | The job named in the required-status-context declaration and its transitive `needs` closure, with a condition planted on one job it depends on                                                            | The hygiene lane exits 1 and reports the declared job as skippable, even though that job carries no condition of its own                                                                                                                                                                                                                                                                                                                                                                          |
