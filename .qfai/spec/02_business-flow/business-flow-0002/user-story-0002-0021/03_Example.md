# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                     | Expected                                                                                                                                                                                                                               |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0021-01 | AC-0002-0021-02 | The layer tokens and headings extracted from the files the policy loader reads, plus the built-in token set               | The five declared layer tokens and built-in set are unchanged. The CI mapping section introduces no new layer token or heading. Extending the built-in set to legalize CI vocabulary is rejected.                                      |
| EX-0002-0021-02 | AC-0002-0021-01 | The packaged `rule/test-layers.md`, its repository-root link, and both trees searched for the former sibling mapping file | The mapping is a section of the packaged rule, the repository-root rule reads the same section, and `catalog/test-layers-ci-lanes.md` is absent from both trees. The section names no layer token or heading outside the built-in set. |
| EX-0002-0021-03 | AC-0002-0021-01 | The CI mapping section in the shipped rule that the layer-policy loader reads                                             | The section states that it adds no layer token and does not activate per-level routing. It makes no claim that the loader ignores the rule.                                                                                            |
| EX-0002-0021-04 | AC-0002-0021-01 | The mapping section and the rule's annotation-routing table                                                               | The section does not activate per-level routing or direct any `QFAI:TC-*` annotation outside the validator's configured test scan.                                                                                                     |
| EX-0002-0021-05 | AC-0002-0021-02 | The recorded layer-vocabulary warning count, the built-in token set, and the layer codes named in the mapping section     | The warning count equals the recorded baseline, the built-in token set is unmodified, and every named layer code occurs in the taxonomy.                                                                                               |
| EX-0002-0021-06 | AC-0002-0021-03 | An asset-linked repository-root rule, then a planted regular-file mirror at the same root path                            | The link check accepts the symlink and the root rule reads the asset bytes. It rejects the regular file and names the root path and expected link.                                                                                     |
