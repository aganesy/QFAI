# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                  | Expected                                                                                                                                                                                                    |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0021-01 | AC-0002-0021-02 | Every layer token and layer heading extracted from the files the layer-policy loader reads, plus the built-in token set                                                | The five declared tokens are unchanged and the built-in set is unmodified. No new layer, layer token or layer heading is introduced, and extending the built-in set to legalize CI vocabulary is rejected   |
| EX-0002-0021-02 | AC-0002-0021-01 | The packaged asset catalog directory and the mirrored repository-root catalog directory, plus the layer catalog's links                                                | The mapping file exists in both trees, the layer catalog cross-links to it and it cross-links back. A sibling file is invisible to the layer-policy parser; a new section inside the layer catalog is not   |
| EX-0002-0021-03 | AC-0002-0021-01 | The mapping document's header block                                                                                                                                    | It states that the layer-policy loader does not read this file, so a later reader cannot mistake it for enforced vocabulary                                                                                 |
| EX-0002-0021-04 | AC-0002-0021-01 | The mapping document's prose, read against the layer catalog's statement that per-level routing is a target state that is not enforced                                 | The prose does not read as activating per-level routing, and it does not instruct a reader to place a `TC-*` annotation outside the directory the live gate scans                                           |
| EX-0002-0021-05 | AC-0002-0021-02 | The layer-vocabulary warning occurrence count before and after the mapping document lands, the built-in token set, and every layer code the document names             | The count equals the recorded baseline, the built-in token set is unmodified, and every layer code the document names also appears in the layer catalog                                                     |
| EX-0002-0021-06 | AC-0002-0021-03 | Two candidate trees: one where the repository-root mapping path is a symbolic link to the packaged asset catalog, one where it is a regular file holding its own bytes | The first leaves the link check reporting no drift and the repository-root path reading the asset's bytes; the second fails the link check, which names the path and the link it must be, so it is rejected |
