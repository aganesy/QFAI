# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                                  | Expected                                                                                                                                                                            |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0010-01 | AC-0002-0010-01 | Given three PRs: one edits `packages/qfai/src/core/validators/findDesignMdViolations.ts` without touching `packages/qfai/assets/init/.claude/skills/qfai-prototyping/references/generator-prompt.md`, one edits only that prompt, and one edits both. When the SSOT-sync-pair `pnpm ci:lint` lane runs | Then each one-sided PR fails the lane, emitting `R-PROMPT-SCANNER-DRIFT` (severity error) naming the modified path and its un-paired counterpart, and the PR that edits both passes |
| EX-0002-0010-02 | AC-0002-0010-02 | Given a PR that touches neither `findDesignMdViolations.ts` nor `generator-prompt.md` (e.g. README typo fix) When the SSOT-sync-pair lane runs                                                                                                                                                         | Then the lane passes silently with no `R-PROMPT-SCANNER-DRIFT` emission and no exit-code regression                                                                                 |
