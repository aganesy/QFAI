# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                 | Expected                                                                                                                                                                                                                    |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0015-01 | AC-0002-0015-01 | Occurrences of the frozen-lockfile install literal in the own-CI workflow and in the shared setup definition          | Zero in the workflow and exactly one in the shared definition, down from six duplications in one file                                                                                                                       |
| EX-0002-0015-02 | AC-0002-0015-01 | Every own-CI job that needs the toolchain                                                                             | Each consumes the shared setup definition; zero jobs restate any step of the preamble inline                                                                                                                                |
| EX-0002-0015-03 | AC-0002-0015-01 | The shared setup definition's step list, in order                                                                     | Package-manager shim enabled, Node set up with the package-manager cache and an explicit cache-dependency path, package manager re-shimmed against the toolcache Node, frozen-lockfile install. The re-shim step is present |
| EX-0002-0015-04 | AC-0002-0015-02 | The own workflows tree searched for workflow-level Node version literals, plus the shared definition's version source | Exactly one remains, the publish job's declared exception; the release gate carries none, and the shared definition reads the version from a file in the repository                                                         |
| EX-0002-0015-05 | AC-0002-0015-02 | The packaged asset tree searched for the shared setup definition, plus a run of the pack verifier                     | The definition is absent from `packages/qfai/assets/init/**`, and pack verification still throws on any child other than `workflows` under the shipped GitHub configuration                                                 |
