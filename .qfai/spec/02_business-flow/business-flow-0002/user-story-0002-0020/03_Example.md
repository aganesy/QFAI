# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                           | Expected                                                                                                                                                                                                                         |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0020-01 | AC-0002-0020-01 | The workflows directory listing and the trigger set of every remaining workflow                                 | The repository's own duplicate of the shipped validate workflow is absent, and exactly one workflow is triggered by a pull request                                                                                               |
| EX-0002-0020-02 | AC-0002-0020-01 | The folded full-profile validate invocation, plus the alternative that repoints it at the shipped workflow file | The run executes from the `build` job against the repository root using the locally built binary. The repoint is rejected: the root manifest declares no dependency on the package, so it would resolve to the published one     |
| EX-0002-0020-03 | AC-0002-0020-01 | The `build` job's enumerated verification set after the fold                                                    | The folded full-profile run is an item of that set, so removing it later is a release blocker rather than a cleanup                                                                                                              |
| EX-0002-0020-04 | AC-0002-0020-02 | A change deleting the duplicate validate workflow while no structural contract gate over the shipped set exists | Rejected in review. The accepting shape has the gate present in the same change or an earlier one, with the recorded justification being the loss of the manual cross-check, not the absence of a mirror that was already absent |
