# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                   | Expected                                                                                                                                                                                                                                                                                        |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0002-0018-01 | AC-0002-0018-01 | The hygiene lane run over the hardened own workflows tree                                                                                                               | Exit 0, with exactly five rules asserted: permissions and `timeout-minutes` per job, checkout credential refusal, SHA-pinned action references, matrix `fail-fast` disabled, and zero secret inheritance                                                                                        |
| EX-0002-0018-02 | AC-0002-0018-02 | The hygiene lane's output after a run that exited 0                                                                                                                     | The output enumerates each rule the run evaluated. A rule that was not evaluated is absent from that list rather than implied by the green result                                                                                                                                               |
| EX-0002-0018-03 | AC-0002-0018-03 | A positive and a negative fixture for each hygiene rule, with exactly one rule's violation planted at a time                                                            | Exit 1 naming the offending file, the offending job and the rule identifier; removing the planted violation returns the lane to exit 0. An aggregate fixture cannot show which rule fired                                                                                                       |
| EX-0002-0018-04 | AC-0002-0018-03 | The failure the hygiene lane emits, and the reviewer-finding justification catalog                                                                                      | The failure is emitted as `R-WORKFLOW-HYGIENE-DRIFT` in the bare lint-aggregate namespace. Catalog membership is a separate, severity-class question deferred under `OQ-0015-0001`; the code's present absence from the catalog is a temporary divergence and must not be asserted as permanent |
| EX-0002-0018-05 | AC-0002-0018-06 | The lint aggregate's and the release-only gate aggregate's script definitions, plus a planted violation on a pull request                                               | The hygiene lane's invocation appears in the lint aggregate or in a runner project the test matrix drives, is absent from the release-only aggregate, and the planted violation turns the pull request red                                                                                      |
| EX-0002-0018-06 | AC-0002-0018-07 | The hygiene script's inputs, and the declaration naming the job expected to carry the required status context                                                           | The declaration is read from the working tree as a checked-in file; the script makes no call to live repository settings, which a pull request could not perform anyway                                                                                                                         |
| EX-0002-0018-07 | AC-0002-0018-07 | Three planted states: the declared context resolving to no job; the declared job made skippable through a job it depends on; one item removed from its verification set | Exit 1 in each of the three. Each has its own quiet failure mode, so one property standing in for the other two is not sufficient                                                                                                                                                               |
| EX-0002-0018-08 | AC-0002-0018-04 | The hygiene lane pointed at both the own workflows tree and the shipped workflows tree, then a violation planted in a shipped file only                                 | The clean pair exits 0; the planted case exits 1 and names the shipped path rather than an own-CI path. The scanned shipped files stay owned by the init capability                                                                                                                             |
| EX-0002-0018-09 | AC-0002-0018-04 | A change that enables the shipped-tree scan while the shipped tree is still unhardened                                                                                  | Rejected in review, because it lands instantly red. The accepting order is shipped-tree coverage in the same change as the shipped hardening or later, never before it                                                                                                                          |
| EX-0002-0018-10 | AC-0002-0018-05 | The shipped third-party rule evaluated against the one sanctioned package-manager setup action and against an unsanctioned third-party reference                        | The sanctioned entry passes and the unsanctioned reference exits 1. A rule expressed as a count of zero fails on the sanctioned entry, which is why that formulation is rejected                                                                                                                |
