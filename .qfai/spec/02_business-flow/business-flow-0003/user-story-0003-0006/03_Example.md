# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                         | Expected                                                                                                                     |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| EX-0003-0006-01 | AC-0003-0006-01 | `qfai doctor --profile prototyping` with `node_modules/.bin/playwright` present (on Windows `playwright.cmd`)                                                                                                                                 | The `prototyping.playwrightCli` check is `ok` with `details.resolvedStage` `primary`, and no `D-DEPRECATED-PROBE` is emitted |
| EX-0003-0006-02 | AC-0003-0006-04 | `qfai doctor --profile prototyping` (playwright-cli のみが node_modules に存在)                                                                                                                                                               | `D-DEPRECATED-PROBE` is emitted at severity `error`, and its message contains `sunset: 1.10.0`                               |
| EX-0003-0006-03 | AC-0003-0006-02 | `qfai doctor --profile prototyping` (playwright も playwright-cli も無い)                                                                                                                                                                     | error text に `npm i -D playwright` が含まれる; severity error                                                               |
| EX-0003-0006-04 | AC-0003-0006-03 | In an empty directory run `qfai init`, then `npm i -D playwright`, then `qfai doctor --profile prototyping`                                                                                                                                   | No line on stdout or stderr starts with `[error]`                                                                            |
| EX-0003-0006-05 | AC-0003-0006-01 | `qfai doctor --profile prototyping` with no `node_modules/.bin/playwright`, a `playwright-cli` shim in `node_modules/.bin` (on Windows `playwright-cli.cmd`), and an `npx` on PATH for which `npx --no-install playwright --version` succeeds | `details.resolvedStage` of `prototyping.playwrightCli` is `npx-fallback`, and no `D-DEPRECATED-PROBE` is emitted             |
