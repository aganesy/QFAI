# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                | Expected                                                                                                                                                                                               |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EX-0003-0011-01 | AC-0003-0011-01 | temp dir の adopter tree で `.github/workflows/` の shipped workflow を 1 行手編集し `qfai doctor`                                                                                                   | `workflows.integrity` が severity `info` で fire し stale file の相対 path を名指しする; 手編集を戻すと severity `ok` で drift finding 0 件                                                            |
| EX-0003-0011-02 | AC-0003-0011-02 | drift 検出状態で `qfai doctor --fail-on error`、続けて `qfai validate`                                                                                                                               | doctor は exit 0 (advisory) で finding を "warnings advisory of drift" group に出す; validate は当該 drift の finding を 0 件 emit                                                                     |
| EX-0003-0011-03 | AC-0003-0011-03 | drift 検出状態の finding message body 検査                                                                                                                                                           | body は手動 repair を名指しし command / CLI verb / flag は 0 件                                                                                                                                        |
| EX-0003-0011-04 | AC-0003-0011-04 | adopter が自作した `qfai-`-名前空間の workflow を置き、provenance entry を作らない。doctor は当該ファイルを一切名指ししない。provenance entry を持つ stale file を併置すると、そちらだけが報告される | adopter-owned は shipped name と衝突しても永久に silent — prefix は reservation notice であって selector ではない (`.qfai/spec/03_contract/cli/shipped-workflows.md` §1 / §8)                          |
| EX-0003-0011-05 | AC-0003-0011-05 | drift finding だけが存在する tree (他の warning / error は 0 件) で `qfai doctor --fail-on warning`                                                                                                  | exit code 0; `summary.warning` は 0 のまま、drift は `summary.info` に計上される                                                                                                                       |
| EX-0003-0011-06 | AC-0003-0011-05 | 同じ tree に `workflows.integrity` と無関係な warning finding を 1 件足して `qfai doctor --fail-on warning`                                                                                          | exit code 1 — `--fail-on warning` leg が実際に warning を捕まえる対照ケース (EX-0003-0011-05 が vacuous でないことを示す)                                                                              |
| EX-0003-0011-07 | AC-0003-0011-06 | provenance entry を持つ shipped workflow を 1 つ手編集し、別の 1 つを削除した tree で `qfai doctor --format json`                                                                                    | finding の `details` が `workflowsDir` / `modified` / `declined` / `packagedDir` を運び、`declined` に削除 file が現れる; severity は `info` のまま、message body は declined file を stale と呼ばない |
| EX-0003-0011-08 | AC-0003-0011-06 | provenance entry を持つ file がすべて削除済みで `modified` が 0 件の tree で `qfai doctor --format json`                                                                                             | drift finding は 1 件も emit されず `details` も出力に現れない; check severity は `ok` (`declined` は finding のトリガではない)                                                                        |
| EX-0003-0011-09 | AC-0003-0011-02 | error severity の finding を 1 件持つ tree で `qfai doctor --fail-on error`                                                                                                                          | exit 1. The control shows that an `error` finding does change the exit code, which `info` never does                                                                                                   |
| EX-0003-0011-10 | AC-0003-0011-03 | `qfai doctor` on a tree where a shipped workflow name has no provenance entry and no file on disk (`absent`)                                                                                         | No drift finding names it                                                                                                                                                                              |
| EX-0003-0011-11 | AC-0003-0011-03 | `qfai doctor` where the shipped copy inside the installed package cannot be resolved                                                                                                                 | The `workflows.integrity` check is skipped at severity `info`                                                                                                                                          |
| EX-0003-0011-12 | AC-0003-0011-01 | `qfai doctor` where an installed shipped workflow with a provenance entry differs from the packaged copy only in CRLF versus LF line endings                                                         | `workflows.integrity` is `ok`, and there is no drift finding                                                                                                                                           |
| EX-0003-0011-13 | AC-0003-0011-01 | A shipped workflow with a provenance entry, present in the adopter tree, whose name the installed package no longer ships (`extra`)                                                                  | The workflow comparison reports status `ok` with an empty `modified` set; no drift is reported for it                                                                                                  |
