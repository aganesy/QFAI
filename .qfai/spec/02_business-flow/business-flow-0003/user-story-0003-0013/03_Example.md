# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                              | Expected                                                                                                                     |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| EX-0003-0013-01 | AC-0003-0013-01 | `<paths.specsDir>/01_policy/` に明示的な `DG-0001` が1件と `MUST` だけを含む別の文がある                                                                                                                           | `DG-0001` のみ検出され、`MUST` の文は項目にならない                                                                          |
| EX-0003-0013-02 | AC-0003-0013-01 | `qfai guardrails list` with one explicit `DG-0001` entry of type `non-goal` in `<paths.specsDir>/01_policy/policy.md`                                                                                              | A `# Decision Guardrails (list)` header, then the line `- [DG-0001][non-goal] <statement> (<file>:<line>)`                   |
| EX-0003-0013-03 | AC-0003-0013-02 | ガードレール 0 件で `qfai guardrails list`                                                                                                                                                                         | `- (none)` が出力される                                                                                                      |
| EX-0003-0013-04 | AC-0003-0013-01 | On the story tree, `qfai guardrails list --format json` with explicit `DG-0001` (`non-goal`) under `<paths.specsDir>/01_policy/` and `DG-0002` (`not-now`) under `<paths.contractsDir>/`                           | Both items carry their ID, type, statement, rationale, reconsideration condition and source file                             |
| EX-0003-0013-06 | AC-0003-0013-01 | `qfai guardrails list --format json` with `DG-0001` (`trade-off`, statement padded with spaces) and `DG-0003` (`non-goal`) in `<paths.specsDir>/01_policy/`, and `DG-0002` (`non-goal`) in `<paths.contractsDir>/` | Items are in the order `DG-0002`, `DG-0003`, `DG-0001`; the padded statement is trimmed, and each item keeps its source file |
| EX-0003-0013-07 | AC-0003-0013-01 | `qfai guardrails list` with no `--path`, and an explicit `DG-0001` entry only in `<paths.specsDir>/02_business-flow/business-flow.md`                                                                              | `- (none)`; the entry is not listed                                                                                          |
