# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                | Expected                                                                                                                                                                                                                                                                                                              |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0203-01 | AC-0001-0203-01 | `qfai init` in an empty directory                                                                                                                    | `qfai-run/SKILL.md` and `qfai-maintain/SKILL.md` exist under `.qfai/assistant/skill/`. Each stage skill a built-in plan names has `references/orchestrated-mode.md`. None of `direct.yml`, `bugfix.yml`, `bounded-change.yml`, `feature.yml` and `discovery.yml`, and no `*.schema.json`, is written into the project |
| EX-0001-0203-02 | AC-0001-0203-01 | `qfai init` in an empty directory; read `.claude/skills/`, `.agents/skills/`, `.codex/skills/` and `.github/skills/`                                 | Each of the four holds `qfai-run` and `qfai-maintain`, and each of the eight entries has the real path of the matching `.qfai/assistant/skill/<name>/`                                                                                                                                                                |
| EX-0001-0203-03 | AC-0001-0203-01 | An install without `qfai-run`, `qfai-maintain` and their host links; plain `qfai init`                                                               | The two skills of EX-0001-0203-01 and the eight links of EX-0001-0203-02 arrive. Every other skill directory is byte-identical                                                                                                                                                                                        |
| EX-0001-0203-04 | AC-0001-0203-02 | `qfai init` in an empty directory, then `qfai init --force`                                                                                          | After each run, no skill directory reached through the four host skills directories contains `agents/openai.yaml`                                                                                                                                                                                                     |
| EX-0001-0203-05 | AC-0001-0203-03 | `qfai init` in an empty directory; read `AGENTS.md`, `CLAUDE.md` and `.github/copilot-instructions.md`                                               | `AGENTS.md` and `CLAUDE.md` each begin with exactly one entry directive, which names `qfai-run`. `.github/copilot-instructions.md` holds none                                                                                                                                                                         |
| EX-0001-0203-06 | AC-0001-0203-03 | An existing `AGENTS.md` and `CLAUDE.md` with project text and CRLF line endings, and no entry directive; plain `qfai init`                           | Each file is the directive line, terminated by CRLF, followed by the original bytes unchanged                                                                                                                                                                                                                         |
| EX-0001-0203-07 | AC-0001-0203-03 | Two projects with no entry directive, one with `REVIEW.md` and one without; plain `qfai init` in each                                                | Both get the entry directive. Only the project with `REVIEW.md` gets the review directive                                                                                                                                                                                                                             |
| EX-0001-0203-08 | AC-0001-0203-03 | A rerun over a project whose entry points carry the directive; and a project whose `AGENTS.md` carries the directive only inside a fenced code block | The rerun leaves both files byte-identical. The fenced copy is not operative, so one directive is prepended above it                                                                                                                                                                                                  |
| EX-0001-0203-09 | AC-0001-0203-03 | `AGENTS.md` is a symbolic link to a file inside the project, with no entry directive; plain `qfai init`                                              | The run refuses `AGENTS.md`, naming the file and the reason. The link and its target are unchanged                                                                                                                                                                                                                    |
| EX-0001-0203-10 | AC-0001-0203-03 | The shipped `AGENTS.md` and `CLAUDE.md` templates, checked with the shipped Markdown lint configuration                                              | The directive line above each file's first heading raises no first-line-heading finding, and a template whose first heading is not level 1 still raises one                                                                                                                                                           |
| EX-0001-0203-11 | AC-0001-0203-04 | `qfai init` in an empty directory, without `--yes` and with stdin closed                                                                             | `qfai.config.yaml` has no `workflow` key. No prompt is printed. The summary has one line naming mode `active`                                                                                                                                                                                                         |
| EX-0001-0203-12 | AC-0001-0203-04 | Plain `qfai init` over an install whose `qfai.config.yaml` has no `workflow` key                                                                     | `qfai.config.yaml` is byte-identical, and the mode line names `active`                                                                                                                                                                                                                                                |
| EX-0001-0203-13 | AC-0001-0203-04 | Four installs whose `workflow.mode` is `active`, `shadow`, `off` and `bogus`; plain `qfai init` in each                                              | The mode line names each value and names `bogus` as invalid. The configuration is byte-identical, and each run exits 0                                                                                                                                                                                                |
| EX-0001-0203-14 | AC-0001-0203-05 | `qfai.config.yaml` with `workflow.mode: always`; `qfai validate`                                                                                     | One `QFAI_CONFIG_INVALID` issue at severity error, whose English message names `workflow.mode`, `active`, `shadow` and `off`                                                                                                                                                                                          |
| EX-0001-0203-15 | AC-0001-0203-05 | `qfai validate` over three configurations: no `workflow` key, `workflow.mode: shadow` and `workflow.mode: off`                                       | No `QFAI_CONFIG_INVALID` issue names `workflow.mode`                                                                                                                                                                                                                                                                  |
| EX-0001-0203-16 | AC-0001-0203-06 | `qfai init` in an empty directory; read every key of `.qfai/assistant/.assets.lock.json`                                                             | Each key is a relative path whose only separator is `/`, and none holds a backslash, on Linux and on Windows alike                                                                                                                                                                                                    |
| EX-0001-0203-17 | AC-0001-0203-06 | The built CLI runs `qfai init`, then a plain upgrade, in a root whose name contains a space                                                          | Both runs exit 0. The mode line, the provenance lock and the host links' real paths match those of the same runs made in process                                                                                                                                                                                      |
| EX-0001-0203-18 | AC-0001-0203-06 | An unmodified shipped rule under `.qfai/assistant/rule/` rewritten with CRLF endings; plain `qfai init`                                              | The provenance hash of the CRLF copy equals the hash the lock records for the shipped text, so init treats the rule as unmodified                                                                                                                                                                                     |
| EX-0001-0203-19 | AC-0001-0203-07 | An install where one shipped skill's `SKILL.md` has a local edit and another differs from the template only by CRLF endings; plain `qfai init`       | Both skills are byte-identical. One summary line counts one skipped skill, names `qfai init --force` as the command that updates it, and says that command replaces it with the shipped version, overwriting local edits                                                                                              |
| EX-0001-0203-20 | AC-0001-0203-07 | An install where one shipped skill's `SKILL.md` has a local edit; `qfai init --force`                                                                | The summary prints no skipped-skill count, and the skill holds the shipped bytes                                                                                                                                                                                                                                      |
