# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                           | Expected                                                                                                                                                                                                                           |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0049-01 | AC-0001-0049-01 | Given a working tree where `qfai validate --profile prototyping` ran first and `qfai validate --profile default` ran second When the report directory `.qfai/report/` is listed | Then both `validate-prototyping.json` and `validate-default.json` exist with independent contents; `validate.json` exists with top-level `{"profile": "default", ...}` reflecting only the second (most recent) run                |
| EX-0001-0049-02 | AC-0001-0049-02 | Given a downstream consumer that still points at the legacy `.qfai/output/validate.json` path on `qfai@1.9.x` When `qfai validate` runs                                         | Then the legacy path is written AND `D-DEPRECATED-PATH` warning fires with body literally containing `sunset: 1.10.0` And in `qfai@1.10.0+`, the same condition exits with error severity and the legacy path is no longer written |
| EX-0001-0049-03 | AC-0001-0049-02 | Observe a downstream consumer of legacy `.qfai/output/validate.json` after the announced sunset                                                                                 | `D-DEPRECATED-PATH` is an error and the old output path is no longer written; no deprecation-window warning is treated as the current result                                                                                       |
| EX-0001-0049-04 | AC-0001-0049-01 | Run prototyping-profile validation, then default-profile validation, then `qfai prototyping certify`                                                                            | Certify reads `validate-prototyping.json` for its gate and does not treat the newer default-profile `validate.json` pointer as a prototyping pass                                                                                  |
