# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                         | Expected                                                                                                                                                                                                                                                   |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0211-01 | AC-0001-0211-01 | Mode `active`: `/qfai-prototyping` picked by the host for free text with no work order, and later handed a valid work order                                                                   | The first edits nothing and passes the request to `qfai-run`. The second does only that work order. `SKILL.md` has one line citing `references/orchestrated-mode.md`                                                                                       |
| EX-0001-0211-02 | AC-0001-0211-02 | The first table under `## Operations` of `qfai-prototyping/references/orchestrated-mode.md`                                                                                                   | Its `Operation` column holds exactly `existing-runtime-contract`                                                                                                                                                                                           |
| EX-0001-0211-03 | AC-0001-0211-03 | In an adopter project, a `prototype` work order whose `target` is `{ kind: "flow", flowId: "BF-0002" }`, where `CON-UI-0001` serves BF-0002 and `CON-UI-0002` serves BF-0003, both UI-bearing | It settles the one visual decision for BF-0002 within the existing `DESIGN.md` and `CON-UI-0001`, leaves `CON-UI-0002` byte-for-byte unchanged and creates no contract. A standalone `/qfai-prototyping` in the same project still resolves both contracts |
| EX-0001-0211-04 | AC-0001-0211-04 | In an adopter project, a `prototype` work order whose `target` binds BF-0004, which no UI-bearing contract serves                                                                             | It writes no `DESIGN.md`, no UI contract and no surface declaration, and returns outcome `blocked` with one `debts` entry: its `cause` names the missing UI surface, its `owningFlow` is `BF-0004`, and its `resolvingOwner` is `operator`                 |
