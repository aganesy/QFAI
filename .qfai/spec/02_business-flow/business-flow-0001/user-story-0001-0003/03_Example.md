# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                                                                                    | Expected                                                                                                                                                                                                                                                   |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0003-01 | AC-0001-0003-01 | 現行 workflow の Skill 実行順と任意段階を確認                                                                                                                                                                                                                                                                                                            | configure を任意の前提とし、discussion(optional) → sdd → prototyping(optional) → atdd → implement → verify の順序が定義され、循環しない                                                                                                                    |
| EX-0001-0003-02 | AC-0001-0003-02 | The built-in `bugfix` plan, whose stages are a diagnose stage (`qfai-implement`), an `sdd_append` stage (`qfai-sdd`), an acceptance stage (`qfai-atdd`), an implement stage (`qfai-implement`), a regression-fix stage (`qfai-implement`), a test-fix stage (`qfai-atdd` or `qfai-implement`) and a verify stage (`qfai-verify`), read against the order | The `qfai-sdd` stage precedes the `qfai-atdd` stage, which precedes the `qfai-verify` stage; the `qfai-implement` stages and the test-fix stage sit where the plan places them, the diagnose stage ahead of `qfai-sdd` included; no stage names `qfai-run` |
