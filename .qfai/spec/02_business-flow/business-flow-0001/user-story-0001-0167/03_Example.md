# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                  | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0167-03 | AC-0001-0167-01 | Given the 19 card files under `.qfai/assistant/agent/` When the card frontmatter and the generated host integration files are read                                                     | Then each card's frontmatter carries `name`, `kind`, `domain`, `mission`, `replaces`, `owned_artifacts`, `tool_profile`, `permission_profile` and `specialization_tags`; `mission` is a key of its own beside `description`; no `agent-catalog.yml` exists; and each generated Codex agent TOML equals what the generator produces from its card Contrast: a card with no `mission` key, a mission read from `description`, an `agent-catalog.yml` left in the tree, or a Codex agent TOML edited by hand so that it differs from its card, each breaks the rule |
| EX-0001-0167-04 | AC-0001-0167-03 | Given a project whose `qfai.config.yaml` has no `routing:` and no `reviewProfiles:` key When a skill resolves its routing and its review profile                                       | Then it gets the defaults built into the package, and the project holds no `agent-routing.yml` and no `review-profiles.yml` Given the same project with a `routing:` entry for `qfai-sdd` that declares only a `review` phase, and a `reviewProfiles:` entry named `project-strict` that no default carries When the routing and the review profiles are resolved Then the `qfai-sdd` routing is the override entry alone, with none of the default entry's other phases merged in, and `project-strict` is available beside the default profiles                |
| EX-0001-0167-05 | AC-0001-0167-03 | Given a project's `routing:` override for `qfai-sdd` names `release-auditor` in a phase's `mandatory_agents`, and no card named `release-auditor` When `qfai validate` runs            | Then it reports `QFAI-AGENT-008` naming `release-auditor` Given the keys `qfai.config.yaml` accepts for overrides When they are listed Then they are `routing:` and `reviewProfiles:`, and none of them overrides `devils-advocate` or `pattern-doubler`, which keep their built-in settings                                                                                                                                                                                                                                                                     |
| EX-0001-0167-06 | AC-0001-0167-04 | Given the routing defaults QFAI ships and the shipped `/qfai-migration-spec-to-story` skill When the `qfai-migration-spec-to-story` routing entry and the skill's frontmatter are read | Then the entry's phases are, in order, `plan` (mandatory `requirements-analyst` and `solution-architect`, blocking `solution-architect`), `execution` (mandatory `devops-ci-engineer`) and `review` (mandatory and blocking `completion-reviewer` and `architecture-reviewer`), and its review profile is `architecture-heavy`; the skill's `roles:` names those five agents and every reviewer `architecture-heavy` selects, its `routing-profile:` is `architecture-heavy`, and `qfai validate` reports no `QFAI-AGENT-015` to `QFAI-AGENT-019` finding for it |
| EX-0001-0167-07 | AC-0001-0167-02 | Given `solution-architect.md` replaces `architect` and `contract-designer` When its card is checked                                                                                    | Then its mission, ownership and body still cover architecture boundaries and contract decisions; neither prior responsibility disappears through the merge                                                                                                                                                                                                                                                                                                                                                                                                       |
| EX-0001-0167-08 | AC-0001-0167-05 | Given the `agent-routing.yml` and `review-profiles.yml` defaults built into the package When the routing entries of `qfai-run` and `qfai-maintain` are read                            | Then `qfai-run` has the orchestrator role and no authoring or reviewing phase; `qfai-maintain` has an authoring phase and an independent reviewer with `review_profile: default`; `review-profiles.yml` still defines exactly `default`, `requirements-heavy`, `architecture-heavy`, `ui-bearing`, `runtime-heavy` and `implementation-heavy`                                                                                                                                                                                                                    |
