# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                    | Expected                                                                                                                                                             |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0206-01 | AC-0001-0206-01 | A discussion work order whose `settled` field lists the checked proposal's routing result, which fixes the route and the bound flow, and one answered question whose chosen answer fixes the release window. Which users the change serves is still open | The discussion covers which users the change serves. It asks nothing about the route, the flow or the release window                                                 |
| EX-0001-0206-02 | AC-0001-0206-02 | Mode `active`: `/qfai-discussion` picked by the host for free text with no work order, and `/qfai-discussion` handed a valid work order                                                                                                                  | The first edits nothing and passes the request to `qfai-run`. The second does only that work order. `SKILL.md` has one line citing `references/orchestrated-mode.md` |
| EX-0001-0206-03 | AC-0001-0206-03 | The `## Operations` table of `qfai-discussion/references/orchestrated-mode.md`                                                                                                                                                                           | Its `Operation` column holds exactly `resolve-unsettled-product-scope`                                                                                               |
