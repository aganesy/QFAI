# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                                                    | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0207-01 | AC-0001-0207-01 | Given workflow mode `active` and a work order for run `run-20260924045712999`, stage instance `implement-1` and operation `implement`, whose obligations name three EXs of `BF-0003` that no test annotates When `/qfai-implement` receives it and the run, stage instance and work-order IDs all match the issued order | Then it works those three EXs only, and says nothing to the operator. When it is selected in the same mode with no work order and not by name, it edits nothing and passes the request to `qfai-run`. `qfai-implement/SKILL.md` holds exactly one line citing `references/orchestrated-mode.md`                                                                                                                                                                                                                                                                     |
| EX-0001-0207-02 | AC-0001-0207-02 | Given `qfai-implement/references/orchestrated-mode.md` When the first table under its `## Operations` heading is read                                                                                                                                                                                                    | Then the `Operation` column holds exactly `diagnose-only`, `implement`, `seam-only`, `regression-fix` and `test-fix`, one backticked ID per cell, and no other operation                                                                                                                                                                                                                                                                                                                                                                                            |
| EX-0001-0207-03 | AC-0001-0207-03 | Given an `implement` work order whose `target` is `{ kind: "flow", flowId: "BF-0003" }` When the stage starts                                                                                                                                                                                                            | Then its gate and next-test selection run with `--flow BF-0003`, and no question asks which flow. When `/qfai-implement` is invoked by name with no work order, it chooses the flow as it does today                                                                                                                                                                                                                                                                                                                                                                |
| EX-0001-0207-04 | AC-0001-0207-04 | Given an `implement` work order whose obligations name three EXs of `BF-0003`, and whose `checkpointRef` names the second after an earlier attempt wrote a test annotating the first When the stage resumes                                                                                                              | Then it starts the TDD cycle at the second EX and does not rework the first. Its result names the EXs by ID and carries no record of which are done, since a test annotating an EX is what says so. Each EX still goes through RED, GREEN, refactor and review in the skill's own order                                                                                                                                                                                                                                                                             |
| EX-0001-0207-05 | AC-0001-0207-05 | Given an active run whose shared preflight snapshot is `valid` for the inputs it covers, and a test annotating one of the three bound EXs of `BF-0003` that was written after the snapshot was taken When the implement stage starts                                                                                     | Then it takes the inputs the snapshot covers from the snapshot. It runs `qfai validate --profile tdd --flow BF-0003` itself and selects its next EX from that result, so the EX now annotated is not worked again                                                                                                                                                                                                                                                                                                                                                   |
| EX-0001-0207-06 | AC-0001-0207-06 | Given a `seam-only` work order whose `parentWorkOrderId` names the acceptance work order, for target test `orders::creates an order`, which fails with `Cannot find module "../src/routes/orders"` When `/qfai-implement` serves it through the minimal-seam step                                                        | Then it registers the route with a handler answering `501`, a status no EX of the flow states, and writes no order-creation logic. The target test now fails at `expect(response.status).toBe(201)`, and the result's `seam` reports that test with observation `fail`. The main implementation waits until the acceptance stage has taken RED at that assertion                                                                                                                                                                                                    |
| EX-0001-0207-07 | AC-0001-0207-06 | Given a `seam-only` work order for target test `orders::creates an order`, and a test process that cannot start because its database container is absent, so the seam cannot be observed at the target test When `/qfai-implement` serves it                                                                             | Then the outcome is `blocked`, and `debts` lists the missing environment with `resolvingOwner` `operator`. Its `seam` reports no `pass`, nothing beyond the minimal connection is written, and the main implementation is not started. Where the cause is one the stage can repair inside its write areas, such as a type error in a helper it may edit, the outcome is `needs_repair`, not `blocked`. The acceptance attempt that asked for the seam stays open, and once `resume` has cleared the cause the seam-only work order is issued again as a new attempt |
