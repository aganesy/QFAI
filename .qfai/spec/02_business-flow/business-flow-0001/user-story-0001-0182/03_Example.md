# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                   | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0182-01 | AC-0001-0182-01 | Given a pull request in which one shipped workflow file loses its `permissions:` block, When the workflow-hygiene lane runs and reports the violation with its file, job and rule name, | Then the Reviewer Gate surfaces `R-WORKFLOW-HYGIENE-DRIFT` carrying those three fields unchanged, and does not fail the finding for a missing `justification:`. Contrast: `R-PACK-LOCATION-DRIFT` is emitted only by a repository lint script too, is error class, and **is** a catalog member — so it is rejected outright when its `justification:` is empty. The two codes differ today only because this one's catalog registration is deferred (OQ-0015-0001), not because a script emits it. |
