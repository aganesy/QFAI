# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                           | Expected                                                                                                                                                                                                                                                                                |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0159-01 | AC-0001-0159-01 | Given the shipped `packages/qfai/assets/init/.qfai/assistant/skill/qfai-sdd/templates/contracts/ui-contract.sample.yaml` template When the template is parsed at `qfai init` time                               | Then every entry in `screens[]` carries a `primary_tasks` list; the requirements-analyst agent guide instructs at least one primary task per screen                                                                                                                                     |
| EX-0001-0159-02 | AC-0001-0159-02 | Given a `<paths.contractsDir>/ui/orders-dashboard.yaml` with `screens: [{ id: orders-dashboard, primary_tasks: [] }]` When `qfai validate --fail-on error` runs (with the new QFAI-AUD-001 aligned lane active) | Then the lane FAILS at severity error naming `orders-dashboard` and the empty-`primary_tasks` violation; `/qfai-prototyping` preflight refuses to proceed; populating `primary_tasks: ["Review pending orders", "Mark order shipped"]` passes the lane and unblocks `/qfai-prototyping` |
| EX-0001-0159-03 | AC-0001-0159-02 | A UI contract under `<paths.contractsDir>/ui/` whose screen `orders-dashboard` has no `primary_tasks` key                                                                                                       | `QFAI-AUD-001` at severity error naming the screen, whose message names the legacy form and the `1.10.0` sunset; no info-severity `QFAI-AUD-001` is emitted                                                                                                                             |
