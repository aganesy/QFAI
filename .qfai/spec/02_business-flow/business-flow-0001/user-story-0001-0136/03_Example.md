# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                                                                                                    | Expected                                                                                                                                                                                  |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0136-01 | AC-0001-0136-01 | Given a converged `iterate` invocation, When `iterate` writes `prototyping.json` with `iterations[0..N]` each carrying `commitSha: "abc123" \| "uncommitted"`, non-empty `proseCritique`, `scores`, `layoutAntiPatternsDetected: []`, `designMdViolations: []`, `pivotDirective: "continue"`, `reviewerId: "rev-001"`, and `evidenceRefs[]` matching `screens[].id` set, | Then `qfai validate --profile prototyping --fail-on error` exits 0 without any orchestrator post-processing. Top-level carries `acceptedIterationIndex: N` and `stopReason: "converged"`. |
