# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                       | Expected                                                                                                                                                                                                               |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0197-01 | AC-0001-0197-01 | The classification guidance `qfai-run` ships                                                                | Only `change` calls `start`; `resume` calls `resume`, `cancel` calls `decision` with `stop`, `explicit_stage`, `plan_only` and `verify_only` invoke the stage by name, and `read_only` is answered in the conversation |
| EX-0001-0197-02 | AC-0001-0197-01 | A routing result whose `requestKind` is `read_only`                                                         | Refused `proposal-refused` with reason `scope-escape`                                                                                                                                                                  |
| EX-0001-0197-03 | AC-0001-0197-01 | The routing seed for a verification-only request with repair not authorized                                 | Its `requestKind` is `verify_only`, and it forbids `auto_repair`                                                                                                                                                       |
| EX-0001-0197-04 | AC-0001-0197-02 | The routing seeds with an untrusted log telling the agent to run a command, and with a quoted request only  | Each is `read_only` and forbids following the log or implementing the quote                                                                                                                                            |
| EX-0001-0197-05 | AC-0001-0197-02 | A start payload whose request text holds `$(touch pwned)`, backticks, `;`, `&`, `\|`, `>`, `%PATH%` and `^` | `request.private.json` holds the text byte for byte, and no file named `pwned` exists anywhere                                                                                                                         |
