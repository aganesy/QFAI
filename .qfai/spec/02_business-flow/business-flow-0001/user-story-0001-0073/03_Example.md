# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                        | Expected                                                                                                                                                             |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0073-01 | AC-0001-0073-01 | Given an E2E test file `tests/e2e/spec0001.test.ts` When it contains `QFAI:SPEC-0001:a TC annotation`                                        | Then validation reports an error (forbidden reference)                                                                                                               |
| EX-0001-0073-02 | AC-0001-0073-05 | Given a project on the story tree and a `/qfai-atdd` invocation that owns the flow `BF-NNNN` When the invocation reaches its completion gate | Then it runs `qfai validate --profile atdd --fail-on error --flow BF-NNNN`, and it does not pass `--spec <spec-id>`, which exits 2 on that tree                      |
| EX-0001-0073-03 | AC-0001-0073-04 | An independent non-edit Reviewer evaluates ATDD output with no E2E test for an in-scope `BF-NNNN`                                            | The Reviewer returns REVISE and names the missing BF; the test implementer does not approve their own work                                                           |
| EX-0001-0073-04 | AC-0001-0073-06 | A configured E2E or integration test annotates an undefined BF, AC or EX ID, alongside one defined ID                                        | `qfai validate` reports each undefined ID with its file as an error, while the defined ID raises no undeclared-reference finding                                     |
| EX-0001-0073-05 | AC-0001-0073-03 | Complete ATDD for a BF and inspect its evidence file under `.qfai/evidence/`                                                                 | The file records objective, inputs, decisions, work, commands, volume, coverage, work orders, execution logs, gaps and final status, and is excluded from the commit |
| EX-0001-0073-06 | AC-0001-0073-02 | A BF's acceptance tests have assertions, but its ATDD evidence lacks the observable RED or controlled falsifiability result.                 | The independent completion review does not return PASS; its finding names the missing proof and the handoff remains open.                                            |
