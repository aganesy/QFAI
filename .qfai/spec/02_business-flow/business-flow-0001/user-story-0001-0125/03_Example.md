# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                    | Expected                                                                                                                                                                                                                                             |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0125-02 | AC-0001-0125-03 | Given a seeded `prototyping.json` with `uiContractsCovered: ["CON-UI-0012"]`. When `qfai prototyping show-ui-contract` emits its JSON payload.                                           | Then the payload carries `uiContractsCovered`, `frozenSurfaceUnion` and `liveUiBearing` of `CON-UI-NNNN` IDs and a `primary` block naming `CON-UI-0012`; it carries no `frozenSpecsCoveredSource`. With no pin, `primary.source` is `contract-scan`. |
| EX-0001-0125-03 | AC-0001-0125-03 | Given `prototyping.json` carries `specsCovered` and `frozenSpecsCovered` and no `uiContractsCovered`. When `qfai prototyping show-ui-contract` runs.                                     | Then it exits 2.                                                                                                                                                                                                                                     |
| EX-0001-0125-04 | AC-0001-0125-01 | Given cycle 0 froze `CON-UI-0007` and `CON-UI-0011`, and `CON-UI-0019` is added before cycle 3. When the cycle 3 drift gate compares the live UI contract set with `frozenSurfaceUnion`. | Then the run exits 2, names `CON-UI-0019`, keeps the cycle-0 frozen set, and defers the new contract to the next invocation.                                                                                                                         |
| EX-0001-0125-05 | AC-0001-0125-02 | Given valid UI contracts `CON-UI-0007` and `CON-UI-0011` are resolved at cycle 0. When cycle 0 completes.                                                                                | Then `uiContractsCovered[]` and `frozenSurfaceUnion[]` record exactly those IDs for subsequent cycles.                                                                                                                                               |
