# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                     | Expected                                                                                                                                                                                                                                                                                                         |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0202-01 | AC-0001-0202-01 | Given workflow mode `active`, and the host picks `qfai-sdd` for the free-text request "add a CSV export to the orders page" with no work order When the skill starts                      | Then it edits no file, and in the same turn passes the request to `qfai-run`; the operator sees at most one line from `qfai-sdd`, with no explanation of modes or stages, and then the request entry of `qfai-run`                                                                                               |
| EX-0001-0202-02 | AC-0001-0202-01 | Given workflow mode `shadow`, and then mode `off`, and the host picks `qfai-sdd` for the same free-text request When the skill starts                                                     | Then in both modes no entry check runs and the skill behaves exactly as when `/qfai-sdd` is invoked by name; nothing is passed to `qfai-run`                                                                                                                                                                     |
| EX-0001-0202-03 | AC-0001-0202-02 | Given workflow mode `active`, and `qfai-atdd` handed a work order whose `runId`, `stageInstanceId` and `workOrderId` match the ones `npx qfai workflow next` issued When the skill starts | Then it does only the work that work order names, within its scope, and writes nothing to the operator; its result goes back to the harness                                                                                                                                                                      |
| EX-0001-0202-04 | AC-0001-0202-02 | Given workflow mode `active`, and `qfai-implement` handed a work order whose `workOrderId` matches no work order the run issued When the skill starts                                     | Then it edits no file and returns the refusal to the harness; it neither passes the request to `qfai-run` nor starts the stage                                                                                                                                                                                   |
| EX-0001-0202-05 | AC-0001-0202-03 | Given the operator invokes `/qfai-atdd --flow BF-0001` by name, and later invokes `/qfai-sdd` by name When each runs                                                                      | Then `/qfai-atdd` authors the acceptance tests and stops at that stage, and `/qfai-sdd` writes the specification and stops without continuing into implementation; neither starts another stage                                                                                                                  |
| EX-0001-0202-06 | AC-0001-0202-03 | Given the operator invokes `/qfai-sdd` by name and asks it to take the change "to the end" When the skill reads the request                                                               | Then the request is handed to `qfai-run` and becomes a whole run; `/qfai-sdd` does not chain the later stages itself                                                                                                                                                                                             |
| EX-0001-0202-07 | AC-0001-0202-04 | Given the built-in plans name `qfai-sdd`, `qfai-atdd`, `qfai-implement`, `qfai-verify`, `qfai-discussion`, `qfai-prototyping` and `qfai-maintain` When the `description:` of each is read | Then each opens with a sentence saying to use the skill when it is invoked by name or handed a QFAI work order, nothing precedes that sentence, and each is at most 1024 characters with no `<` or `>`; a description that opens "Runs Stage 0, then triage, then the contract step" does not meet the criterion |
| EX-0001-0202-08 | AC-0001-0202-05 | Given the files of each of the seven skills the built-in plans name When they are read                                                                                                    | Then each has exactly one `references/orchestrated-mode.md`, its `SKILL.md` has exactly one line citing that file, and no other line of the `SKILL.md` body states an orchestrated-mode rule                                                                                                                     |
| EX-0001-0202-09 | AC-0001-0202-06 | Given the `SKILL.md` frontmatter of the seven skills the built-in plans name, and of `qfai-grill`, which no built-in plan names When each is read                                         | Then none of the seven carries `disable-model-invocation`; `qfai-grill` is outside the criterion whether or not it carries the key                                                                                                                                                                               |
