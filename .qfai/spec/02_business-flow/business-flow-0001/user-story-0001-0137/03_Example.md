# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                          | Expected                                                                                                                                                                                                                                                                                                         |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0137-01 | AC-0001-0137-01 | Given `verify.json` carrying `scope: "prototyping"` with the prototyping-profile lanes PASS, When `qfai prototyping certify --check` runs at HEAD with no `/qfai-atdd` or `/qfai-implement` artifacts present, | Then certify exits 0 and writes `completion-certificate.json` with `scope: "prototyping"`. A second invocation with `scope: "full"` requires the full ATDD + implement gates and is rejected when those artifacts are missing. Reviewer-Gate `R-CERTIFY-VERIFY-CIRCULAR` fires when a PR reintroduces the cycle. |
