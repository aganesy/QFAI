# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                               | Expected                                                                                                                                                                                                                                                                                                                                  |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0192-01 | AC-0001-0192-01 | A clear request to let each customer register up to five notification emails, with no duplicates per customer and the existing address kept; the checked routing result names one new story in `newStories`, with the customer flow as its `flowId` | The run moves to `awaiting_input` with one `create` question and no story-authoring work order; after the answer no later stage opens a `create` question                                                                                                                                                                                 |
| EX-0001-0192-02 | AC-0001-0192-01 | The operator answers the `create` question with the option whose effect is `proceed`                                                                                                                                                                | One `human_decision` authorization with operation `CREATE`, bound to the question's `slotId`, recorded `agent_captured`                                                                                                                                                                                                                   |
| EX-0001-0192-03 | AC-0001-0192-01 | A routing result whose proposal names two new stories                                                                                                                                                                                               | Two `create` questions, both open in the same round, each with its own `slotId`                                                                                                                                                                                                                                                           |
| EX-0001-0192-04 | AC-0001-0192-02 | The story-authoring work order after the approval, then an `sdd` result reporting `bindings` for the slot                                                                                                                                           | The work order's `target` is `{ kind: "new_story", slotId }`; a binding event is published naming the flow and the created story IDs, and every later work order of the run targets `{ kind: "flow", flowId }`                                                                                                                            |
| EX-0001-0192-05 | AC-0001-0192-02 | The approval was given, then the scope digest changes before the story-authoring work order is accepted                                                                                                                                             | The run moves to `awaiting_input` with a new `create` question, and the old approval is not reused                                                                                                                                                                                                                                        |
| EX-0001-0192-06 | AC-0001-0192-03 | The operator answers the `create` question with the option whose effect is `stop`                                                                                                                                                                   | The run is `cancelled`; no story or flow is created; nothing exists under `.qfai/evidence/workflow/`, and nothing outside `.qfai/run/<runId>/` changed                                                                                                                                                                                    |
| EX-0001-0192-07 | AC-0001-0192-04 | A routing result is accepted and its plan checked                                                                                                                                                                                                   | The plan document returned holds the goal, the stages in plan order and the write scope; `qfai-run` announces those three in plain words and asks nothing                                                                                                                                                                                 |
| EX-0001-0192-08 | AC-0001-0192-04 | One routing proposal names `{ kind: "flow-id", ref: "BF-9999" }` with `confidence: 1`. Another names `{ kind: "path", ref: "Dockerfile" }` without a current path-existence fact                                                                    | The first is refused `proposal-refused` / `unknown-id` despite confidence. The second is refused `proposal-refused` / `unknown-path`, naming `Dockerfile`. Each leaves `routing` unchanged with no events                                                                                                                                 |
| EX-0001-0192-09 | AC-0001-0192-04 | A routing proposal has normative `{ kind: "request", ref: "request" }` and `{ kind: "flow-id", ref: "BF-0001" }`, observed `path` and `evidence` entries whose files exist, and one new story found by a lexical miss                               | The checked plan retains typed `expectedBehaviorRefs` and `observedRefs` separately; the new story opens a `create` question rather than recording an authorization                                                                                                                                                                       |
| EX-0001-0192-10 | AC-0001-0192-05 | A run on each of the five plans, driven by canned accepted results through `next` and `accept` only                                                                                                                                                 | Every work order names its executor skill and operation, in plan order, until `next` returns `workOrder: null`; no step needs an operator-typed stage name                                                                                                                                                                                |
| EX-0001-0192-11 | AC-0001-0192-05 | `start` in mode `active` with a start input holding exactly `request`, `completionTarget` and `harness`, and a second start input that also carries `scope`                                                                                         | The first creates `.qfai/run/<runId>/` whose journal holds `run-created` and `capture-request`, the run in state `routing`, with no file outside `.qfai/run/` changed; the second is refused `invalid-input` with reason `schema` and leaves no run                                                                                       |
| EX-0001-0192-12 | AC-0001-0192-05 | `next` called twice on a run in `running`, then `resume` called twice, with no result between                                                                                                                                                       | The same `workOrderId` every time                                                                                                                                                                                                                                                                                                         |
| EX-0001-0192-13 | AC-0001-0192-05 | The `feature` plan the package ships                                                                                                                                                                                                                | Stage kinds in order: `sdd`, `prototype` under `prototype_decision_needed`, `acceptance` under `acceptance_obligations_unmet`, `implement`, `verify` with operation `verify-full`                                                                                                                                                         |
| EX-0001-0192-14 | AC-0001-0192-05 | The `direct`, `bugfix`, `bounded-change` and `feature` plans the package ships                                                                                                                                                                      | Each ends in a `verify` stage with operation `verify-full`, reachable from every stage                                                                                                                                                                                                                                                    |
| EX-0001-0192-15 | AC-0001-0192-05 | A result with `notRun: { kind: "not_applicable", reason }`, one with no reason, and one reusing a stale receipt                                                                                                                                     | The first is recorded with its reason and counts as no pass; the second is refused `skip-unexplained`; the third `reuse-stale`                                                                                                                                                                                                            |
| EX-0001-0192-16 | AC-0001-0192-05 | The shipped `qfai-run` skill and its default routing entry                                                                                                                                                                                          | The routing entry names no authoring and no reviewing role for `qfai-run`, and the role validators pass                                                                                                                                                                                                                                   |
| EX-0001-0192-17 | AC-0001-0192-05 | `npx qfai workflow route`, `npx qfai workflow exec`, an unknown operation name and an unknown flag                                                                                                                                                  | Each exits 2 with a JSON error document of code `invalid-input` on stdout                                                                                                                                                                                                                                                                 |
| EX-0001-0192-18 | AC-0001-0192-05 | Each of the seven operations on a success path and on an error path                                                                                                                                                                                 | stdout holds exactly one JSON document every time; `status` and `next` on a run in `running` exit 0 and report no completed verdict                                                                                                                                                                                                       |
| EX-0001-0192-19 | AC-0001-0192-06 | `finish` on a run whose result submits `gateResults` with a validate PASS, in a tree where validate reports an error                                                                                                                                | The validate receipt is `cli_observed`; `unmet[]` holds `gate-failed`; the submitted PASS is recorded `agent_reported` and decides nothing                                                                                                                                                                                                |
| EX-0001-0192-20 | AC-0001-0192-06 | `finish` offered a `verify.json` that is another run's stage copy                                                                                                                                                                                   | `unmet[]` holds `verify-foreign`, and the run does not complete                                                                                                                                                                                                                                                                           |
| EX-0001-0192-21 | AC-0001-0192-06 | `finish` where the only `qa-gatekeeper` PASS comes from an instance the actor history shows as an author                                                                                                                                            | `unmet[]` holds `review-missing`                                                                                                                                                                                                                                                                                                          |
| EX-0001-0192-22 | AC-0001-0192-06 | `finish` on a run in `ready` with one condition unmet                                                                                                                                                                                               | The state stays `ready`, exit 1, and `unmet[]` names that condition once with its owner                                                                                                                                                                                                                                                   |
| EX-0001-0192-23 | AC-0001-0192-06 | A `start` baseline holding one error finding, and a `finish` finding set holding it and one new error                                                                                                                                               | `gate-failed` lists the first as `pre-existing` and the second as `new`; neither is waived                                                                                                                                                                                                                                                |
| EX-0001-0192-24 | AC-0001-0192-07 | A run started with `completionTarget: working_tree`, whose conditions all hold except `uncommitted`                                                                                                                                                 | `target: "working_tree"` with `deliveryUnmet[]`; the result never reads `qfai_done`                                                                                                                                                                                                                                                       |
| EX-0001-0192-25 | AC-0001-0192-07 | "Don't commit": a `working_tree` run whose changed paths are uncommitted                                                                                                                                                                            | `deliveryUnmet[]` names `uncommitted` with the uncommitted paths                                                                                                                                                                                                                                                                          |
| EX-0001-0192-26 | AC-0001-0192-07 | An `accepted_with_debt` result whose debt has another flow as `owningFlow` and `qfai-sdd` as `resolvingOwner`, then `finish` while it stands; and a same-flow debt whose finding the `finish` validate no longer reports at its path                | The cross-flow debt: `running → ready` at `accept`, and `finish` lists `debt-open` with `qfai-sdd` as owner and that flow in `subject`; the same-flow debt is resolved and not listed                                                                                                                                                     |
| EX-0001-0192-27 | AC-0001-0192-08 | An acceptance result with `seamRequest: { targetTestId }` and outcome `needs_repair`, then the seam-only result                                                                                                                                     | A seam-only work order to `qfai-implement` whose `parentWorkOrderId` names the acceptance order; after it is accepted, the next work order is the same acceptance stage instance as a new attempt                                                                                                                                         |
| EX-0001-0192-28 | AC-0001-0192-08 | A seam-only result whose `seam.observation` is `pass` at its target test                                                                                                                                                                            | Refused `invalid-input` with reason `seam-passed`                                                                                                                                                                                                                                                                                         |
| EX-0001-0192-29 | AC-0001-0192-08 | An acceptance result with `expected_red` and `red.failureKind: assertion`                                                                                                                                                                           | Accepted; the next work order is implement                                                                                                                                                                                                                                                                                                |
| EX-0001-0192-30 | AC-0001-0192-08 | A result with outcome `unrun`, and an accepted verify result naming its `verify.json`                                                                                                                                                               | The `unrun` result moves the run to `blocked`, never to a pass; the stage copy of `verify.json` is byte-identical to the file submitted                                                                                                                                                                                                   |
| EX-0001-0192-31 | AC-0001-0192-09 | The accepted `sdd` result resubmitted with the same `resultId` after the run moved on, its expected sequence now stale                                                                                                                              | The stored verdict and no event; the stories that stage created, and the rows it appended to `decisions.md`, each still exist once                                                                                                                                                                                                        |
| EX-0001-0192-32 | AC-0001-0192-09 | A result whose `expectedSequence` is behind the run's current sequence                                                                                                                                                                              | Refused `stale-sequence`, carrying the current state and sequence; no event                                                                                                                                                                                                                                                               |
| EX-0001-0192-33 | AC-0001-0192-10 | A maintenance result whose `changedFiles` holds a path in neither `scope.writeAreas` nor `recordAreas`                                                                                                                                              | Refused `invalid-input` with reason `write-scope`; the state is unchanged                                                                                                                                                                                                                                                                 |
| EX-0001-0192-34 | AC-0001-0192-10 | An `sdd` result binding the story it created for the goal's slot, and one creating a second story no slot is bound to                                                                                                                               | The first is accepted; the second is refused `invalid-input` with reason `unbound-story`                                                                                                                                                                                                                                                  |
| EX-0001-0192-35 | AC-0001-0192-11 | A run that edits `qfai.config.yaml` inside its write scope, then `finish`                                                                                                                                                                           | `unmet[]` holds `policy-drift`; the run does not complete                                                                                                                                                                                                                                                                                 |
| EX-0001-0192-36 | AC-0001-0192-06 | `finish` on a run in `ready` whose validate is clean, whose verify stage copy holds `status: PASS` and `scope: full`, and whose `qa-gatekeeper` PASS is independent                                                                                 | `target: "qfai_done"`, the run `completed`, exit 0                                                                                                                                                                                                                                                                                        |
| EX-0001-0192-37 | AC-0001-0192-09 | Through the built CLI: a result accepted, its response lost, the result resubmitted, then a new result carrying the old sequence                                                                                                                    | The resubmission returns the stored verdict with exit 0 and the journal unchanged; the new result is refused `stale-sequence` with exit 2 and the journal unchanged                                                                                                                                                                       |
| EX-0001-0192-38 | AC-0001-0192-12 | An implement work order bound to `BF-0001`, with results changing: `.qfai/evidence/implement-BF-0001.md`; `.qfai/evidence/implement-BF-0002.md`; `.qfai/spec/decisions.md`                                                                          | `recordAreas` names exactly the first path; the first result is accepted; the second and the third are refused `invalid-input` with reason `write-scope`                                                                                                                                                                                  |
| EX-0001-0192-39 | AC-0001-0192-13 | A feature plan whose `sdd` stage will write a new story under `BF-0001`, and a route proposal whose `proposedWriteScope` also names `.qfai/spec/decisions.md`                                                                                       | The proposal names the new story's files; the one naming the decisions table is refused `proposal-refused` with reason `protected-surface`                                                                                                                                                                                                |
| EX-0001-0192-40 | AC-0001-0192-13 | The routing reference `qfai-run` ships                                                                                                                                                                                                              | Per stage kind: `sdd_delta` names only the story and contract files of the bound flow it changes; `sdd` names the new story's directory, or the new flow's directory for a slot that creates a flow; `discussion` its tracked records, and `DESIGN.md` for a UI-bearing target; a UI-bearing `prototype` `<paths.contractsDir>/design/**` |
| EX-0001-0192-41 | AC-0001-0192-14 | A ready `qfai_done` run has uncommitted tracked run changes or `.qfai/evidence/workflow/<runId>/summary.json`, with all other gates met                                                                                                             | `finish` reports `uncommitted`, leaves the state `ready` and publishes no completion event; after all tracked run changes and workflow evidence are committed, it may complete                                                                                                                                                            |
| EX-0001-0192-42 | AC-0001-0192-15 | A ready `qfai_done` run has all tracked run changes and workflow evidence committed; `summary.json` was last written at `ready`                                                                                                                     | Successful `finish` leaves the tracked summary byte-identical and records `completed` in the runtime journal and snapshot; `status` reports `completed`                                                                                                                                                                                   |
| EX-0001-0192-43 | AC-0001-0192-15 | A ready `working_tree` run has uncommitted tracked changes and a summary last written at `ready`, with its other gates met                                                                                                                          | Successful `finish` requires no commit, leaves the tracked summary byte-identical, records `completed` at runtime, and makes `status` report `completed`                                                                                                                                                                                  |
| EX-0001-0192-44 | AC-0001-0192-02 | A ready feature run has an apparent CREATE approval bound to the right slot with effect `proceed`, but no persisted `authorizationId`; `next` is called before the story-authoring work order                                                       | The core moves to `awaiting_input` with a new `create` question for that slot, returning no work order and emitting no `work-order-issued` event; `/qfai-sdd` asks nothing                                                                                                                                                                |
| EX-0001-0192-45 | AC-0001-0192-04 | A routing result has a bare string in either `expectedBehaviorRefs` or `observedRefs`                                                                                                                                                               | Each malformed payload is refused `invalid-input` / `schema` before route checks, with unchanged state and no events                                                                                                                                                                                                                      |
| EX-0001-0192-46 | AC-0001-0192-16 | An `sdd` result that appended a CREATE row for its slot and a `Change request:` row, each at WIP, whose Approach cites the `<runId>/<authorizationId>` of the `human_decision` that answered it                                                     | Accepted; `decisions.md` rows present at issue are unchanged                                                                                                                                                                                                                                                                              |
| EX-0001-0192-47 | AC-0001-0192-16 | An `sdd_delta` result that edits the Content of a row present at issue; another that moves the Status of a row this run did not append; another whose appended CREATE row stands at WIP citing no `human_decision` of this run                      | The first two are refused `invalid-input` with reason `record-rewritten`, the third with reason `record-unauthorized`; the run state is unchanged each time                                                                                                                                                                               |
| EX-0001-0192-48 | AC-0001-0192-04 | A routing proposal with no `newStories`, whose plan has a stage that takes a flow target and whose `affectedFlowIds` names `BF-0001` and `BF-0002`; then the same proposal naming `BF-0001` only                                                    | The first is refused `proposal-refused` with reason `flow-binding`. The second is checked, a `binding-recorded` event names `BF-0001`, and every later flow-target work order targets `{ kind: "flow", flowId: "BF-0001" }`                                                                                                               |
| EX-0001-0192-49 | AC-0001-0192-12 | An `sdd_append` work order issued after a `missing-test` diagnosis whose first matched ID is `AC-0001-0005-02`, then a result that also changes that story's `02_Acceptance-Criteria.md`                                                            | `recordAreas` names exactly `user-story-0001-0005/03_Example.md`, the contract whose rule cites the examples of `AC-0001-0005-02`, `decisions.md` and `.qfai/evidence/sdd-BF-0001.md`; the result is refused `invalid-input` with reason `write-scope`                                                                                    |
| EX-0001-0192-50 | AC-0001-0192-12 | Three `sdd_append` results for the work order of EX-0001-0192-49: one adds the new example's ID to one rule's Examples cell of the derived contract; one also rewords that rule's Statement; one adds the ID to the Examples cells of two rules     | The first is accepted; the second and the third are refused `invalid-input` with reason `rule-changed`, and the run state is unchanged                                                                                                                                                                                                    |
