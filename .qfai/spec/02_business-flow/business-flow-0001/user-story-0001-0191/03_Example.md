# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                               | Expected                                                                                                                                                                                                                                                                                                                |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0191-01 | AC-0001-0191-01 | `.qfai/evidence/prototyping/prototyping.json` records `stopReason: "max-iterations"` and `acceptedIterationIndex: null`, and `qfai prototyping iterate --check-convergence` runs without `--cycle`. | It reports cycle 9, `stopReason: max-iterations` and `acceptedIterationIndex: null`, prints `Not converged` with the reason, exits 2, and leaves `prototyping.json` byte-for-byte unchanged. The same run against a record of `stopReason: "converged"` and `acceptedIterationIndex: 3` prints `Converged` and exits 0. |
