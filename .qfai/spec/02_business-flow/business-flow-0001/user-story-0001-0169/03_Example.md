# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                 | Expected                                                                                                                                                                                                                           |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0169-01 | AC-0001-0169-03 | Given the first required delegation to `delivery-planner` fails with a native tool error that names no retry window When the stage starts                                                                                                                             | Then the failure is classified `unavailable`, the stage stops immediately, and it reports the attempted role/task, failure summary, failure class, required user remediation, and retry condition                                  |
| EX-0001-0169-02 | AC-0001-0169-02 | Given a skill stage such as `/qfai-implement` whose first required delegation is `delivery-planner` When the orchestrator starts the stage in a native sub-agent environment                                                                                          | Then it attempts `delivery-planner` immediately, treats that real delegation attempt as the Capability Probe, and does not wait for any separate availability confirmation before execution                                        |
| EX-0001-0169-03 | AC-0001-0169-01 | Given a spec SDD task requiring requirements, architecture and test design When the orchestrator starts the task                                                                                                                                                      | Then it creates work orders and delegates the first drafts to `requirements-analyst`, `solution-architect` and `test-design-analyst`; it integrates their outputs without writing a primary artifact first draft itself            |
| EX-0001-0169-04 | AC-0001-0169-04 | Given a run whose history records agent instance `sa-1` as the author of a contract section at the `sdd` stage When a later work order requires an independent review of that section                                                                                 | Then the work order carries the history, `sa-1` does not count as the independent reviewer, and the required reviewer is dispatched rather than dropped to save tokens                                                             |
| EX-0001-0169-05 | AC-0001-0169-05 | Given a delegated grilling session inside a run, whose work order's `settled` field records the checked proposal's routing result, which fixes the target flow `BF-0001`, and one answered question that fixes a design decision When the session builds its frontier | Then neither settled item is on the frontier, only the remaining decisions are worked, the session stays delegated, and no stage of the run invokes `qfai-grill`                                                                   |
| EX-0001-0169-06 | AC-0001-0169-03 | The first required delegation fails with `agent thread limit reached` on every retry                                                                                                                                                                                  | Classified `saturated`; the identical delegation is retried with bounded backoff while the stage stays open; when the budget is exhausted the stage stops and reports `saturated (retry budget exhausted)`, with no self-execution |
| EX-0001-0169-07 | AC-0001-0169-06 | The Work Orders Summary table of the shipped `shared-skill-delegation-baseline.md` and of the shipped `qfai-sdd` evidence template `templates/evidence/sdd-flow.md`                                                                                                   | Its columns are Step, Role (sub-agent), Agent instance, Task title, Input (refs), Output (refs) and Status (PASS/REVISE/PENDING)                                                                                                   |
