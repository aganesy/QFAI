# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                      | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0038-01 | AC-0001-0038-01 | Run `qfai init` in an empty directory that `qfai init` lays out as the story tree, then list every path under `.qfai/`                                                     | `.qfai/spec/` holds `decisions.md` and `open-questions.md`, each with its header row and no data row, `01_policy/glossary.md`, `01_policy/constraint.md`, `02_business-flow/business-flows.md`, `03_contract/contracts.md` and the directories `03_contract/api/`, `db/`, `ui/`, `cli/` and `design/`. No `business-flow-NNNN/` and no `user-story-NNNN-NNNN/` directory exists, and neither `.qfai/specs/` nor `.qfai/contracts/` exists |
| EX-0001-0038-02 | AC-0001-0038-01 | Run `qfai validate --fail-on error` on the tree a story-tree `qfai init` has just written                                                                                  | Exit 0 with no error-level finding: the fresh tree carries no test obligation                                                                                                                                                                                                                                                                                                                                                             |
| EX-0001-0038-03 | AC-0001-0038-01 | Run the mdschema check over each Markdown file the story-tree `qfai init` seeded under `.qfai/spec/`                                                                       | The mdschema manifest routes every seeded file to a schema, and each conforms to it: 0 violations, and no seed carries the ignore marker                                                                                                                                                                                                                                                                                                  |
| EX-0001-0038-04 | AC-0001-0038-02 | On a story-tree project, edit `01_policy/glossary.md`; then, deleting `03_contract/contracts.md` before each run, run `qfai init` and `qfai init --force`                  | After both runs the edited `glossary.md` is byte-identical to the edit, and the deleted `contracts.md` is written again from its template. `--force` rewrites no existing seed                                                                                                                                                                                                                                                            |
| EX-0001-0038-05 | AC-0001-0038-03 | Run `qfai init` in an empty directory that `qfai init` lays out as the story tree; separately, load a `qfai.config.yaml` whose `paths` omits `specsDir` and `contractsDir` | The written `qfai.config.yaml` carries `paths.specsDir: .qfai/spec` and `paths.contractsDir: .qfai/spec/03_contract`, and the configuration loaded without the two keys resolves them to the same two paths                                                                                                                                                                                                                               |
| EX-0001-0038-06 | AC-0001-0038-04 | Run `qfai init` on a project whose configured `paths.specsDir` holds a `spec-*/` directory, and separately on one whose `paths.specsDir` holds only `_policies/`           | On each, nothing is written under `.qfai/spec/`, every other output is written, the migration skill and its host links included, one output line names the detected path and `/qfai-migration-v1-to-v2`, and the exit code is 0, as on a run that detects nothing                                                                                                                                                                         |
| EX-0001-0038-07 | AC-0001-0038-04 | Run `qfai init` on a project that has `.qfai/contracts/` while its configured `paths.specsDir` holds neither `spec-*/` nor `_policies/`                                    | Init still detects the spec-pack layout: nothing is written under `.qfai/spec/`, every other output is written, and the one output line names `.qfai/contracts/` and `/qfai-migration-v1-to-v2`                                                                                                                                                                                                                                           |
| EX-0001-0038-08 | AC-0001-0038-05 | Run `qfai init` in an empty directory that `qfai init` lays out as the story tree                                                                                          | `.qfai/spec/01_policy/objective.md`, `initiative.md` and `principle.md` and `.qfai/spec/03_contract/tech.md` and `structure.md` exist, and none of `catalog/product.md`, `catalog/manifest.md`, `catalog/tech.md` and `catalog/structure.md` exists under `.qfai/assistant/`                                                                                                                                                              |
