# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                | Expected                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0122-01 | AC-0001-0122-02 | Given a legacy `prototyping.json` record with `frozenSpecsCovered` but no `frozenSurfaceUnion` field. When `qfai prototyping iterate --cycle 1` (or later) runs against that record. | Then the CLI exits 2 with a re-seed instruction (`--cycle 0 --target-url <url>`); the cycle ≥ 1 drift gate does NOT silently fall back to `frozenSpecsCovered` (which would re-enable a false positive).                                                                                                                                                                                       |
| EX-0001-0122-02 | AC-0001-0122-02 | Given a cycle ≥ 1 invocation against a `prototyping.json#frozenLicenseCatalog` that differs from the in-memory `DEFAULT_LICENSE_CATALOG` SSOT. When the catalog drift gate runs.     | Then byte-permutation differences (reordered `allowedSources`, reordered tier entries) MUST NOT trip the gate (set-equality semantic via `licenseCatalogsEqual`), but any semantic difference (added source, removed `sourceHosts`, malformed shape) MUST exit 2 with a re-seed instruction. A cycle >= 1 record with no `frozenLicenseCatalog` key also exits 2 with the re-seed instruction. |
| EX-0001-0122-03 | AC-0001-0122-02 | Given `prototyping.json#uiContractsCovered` is `["0012"]`. When `qfai prototyping certify` runs.                                                                                     | Then certify exits 2 before any review path is built from the entry.                                                                                                                                                                                                                                                                                                                           |
| EX-0001-0122-04 | AC-0001-0122-02 | Given `prototyping.json#uiContractsCovered` is present and empty (`[]`). When `qfai prototyping certify` runs.                                                                       | Then certify exits 2 and does not fall back to any legacy scope field.                                                                                                                                                                                                                                                                                                                         |
| EX-0001-0122-05 | AC-0001-0122-02 | Given every reviewer attempt for `(CON-UI-0007, orders-dashboard)` fails to open the target URL during cycle 2. When the orchestrator evaluates the reviewer session result.         | Then after the last failed attempt the dispatcher reports `retryExhausted` for `(CON-UI-0007, orders-dashboard)` and writes no `orders-dashboard.review.json`, so certify treats the pair as missing (BR-0276) and convergence is not declared.                                                                                                                                                |
| EX-0001-0122-06 | AC-0001-0122-02 | Given cycle 0 froze `CON-UI-0007`, but its UI contract is removed before cycle 1. When `qfai prototyping iterate --cycle 1` resolves the live set.                                   | Then it exits 2 for removed mid-run scope; the cycle-0-only no-op path is not used.                                                                                                                                                                                                                                                                                                            |
| EX-0001-0122-07 | AC-0001-0122-02 | Given cycle 0 froze `CON-UI-0007` and `CON-UI-0011`, the last iteration otherwise converges, and `CON-UI-0011` is removed. When cycle 1 or later runs.                               | Then the UI contract drift gate exits 2 before the convergence check can return success.                                                                                                                                                                                                                                                                                                       |
| EX-0001-0122-08 | AC-0001-0122-01 | Given a ten-cycle run with stdin closed. When cycles 0 through 9 execute.                                                                                                            | Then no per-cycle prompt or stdin read occurs before the run exits.                                                                                                                                                                                                                                                                                                                            |
