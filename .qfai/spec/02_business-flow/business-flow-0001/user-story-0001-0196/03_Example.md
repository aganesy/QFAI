# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                    | Expected                                                                                                                                                                                                                        |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0196-01 | AC-0001-0196-01 | `resume` of a run recorded in another worktree                                                                                                                                                                                                           | Refused `identity-mismatch`, and no work order is returned                                                                                                                                                                      |
| EX-0001-0196-02 | AC-0001-0196-01 | The snapshot after `start`                                                                                                                                                                                                                               | The execution context holds `qfaiVersion`, the digests of `qfai.config.yaml`, of `.qfai/assistant/rule/**` and of the package's plan files, the harness and its capabilities and the request digest, and no `schemaVersion` key |
| EX-0001-0196-03 | AC-0001-0196-01 | A run whose recorded `qfaiVersion` is newer than the running package                                                                                                                                                                                     | Every operation is refused `newer-record`                                                                                                                                                                                       |
| EX-0001-0196-04 | AC-0001-0196-02 | A run interrupted mid-implement at an example no test yet annotates, its RED receipt accepted, with nothing upstream changed                                                                                                                             | `resume` classes the RED receipt `valid` and returns the implement work order whose `checkpointRef` names that example; no story-authoring or discussion work order                                                             |
| EX-0001-0196-05 | AC-0001-0196-02 | `status` with no `--run` in a worktree holding one non-terminal run                                                                                                                                                                                      | It reports that run, so a new session told to continue restores the same run                                                                                                                                                    |
| EX-0001-0196-06 | AC-0001-0196-02 | A worktree with one non-terminal run: a second `start`, then a request to continue                                                                                                                                                                       | The second `start` is refused `run-active` naming the run; `status` with no `--run` reports that run, `resume` returns its work order, and no question is opened                                                                |
| EX-0001-0196-07 | AC-0001-0196-03 | Recorded receipts, then an unrelated README edit inside the run's write scope and outside every file and glob a receipt reads, as BR-0659 admits it, then a change to a criterion's text                                                                 | After the README edit every receipt stays `valid`; after the criterion change the dependent test and implementation receipts and every later stage go `stale`                                                                   |
| EX-0001-0196-08 | AC-0001-0196-03 | A RED receipt recorded, then a production-file change                                                                                                                                                                                                    | The RED receipt stays `valid`, and the GREEN receipt over that file goes `stale`                                                                                                                                                |
| EX-0001-0196-09 | AC-0001-0196-04 | A second `start` while a run is non-terminal, and a write operation while `.qfai/run/.lock` names a live pid                                                                                                                                             | `run-active` naming the run and its state; `lock-held` naming the owner's run, operation and start time                                                                                                                         |
| EX-0001-0196-10 | AC-0001-0196-04 | A write that fails with `EBUSY`                                                                                                                                                                                                                          | Refused `io-error` with `cause` naming `EBUSY`, the exit code `## Exit codes` gives, and one write attempt                                                                                                                      |
| EX-0001-0196-11 | AC-0001-0196-04 | The same run cycle on Linux and on Windows, with a CRLF copy of a watched file and a temp root whose name has a space                                                                                                                                    | The same verdicts, and equal digests for the LF and CRLF copies                                                                                                                                                                 |
| EX-0001-0196-12 | AC-0001-0196-05 | A journal whose `000004.json` does not parse                                                                                                                                                                                                             | Every write operation is refused `torn-event` with exit 1, nothing is written, and `status` reports `failed` with the code                                                                                                      |
| EX-0001-0196-13 | AC-0001-0196-05 | A published event with a snapshot one sequence behind, and a deleted snapshot                                                                                                                                                                            | The next operation rebuilds the snapshot from the journal and applies no effect twice; the rebuilt snapshot equals the one deleted                                                                                              |
| EX-0001-0196-14 | AC-0001-0196-06 | A `stop` through `decision` on a run in `running`                                                                                                                                                                                                        | The run is `cancelled`; a later `accept` and `next` are refused `run-terminal`; the outstanding review is never recorded as a pass                                                                                              |
| EX-0001-0196-15 | AC-0001-0196-06 | The recovery guidance `qfai-run` ships                                                                                                                                                                                                                   | A reverse diff limited to the paths the run wrote, and no instruction to reset, stash, switch branch or remove a worktree                                                                                                       |
| EX-0001-0196-16 | AC-0001-0196-06 | `resume` on a run in `running` whose outstanding work order has no accepted result                                                                                                                                                                       | `observed-session-interruption` then `reconciled-resume` in the same call, and the outstanding work order is returned again                                                                                                     |
| EX-0001-0196-17 | AC-0001-0196-06 | `finish` on a run in `running`, and `accept` on a run in `completed`                                                                                                                                                                                     | The first lists `work-order-outstanding` and leaves `running`; the second is refused `run-terminal`                                                                                                                             |
| EX-0001-0196-18 | AC-0001-0196-07 | Four results in turn reporting `delegation.status: saturated` for one work order                                                                                                                                                                         | Attempts 1 to 3 return `retry` with `nextDelaySeconds` 30, 60 and 120, and the fourth moves the run to `blocked` with blocker `budget-exhausted`                                                                                |
| EX-0001-0196-19 | AC-0001-0196-07 | An implement result with `testObservation: fail` and outcome `needs_repair`                                                                                                                                                                              | The next work order goes to the owner of the failing artifact, not a repeat attempt of the same work order                                                                                                                      |
| EX-0001-0196-20 | AC-0001-0196-07 | A result whose submitted digest of an input differs from the file on disk                                                                                                                                                                                | Refused `invalid-input` with reason `digest-mismatch`; the next work order carries the refreshed digest                                                                                                                         |
| EX-0001-0196-21 | AC-0001-0196-07 | A fourth replan in one run                                                                                                                                                                                                                               | The run moves to `blocked` with blocker `budget-exhausted`, and `finish` lists `run-waiting`                                                                                                                                    |
| EX-0001-0196-22 | AC-0001-0196-08 | A run starts with a clean worktree; an implement work order then changes its bound flow's implement evidence within `recordAreas`                                                                                                                        | The next write operation and `finish` admit those cumulative changes against the `start` state                                                                                                                                  |
| EX-0001-0196-23 | AC-0001-0196-08 | A run starts with a clean worktree; its core writes `.qfai/evidence/workflow/<runId>/summary.json`                                                                                                                                                       | The next write operation and `finish` admit that tracked core evidence without granting the stage permission to list it in `changedFiles`                                                                                       |
| EX-0001-0196-24 | AC-0001-0196-08 | A run blocked on `scope-dependency` has findings naming an out-of-scope file; the owning stage, invoked by name outside the run, applies a change a `Change request:` row at WIP approves; another run has an unapproved or digest-changed external path | `resume` records only the approved path and digest as an adjustment to the `start` state, rechecks them at `finish`, and refuses the other external change                                                                      |
| EX-0001-0196-25 | AC-0001-0196-07 | A run in `ready` that has made three replans, whose routing receipt then goes stale, then `next` again                                                                                                                                                   | The first `next` moves the run to `blocked` with blocker `budget-exhausted` and issues no work order; the second returns no work order and the same halt                                                                        |
