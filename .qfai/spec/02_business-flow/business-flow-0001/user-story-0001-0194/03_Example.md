# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                       | Expected                                                                                                                                       |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0194-01 | AC-0001-0194-01 | A diagnose result with verdict `defective-test` whose first matched ID is a criterion, for an acceptance test with a flaky wait                                                             | The `test_fix` work order goes to `qfai-atdd`, then `verify-full`                                                                              |
| EX-0001-0194-02 | AC-0001-0194-01 | A `test_fix` result whose assertion contradicted the criterion (500 where it says 400) and now expects 400, with `citedBefore` equal to `citedAfter`, a review receipt and a re-run receipt | Accepted; every example of the obligation set is annotated as it was at issue                                                                  |
| EX-0001-0194-03 | AC-0001-0194-01 | A work order issued after an acceptance stage, and a review result whose reviewer instance authored that stage                                                                              | The work order's `actorHistory` holds every recorded author, recommender and reviewer; the review result is refused `reviewer-not-independent` |
| EX-0001-0194-04 | AC-0001-0194-02 | A `test_fix` result rewriting the assertion to expect a different status, so `citedAfter` differs from `citedBefore`                                                                        | Refused `invalid-input` with reason `test-fix-meaning`, naming `qfai-sdd` as the owner of the fix; the run is unchanged                        |
| EX-0001-0194-05 | AC-0001-0194-02 | A verify result `needs_repair` whose finding sits in a story file and names `qfai-sdd` as its resolving owner                                                                               | The next work order goes to `qfai-sdd`, not back to verify                                                                                     |
| EX-0001-0194-06 | AC-0001-0194-01 | A diagnose result with verdict `defective-test` whose first matched ID is an example, the defective test a unit test                                                                        | The `test_fix` work order goes to `qfai-implement`, then `verify-full`                                                                         |
| EX-0001-0194-07 | AC-0001-0194-03 | A `test_fix` result that carries no review receipt, and another that carries no re-run receipt                                                                                              | Each is refused `invalid-input` with reason `test-fix-receipt`, and every example of the obligation set is annotated as it was at issue        |
