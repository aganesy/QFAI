# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                               | Expected                                                                                                                                                                                                                                                               |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0154-01 | AC-0001-0154-01 | Given a story on the story tree with two ACs and three EXs When `qfai validate --profile sdd --fail-on error` gates the tree                        | Then the gate passes when each EX names exactly one of the two ACs in its `AC-Ref` cell and each AC is named by at least one EX; an EX whose `AC-Ref` cell is empty or names both ACs, or an AC that no EX names, fails the gate with the ID and its file              |
| EX-0001-0154-02 | AC-0001-0154-01 | Given a contract on the story tree defining two rules, and a story with three EXs When `qfai validate --profile sdd --fail-on error` gates the tree | Then the gate passes when each rule's examples name at least one of the three EXs and the two rules together name all three; a rule with no examples, an EX that no rule names, or an example naming an EX that does not exist fails the gate with the ID and its file |
