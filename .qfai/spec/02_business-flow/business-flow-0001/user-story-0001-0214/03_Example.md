# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                          | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EX-0001-0214-01 | AC-0001-0214-01 | An orchestrated `/qfai-sdd` work order whose `target` is absent, a second whose `target` is `{ kind: "flow", flowId: "BF-0002" }`, and a third whose `target` is `{ kind: "new_story", slotId: "slot-1" }` for a slot whose `flowId` is `null` | The first is refused and the no-argument batch does not run. The second changes nothing outside BF-0002 and gates with `--flow BF-0002`. The third's result reports one `bindings` entry for `slot-1` naming the new `BF-NNNN` and the `US-NNNN-NNNN` IDs written under it                                                                                                                                                                                                                                                                                               |
| EX-0001-0214-02 | AC-0001-0214-02 | The operator types `/qfai-sdd`, and later `/qfai-sdd` with a request to take the change to the end                                                                                                                                             | The first stops when SDD completes, with no run created. The second request is handed to a whole run rather than continued past SDD                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| EX-0001-0214-03 | AC-0001-0214-03 | `/qfai-sdd` selected in mode `active` three ways: with no name and no work order, with a work order that matches no issued one, and with a valid work order                                                                                    | The first edits nothing and passes the request to `qfai-run`, the second edits nothing and returns the refusal, and the third does only the work order's work                                                                                                                                                                                                                                                                                                                                                                                                            |
| EX-0001-0214-04 | AC-0001-0214-04 | The `## Operations` table of `qfai-sdd/references/orchestrated-mode.md`                                                                                                                                                                        | The `Operation` column holds exactly `new-story`, `update-or-applicability-check` and `defect-example-seeding`                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| EX-0001-0214-05 | AC-0001-0214-05 | A run whose Stage 0 snapshot was recorded by an earlier stage, and a `/qfai-sdd` work order in the same run after one file under `.qfai/assistant/rule/` changed                                                                               | It recomputes the snapshot key, refreshes the entries that file feeds, reuses the rest, and runs `npx qfai sdd preflight` again                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| EX-0001-0214-06 | AC-0001-0214-06 | In an adopter project, an `sdd_delta` work order for BF-0002 whose proposal changes `user-story-0002-0003/02_Acceptance-Criteria.md` and `<paths.contractsDir>/api/orders.yaml`, in a run whose `request_scope` authorization is recorded      | The first attempt opens one `decision` question naming both files and the proposed change, returns `awaiting_input` and leaves both files unchanged. After the operator answers `proceed`, the attempt holding that `human_decision` writes both files and the `UPDATE:MODIFY` triage row, and appends one row whose Content opens `Change request:` and names both files and `decisions.md`, already at WIP, with an Approach citing that answer. A result whose row cites only the `request_scope` record is refused `invalid-input` with reason `record-unauthorized` |
