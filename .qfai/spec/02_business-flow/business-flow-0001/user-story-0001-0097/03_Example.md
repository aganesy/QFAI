# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                   | Expected                                                                                                                                      |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0097-01 | AC-0001-0097-01 | Given an implemented EX has phase evidence and checkpoint verification When completion is considered                                                                                                    | Then completion-reviewer and implementation-reviewer both return PASS before completion                                                       |
| EX-0001-0097-02 | AC-0001-0097-03 | Given every scoped EX is annotated by a test or has a DONE `Test exception:` decision When the current scoped TDD validation passes and implement runs                                                  | Then it reports "nothing to do" without reading or writing a ledger status                                                                    |
| EX-0001-0097-03 | AC-0001-0097-02 | Given TDD-0004 with evidence from a previous run When completion is checked                                                                                                                             | Then stale evidence is rejected and fresh evidence is required                                                                                |
| EX-0001-0097-04 | AC-0001-0097-01 | Given the implementation worker that wrote the item When the item is put forward for `done`                                                                                                             | Then `completion-reviewer` and `implementation-reviewer` each return PASS first, and the worker's own approval is neither of them             |
| EX-0001-0097-05 | AC-0001-0097-04 | Given a project on the story tree and a `/qfai-implement` invocation that owns the flow `BF-NNNN` When it runs a checkpoint verification and then its completion gate                                   | Then both run `qfai validate --profile tdd --fail-on error --flow BF-NNNN`, and neither passes `--spec <spec-id>`, which exits 2 on that tree |
| EX-0001-0097-06 | AC-0001-0097-03 | Given a story-tree flow with an unannotated EX and a scoped validate run whose result file is missing, predates that run or has a profile other than `tdd` When `/qfai-implement` selects its next test | Then it stops and reports the validate command, exit code and output for each condition; it never reports "nothing to do"                     |
