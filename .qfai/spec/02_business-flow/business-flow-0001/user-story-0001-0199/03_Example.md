# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                     | Expected                                                                                                                                                                                                                                                                                                     |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EX-0001-0199-01 | AC-0001-0199-01 | `start` under `off`, and under `shadow`, with a malformed payload in the inbox                                                                                                            | `ok` with `mode` naming the mode set and `run: null`, exit 0; the tree digest is unchanged, and no entry under `.qfai/run/`                                                                                                                                                                                  |
| EX-0001-0199-02 | AC-0001-0199-01 | The mode guidance `qfai-run` ships                                                                                                                                                        | Under `shadow` it reads the mode from `status`, proposes the route and its reason and calls no write operation; under `off` stages are invoked by name                                                                                                                                                       |
| EX-0001-0199-03 | AC-0001-0199-01 | `status` on a run while another process holds `.qfai/run/.lock`                                                                                                                           | The run directory is byte for byte the same, and `status` succeeds without the lock                                                                                                                                                                                                                          |
| EX-0001-0199-04 | AC-0001-0199-02 | `qfai.config.yaml` with no `workflow` key                                                                                                                                                 | `start` creates a run in mode `active`, and `status` reports `active`                                                                                                                                                                                                                                        |
| EX-0001-0199-05 | AC-0001-0199-02 | `workflow.mode: always`                                                                                                                                                                   | `start` is refused `fail-closed` with cause `invalid-mode`, exit 2, and no run directory                                                                                                                                                                                                                     |
| EX-0001-0199-06 | AC-0001-0199-03 | `start` where the effective routing — the package defaults with the project's `qfai.config.yaml` `routing:` overrides — lacks a blocking reviewer a dispatched phase requires             | Refused `fail-closed` with cause `reviewer-missing`, and no run directory                                                                                                                                                                                                                                    |
| EX-0001-0199-07 | AC-0001-0199-03 | A run in `running` whose `qfai.config.yaml` `routing:` entry changes outside its write scope before the next `accept`; and the same drift on a run in `ready`, then `next`, then a `stop` | The run moves to `blocked` with cause `policy-drift`; the core leaves `qfai.config.yaml` untouched; once the change is reverted, `resume` moves it to `ready`; on the `ready` run, `next` is refused `fail-closed` with cause `policy-drift` and the run stays `ready`, and the stop moves it to `cancelled` |
| EX-0001-0199-08 | AC-0001-0199-03 | A `qfai.config.yaml` `routing:` override that adds one agent and reorders reviewers while keeping every role the plans require                                                            | `start` creates a run in mode `active`                                                                                                                                                                                                                                                                       |
| EX-0001-0199-09 | AC-0001-0199-03 | A plan directory standing in for the package's, whose `feature.yml` stages form a cycle                                                                                                   | `start` is refused `fail-closed` with cause `contract-undeclared`                                                                                                                                                                                                                                            |
| EX-0001-0199-10 | AC-0001-0199-03 | A plan with a stage that has no path to `verify`, and a plan with a stage whose kind is outside the vocabulary, such as `kind: configure`                                                 | Each is refused on load, with reason `no-verify-path` and `out-of-vocabulary`                                                                                                                                                                                                                                |
| EX-0001-0199-11 | AC-0001-0199-03 | A `direct.yml` copy under the project's `.qfai/assistant/process/workflows/`                                                                                                              | `start` creates the run from the packaged plan; the copy is not read                                                                                                                                                                                                                                         |
