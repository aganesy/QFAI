# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                            | Expected                                                                                                                                                                                                                                                      |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0035-01 | AC-0001-0035-01 | With the `rule/ skill/ agent/ prompt/` assistant tree, run `qfai init --upgrade-assistant-tree` on a project whose legacy assistant layers hold files the relocation table names | Each named file is copied to its destination in the `rule/ skill/ agent/ prompt/` tree, and its legacy file is still present and unchanged. Nothing is written under `constitution/`, `manifest/`, `catalog/` or `process/`, and no migration memo is written |
| EX-0001-0035-02 | AC-0001-0035-01 | The same run on legacy layers that also hold `product.md`, `manifest.md`, `tech.md`, `structure.md` and a file the relocation table does not recognise                           | None of the five is copied, and each stays at its legacy path unchanged. The unrecognised file is not listed in the run's output                                                                                                                              |
| EX-0001-0035-03 | AC-0001-0035-01 | Run `qfai init --upgrade-assistant-tree` on a project already using `rule/`, `skill/`, `agent/` and `prompt/`                                                                    | Existing files in those layers are unchanged, the command exits 0 and reports preserved user edits rather than treating the state as an error                                                                                                                 |
| EX-0001-0035-04 | AC-0001-0035-02 | A recognized legacy `steering/` file and its new-layer destination both exist; the destination contains adopter edits. Run `qfai init --upgrade-assistant-tree`.                 | The destination bytes remain unchanged, the legacy file remains, and stdout emits `W-USER-EDIT-PRESERVED` naming the destination.                                                                                                                             |
