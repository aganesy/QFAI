# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                           | Expected                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0210-01 | AC-0001-0210-01 | Given a unit test of `BF-0003` whose one annotation names an EX and which builds its order from a fixture in the wrong currency, and a diagnosis with verdict `defective-test` whose `matchedIds` name that EX When `/qfai-implement` serves the `test-fix` work order and corrects the fixture | Then the result's `testFix` names that EX in both `citedBefore` and `citedAfter`, a `reviewRef` from an independent reviewer and a `rerunRef` for the re-run. The fixed test still carries that EX's annotation and no other; its file or test title may change where the test moved or was renamed. No story file, no contract and no `decisions.md` row changes. The re-run is recorded in `.qfai/evidence/implement-BF-0003.md` |
| EX-0001-0210-02 | AC-0001-0210-02 | Given a test fix after which the expectation would check a different EX than before When the implement stage returns                                                                                                                                                                            | Then the outcome is `needs_repair`, and `debts` lists that finding with `resolvingOwner` `qfai-sdd`. No result with an accepted `testFix` is returned                                                                                                                                                                                                                                                                              |
| EX-0001-0210-03 | AC-0001-0210-03 | Given three `defective-test` diagnoses for `BF-0003`, whose `matchedIds` start with an EX ID, with an AC ID and with the BF ID When the `test_fix` stage is assigned                                                                                                                            | Then `/qfai-implement` serves the work order for the first. It serves none for the other two, which `/qfai-atdd` serves                                                                                                                                                                                                                                                                                                            |
