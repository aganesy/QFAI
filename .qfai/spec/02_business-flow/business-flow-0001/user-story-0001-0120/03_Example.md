# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                          | Expected                                                                                                                                                                                                                                                                                              |
| --------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0120-02 | AC-0001-0120-01 | Given a review payload with `operability` containing 250 words. When schema validation runs.                                                                                                                   | Then the validator emits a length-bound finding and the payload is rejected; ≤ 200 words per `*Feel` field is enforced.                                                                                                                                                                               |
| EX-0001-0120-03 | AC-0001-0120-02 | Given the frozen pairs `(CON-UI-0007, dashboard)`, `(CON-UI-0007, detail)` and `(CON-UI-0011, list)`, where the first two have converged and `list` has one blocking finding. When the convergence check runs. | Then convergence is not declared; once `list` also has no blocking finding, `stopReason` is `converged`.                                                                                                                                                                                              |
| EX-0001-0120-04 | AC-0001-0120-02 | Given at cycle 5 `(CON-UI-0007, dashboard)` has converged, and `(CON-UI-0011, list)` and `(CON-UI-0013, page)` each have one blocking finding. When the convergence check runs.                                | Then convergence is not declared, `CON-UI-0011` and `CON-UI-0013` are named as lagging, and the loop continues.                                                                                                                                                                                       |
| EX-0001-0120-05 | AC-0001-0120-03 | Given the prototype exposes a sidebar with 6 entries and a topbar with 2 entries. When the Reviewer runs its Playwright session.                                                                               | Then it attempts to navigate every primary menu entry, and `menuReachabilityFeel` in the review payload describes which entries reached intended targets and which did not (e.g. "sidebar/Reports leads to 404"); unreachable entries surface as qualitative critique and do NOT hard-fail the cycle. |
| EX-0001-0120-06 | AC-0001-0120-01 | Given a reviewer finishes its Playwright session for `(CON-UI-0007, orders-dashboard)`. When it writes `iter-03/CON-UI-0007/orders-dashboard.review.json`.                                                     | Then the payload validates with the eleven top-level fields, the six `impressions` fields each at most 200 words, and no axis rating; a payload with `cycle: 10`, or an unknown key under `impressions`, is rejected.                                                                                 |
