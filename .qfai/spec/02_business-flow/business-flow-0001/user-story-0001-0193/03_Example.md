# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                       | Expected                                                                                                                                                                                                                                                                                                                                          |
| --------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0193-01 | AC-0001-0193-01 | A diagnose result with verdict `missing-test`, for a flow whose criterion says an empty phone number returns 400, where no example of that criterion states the empty value | The next work order is `sdd_append` to `qfai-sdd` under `missing_example_needed`, then acceptance under `acceptance_obligations_unmet`, then implement and `verify-full`                                                                                                                                                                          |
| EX-0001-0193-02 | AC-0001-0193-01 | The `sdd_append` work order issued after a `missing-test` diagnosis                                                                                                         | Its inputs carry the diagnosis `reproductionRef` as the reason, and its `recordAreas` hold no `01_User-story.md` and no `02_Acceptance-Criteria.md`                                                                                                                                                                                               |
| EX-0001-0193-03 | AC-0001-0193-02 | A diagnose result with verdict `regression` for an example a test annotates, whose test is correct and now fails                                                            | The next work order is `regression_fix` to `qfai-implement`, then `verify-full`; the example stays annotated                                                                                                                                                                                                                                      |
| EX-0001-0193-04 | AC-0001-0193-02 | A `regression_fix` result with the same test's GREEN re-run receipt and an independent review receipt, and one lacking either                                               | The first is accepted with the example still annotated; the second is refused `invalid-input` with reason `regression-fix-receipt`                                                                                                                                                                                                                |
| EX-0001-0193-05 | AC-0001-0193-03 | A result after which an example of the obligation set that a test annotated at issue has no annotating test                                                                 | Refused `invalid-input` with reason `example-uncovered`; the obligation set is unchanged                                                                                                                                                                                                                                                          |
| EX-0001-0193-06 | AC-0001-0193-03 | An implement result that adds an example to a story of the bound flow                                                                                                       | Refused `invalid-input` with reason `example-added`                                                                                                                                                                                                                                                                                               |
| EX-0001-0193-07 | AC-0001-0193-03 | An implement work order bound to `BF-0001`                                                                                                                                  | `obligations` holds `flowId`, `ids` and `digest`, and no field saying whether a test annotates an item                                                                                                                                                                                                                                            |
| EX-0001-0193-08 | AC-0001-0193-04 | A diagnose result with verdict `expectation-differs`: the story actually says 500                                                                                           | The run moves `running → routing`, and no implement work order is issued                                                                                                                                                                                                                                                                          |
| EX-0001-0193-09 | AC-0001-0193-04 | A routing seed where no story covers the behaviour and the operator states the expected 400                                                                                 | The seed's allowed routes are `bounded-change`, not `bugfix`                                                                                                                                                                                                                                                                                      |
| EX-0001-0193-10 | AC-0001-0193-04 | A run reclassified from `bugfix` to `bounded-change` after a RED receipt was accepted for an example no test yet annotates                                                  | The work order after the replan names that example in `obligations.ids`, and its `priorStageReceiptRefs` gives `{ ref, validity }` for every prior receipt, with the RED receipt `valid`                                                                                                                                                          |
| EX-0001-0193-11 | AC-0001-0193-01 | A diagnose result with verdict `missing-test` whose first matched ID is an example that states the case and that no test annotates                                          | No `sdd_append` work order is issued and no row is appended to `decisions.md`; the next work order is acceptance under `acceptance_obligations_unmet`, then implement under `diagnosis_missing_test`                                                                                                                                              |
| EX-0001-0193-12 | AC-0001-0193-03 | An `sdd_delta` result that adds an example to a story of the bound flow and removes another example a test annotated                                                        | Accepted; neither `example-added` nor `example-uncovered` is raised                                                                                                                                                                                                                                                                               |
| EX-0001-0193-13 | AC-0001-0193-01 | The `bugfix` plan the package ships                                                                                                                                         | Stage kinds and predicates in order: `diagnose` under `always`, `sdd_append` under `missing_example_needed`, `acceptance` under `acceptance_obligations_unmet`, `implement` under `diagnosis_missing_test`, `regression_fix` under `regression_found`, `test_fix` under `test_defect_found`, `verify` with operation `verify-full` under `always` |
