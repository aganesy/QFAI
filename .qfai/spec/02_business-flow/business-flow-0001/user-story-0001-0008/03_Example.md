# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                  | Expected                                                                                                                     |
| --------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0008-04 | AC-0001-0008-02 | A `user-story-NNNN-NNNN/` directory inside `business-flow-NNNN/`, whose story ID starts with the flow's number, whose AC and EX IDs start with the story ID, and whose directory names match their IDs | No ID-grammar finding                                                                                                        |
| EX-0001-0008-07 | AC-0001-0008-03 | A flow whose stories use the tails `0001` and `0003`, with `0002` unused                                                                                                                               | The next story of that flow takes the tail `0004`; the gap at `0002` is not filled                                           |
| EX-0001-0008-08 | AC-0001-0008-01 | A BF ID that is the H1 of `business-flow.md` and is listed in `business-flows.md`, and a US ID that is the H1 of `01_User-story.md` and is named in `01_policy/glossary.md`                            | Each ID is declared once, by its H1; the index file and the policy file hold citations, so no duplicate-ID finding is raised |
| EX-0001-0008-09 | AC-0001-0008-03 | On the story tree, a flow whose stories in the tree use the tails `0001` to `0003`, and a `decisions.md` row naming a retired story of that flow with the tail `0004`                                  | The next story of that flow takes the tail `0005`; the retired `0004` is not reissued                                        |
| EX-0001-0008-10 | AC-0001-0008-01 | A YAML contract's `x-qfai-rules` and a SQL contract's Rule line both declare the same `BR-NNNN`                                                                                                        | An ID-grammar error names `BR-NNNN` and both contract files                                                                  |
| EX-0001-0008-11 | AC-0001-0008-03 | Flow `BF-0001` has stories `0001` to `0003`, and flow `BF-0002` has story `US-0002-0007`                                                                                                               | The next story of `BF-0001` is `US-0001-0004`                                                                                |
