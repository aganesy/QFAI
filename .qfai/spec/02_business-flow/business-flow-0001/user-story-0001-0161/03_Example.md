# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                        | Expected                                                                                                                                                                                      |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0161-01 | AC-0001-0161-01 | Given a screen declaring 9 `primary_tasks` When `QFAI-AUD-020` runs                                                                                                                                                                                                          | Then the warning fires at severity warning and names the ceiling `at most 7`; the `ui-contract.sample.yaml` template comments and `references/ui-contract-guide.md` both document the ceiling |
| EX-0001-0161-02 | AC-0001-0161-02 | Given one item `"Review orders"` (string-only) and one `{id: t1, label: "Mark shipped", acceptance: "order status flips to shipped"}` (structured) and one `{id: t2, label: "x"}` (missing `acceptance`) When `auditProfile.ts` evaluates them during the deprecation window | Then the string-only and the complete structured item are accepted; the item missing `acceptance` is rejected (all-required, closed schema)                                                   |
| EX-0001-0161-03 | AC-0001-0161-02 | A structured item `{id: t2, label: "Mark order shipped", acceptance: "order status flips to shipped", priority: high}`                                                                                                                                                       | Rejected with `QFAI-AUD-021` naming `t2` and `priority`                                                                                                                                       |
| EX-0001-0161-04 | AC-0001-0161-01 | Screens declaring 2, 3, 7 and 8 `primary_tasks`, each checked by `QFAI-AUD-020`                                                                                                                                                                                              | Only the screen declaring 8 raises the warning; 2, 3 and 7 raise none, because there is no floor                                                                                              |
