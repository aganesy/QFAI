# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                                                                                                                        | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EX-0001-0208-01 | AC-0001-0208-01 | Given a `diagnose-only` work order naming the AC the reported behaviour should meet and the scope to search When `/qfai-implement` serves it and reproduces the defect                                                                                                                                                                                                       | Then the result's `changedFiles` is empty, and no file git tracks differs from the tree the work order was issued on, so no product code, test or story file changed. The reproduction record it writes, which git ignores, is named in `artifactRefs` and not in `changedFiles`                                                                                                                                                                                                                                           |
| EX-0001-0208-02 | AC-0001-0208-02 | Given four reported defects against behaviour `BF-0003` states: (a) no test annotates the EX that states the behaviour, or no EX states it; (b) the test annotating the EX reads a fixture that contradicts the EX; (c) the test annotating the EX is correct and now fails; (d) what the operator expects differs from what the AC and its EXs state When each is diagnosed | Then each result carries exactly one verdict: (a) `missing-test`, (b) `defective-test`, (c) `regression`, (d) `expectation-differs`. In (a) `matchedIds` names the EX that states the case, or the AC it falls under where no EX states it. In (b) and (c) it names the ID the covering test annotates, which the next work order acts on. In (d) it names the matching existing IDs, and none where there are none. Its `reproductionRef` names a record that holds the reproduction, the cause candidates and the impact |
| EX-0001-0208-03 | AC-0001-0208-03 | Given a diagnosis with verdict `missing-test` on behaviour an existing AC states When `/qfai-implement` handles that scope gap                                                                                                                                                                                                                                               | Then it appends no `Change request:` row to `decisions.md` and adds no EX. Where an EX already states the case, the implement stage picks it up as an EX no test annotates. Where none does, `/qfai-sdd` adds one EX under that AC. The line of `SKILL.md` that sends out-of-scope discoveries to `/qfai-sdd` as a change request states that carve-out                                                                                                                                                                    |
| EX-0001-0208-04 | AC-0001-0208-03 | Given a RED round that finds an obligation no AC of the flow states When `/qfai-implement` handles that scope gap                                                                                                                                                                                                                                                            | Then it sends the discovery to `/qfai-sdd` as a change request, as it does for any scope gap other than a diagnosed missing test                                                                                                                                                                                                                                                                                                                                                                                           |
