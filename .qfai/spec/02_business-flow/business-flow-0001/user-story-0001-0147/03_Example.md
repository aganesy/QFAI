# Examples

## Examples

| EX-ID           | AC-Ref          | Input                                                                                                                                                                                                                                                                   | Expected                                                                                                                                                                                                                                         |
| --------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EX-0001-0147-01 | AC-0001-0147-01 | Given UI contracts `CON-UI-0001` (`home`, `dashboard`) and `CON-UI-0002` (`settings`) form the frozen surface union, and DESIGN.md defines `--color-primary: #2563eb` and `--radius-md: 8px` When cycle 0 runs with `--emit-skeletons` and the default placeholder mode | Then it writes token-styled placeholder HTML for all three screens without per-screen LLM generation; after convergence each screen has both screenshot and HTML evidence refs. With `--skeleton-mode full`, generation runs per screen instead. |
| EX-0001-0147-02 | AC-0001-0147-02 | Given the same frozen UI contract set When cycle 0 runs without `--emit-skeletons`                                                                                                                                                                                      | Then no skeleton files are emitted and the existing opt-in default is preserved.                                                                                                                                                                 |
