# 02 Initiative

The initiative is the bounded effort undertaken to reach `01_Objective.md`. It
answers "what are we doing about it", one layer above capabilities.

## Initiative

- <one sentence: the bounded effort, not the product>

## Scope

| In scope | Out of scope               | Why                       |
| -------- | -------------------------- | ------------------------- |
| `<area>` | `<adjacent area excluded>` | `<reason for the border>` |

## Assumptions

- `<assumption this initiative rests on, and what falsifies it>`

## Dependencies

- `<external system, team, or decision this initiative waits on>`

## Authoring rules

- The scope table's `Out of scope` column is load-bearing: a border with no
  stated reason is a border no one can defend when it is challenged.
- Record a stated assumption here rather than an open question when the work
  proceeds on it. Genuine questions belong in `09_Open-questions.md`.
- No lower-layer IDs (`references/spec-traceability-rules.md`).
