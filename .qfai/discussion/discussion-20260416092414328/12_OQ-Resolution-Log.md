# 12 OQ Resolution Log

Resolution log for discussion-20260416092414328 (rev9).

| OQ-ID   | Title                                                                                    | Created    | Resolved   | Resolved By | Resolution Summary                                                                                                                                                                                                                                                     |
|---------|------------------------------------------------------------------------------------------|------------|------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| OQ-0001 | ui[] row validation location — inline in prototypingEvidence.ts vs separate utility     | 2026-04-16 | 2026-04-16 | agent       | Option A adopted: inline in `prototypingEvidence.ts`. Design doc §6-1-2 explicitly names `prototypingEvidence.ts` as the changed file. Inline keeps validation cohesive with existing `validateRuntimeGate()`. Extraction can be considered if the function grows significantly in future cycles. |
| OQ-0002 | browserQaEvidenceRefs[] — always non-empty or allow empty when no browser QA run        | 2026-04-16 | 2026-04-16 | agent       | Option A adopted: always required non-empty. Design doc §3-2 fail-closed policy; §3-3 no leniency at parse layer. Rev8 OQ-0003 established the empty-array-is-error precedent for the same pattern. Partial runs that cannot populate `browserQaEvidenceRefs[]` must fail at builder, not pass at validator. |
| OQ-0003 | Axis-level validation granularity — per-axis or aggregate                               | 2026-04-16 | 2026-04-16 | agent       | Option A adopted: per-axis (each axis independently). Design doc §6-1-3 uses per-element language: "array 必須、空配列禁止、各値は concrete artifact ref". Aggregate leniency would allow some axes to lack evidence while appearing valid. Per-axis is the correct fail-closed granularity.          |
| OQ-0004 | README update scope — full leaf-field enumeration or minimal extension note              | 2026-04-16 | 2026-04-16 | agent       | Option A adopted: full enumeration of all concrete-ref leaf fields in README. Design doc §5-6 requires docs/validator mismatch to be fully eliminated. The DoD condition "一部 field だけ strict という状態がどちらにも残らない" is a hard gate that precludes minimal patching. Full enumeration is the only compliant approach. |
