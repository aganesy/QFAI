# 07 Non-Functional Requirements

## NFR Table

| NFR-ID   | Category        | Title                                                                | Target                                                                                                      | Measurement                                                                            | Source        | Priority |
|----------|-----------------|----------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------|---------------|----------|
| NFR-0001 | Reliability     | Zero false-negatives for all leaf-field malformed ref forms          | `validatePrototypingEvidence()` rejects 100% of malformed inputs across all 15 new negative test cases (7 ui[] + 5 axis + 3 reviewer) | Run `pnpm vitest run --project validators --project core`; all 15 negative cases must return errors | SRC-0001 §5   | must     |
| NFR-0002 | Maintainability | No parallel concrete-ref grammar implementation                      | 0 independent regex or pattern definitions for "is concrete ref" outside `pathUtils.ts` in `packages/qfai/src` after WS-1 | Code grep: `grep -r "isConcreteArtifactRef\|/^\./\|concrete.*ref" packages/qfai/src --include="*.ts" -l`; must only show `pathUtils.ts` as the definition site | SRC-0001 §3-4 | must     |
| NFR-0003 | Reliability     | No optional/nullable mismatch between bundle schema and validator   | 0 fields that the validator requires as non-empty but the bundle schema marks as optional or nullable       | TypeScript `tsc --strict` produces 0 errors; schema type review confirms no `?` on leaf array fields | SRC-0001 §5-4 | must     |
| NFR-0004 | Reliability     | Production closure test asserts leaf concreteness                   | `prototypingExecution.productionPath.test.ts` has ≥1 positive closure + ≥1 negative injection asserting leaf refs | Test file exists; `pnpm vitest run --project core` passes including the new assertions | SRC-0001 §5-5 | must     |
| NFR-0005 | Maintainability | Zero synthetic token fixtures in `tests/core/`                      | 0 occurrences of synthetic token strings (`"a"`, `"b"`, `"reviewer:1"`, or similar non-path tokens) as `evidenceRefs` values in test fixtures | `grep -r '"a"\|"b"\|"reviewer:1"' packages/qfai/tests/core --include="*.ts"` returns 0 results in evidenceRefs contexts | SRC-0001 §5-5 | must     |
