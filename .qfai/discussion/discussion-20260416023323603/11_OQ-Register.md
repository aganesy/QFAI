# 11 OQ Register

Open Questions register for discussion-20260416023323603 (rev8).

**Open count: 0** — all 4 OQs resolved.

| OQ-ID   | Title                                                              | Gate       | Disposition | Owner | Rationale                                                                                                                                                                                                       | Options                                                                                                                                                                                                                                                                                                     | Recommendation                                                                                                                                                                                                       | Next-Decision-Point                  | Due        | Evidence                                                                                               |
|---------|--------------------------------------------------------------------|------------|-------------|-------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------|------------|--------------------------------------------------------------------------------------------------------|
| OQ-0001 | pathUtils.ts: new file vs inline in specCoverage.ts                | discussion | resolved    | agent | Ref grammar helpers must be shared between `specCoverage.ts` (builder) and `prototypingEvidence.ts` (validator). If helpers are inlined in `specCoverage.ts`, the validator cannot import them without creating a circular or awkward dependency. A dedicated leaf module is the clean solution. | **Option A**: New file `pathUtils.ts` — isolates ref grammar helpers; enables reuse by builders and validators without circular imports; matches design doc §6-1-1 explicit interface definition; independently testable. **Option B**: Inline in `specCoverage.ts` — no new file, but helpers not reusable by `runtimeGateBuilder.ts` or `prototypingEvidence.ts` without coupling to `specCoverage.ts`. | Option A adopted. Design doc §6-1-1 explicitly defines the `ArtifactRefOptions` interface and `toRepoRelativeArtifactRef` function signature. §6-3-1 requires "package 内で1箇所の helper" (single helper location). Option B would make reuse impossible without coupling. | Resolved in this discussion (rev8)  | 2026-04-16 | SRC-0001 §6-1-1 (helper interface definition); SRC-0001 §6-3-1 ("package 内で1箇所の helper")          |
| OQ-0002 | measurement.ts scope — is it a changed file for WS-3?             | discussion | resolved    | agent | Design doc §6 WS-3 changed files list includes `measurement.ts`. However, it is listed as "必要に応じて" (if needed). The empirical question is whether `measurement.ts` currently generates absolute paths in ref output. Two approaches exist. | **Option A**: Include `measurement.ts` only if an empirical check (grep) confirms it generates absolute paths in evidence refs — minimal, targeted change. **Option B**: Include `measurement.ts` unconditionally since design doc §6 lists it as a WS-3 changed file — conservative, avoids missing a case. | Option B adopted (conservative scope). Design doc §6 WS-3 explicitly lists `measurement.ts` among changed files. Including it ensures completeness and aligns with the design doc's stated scope. Empirical check is still performed during implementation; if no change is needed, the file is left unchanged. | Resolved in this discussion (rev8)  | 2026-04-16 | SRC-0001 §6 WS-3 changed files list                                                                   |
| OQ-0003 | runtimeGate.evidenceRefs — empty array vs "no refs required" when no routes observed | discussion | resolved    | agent | Two positions exist on how to treat an empty `runtimeGate.evidenceRefs` array. The design doc is explicit on this point in §6-2-3. The fail-closed vs. lenient choice has direct impact on downstream auditing: a lenient rule allows evidence-free runs to pass the validator silently. | **Option A**: Empty array is always a validator error — fail-closed; consistent with design doc §6-2-3; consistent with backward-compat-abandoned principle; no special case for "no routes observed". **Option B**: Empty array is allowed when no routes were observed — more lenient; avoids false positives on "empty run" scenarios; but contradicts design doc §6-2-3 and introduces ambiguity. | Option A adopted. Design doc §6-2-3 explicitly states "empty array も error". §3-2 reconfirms fail-closed as the overarching policy. Backward compatibility is abandoned; lenient rules are not introduced. Runs with no route observations must not produce empty evidenceRefs — this indicates a configuration or execution issue that should surface. | Resolved in this discussion (rev8)  | 2026-04-16 | SRC-0001 §6-2-3 ("empty array も error"); SRC-0001 §3-2 (fail-closed policy)                          |
| OQ-0004 | README.md update scope — conditional vs unconditional             | discussion | resolved    | agent | The design doc gives explicit conditional guidance for README updates in §7-8. Two interpretations exist: take the conditional guidance at face value, or update unconditionally for completeness. | **Option A**: Update README only if it contains an obsolete or absent description of the ref grammar or `runtimeGate.evidenceRefs` validator contract — minimal, targeted; avoids unnecessary churn. **Option B**: Update README unconditionally to add a ref grammar section — ensures documentation is always complete, but may introduce unnecessary churn if README already has correct content. | Option A adopted. Design doc §7-8 gives explicit conditional criterion ("変更条件付き": update only if obsolete/absent description exists). Following the design doc literally avoids over-engineering. If the current README has no obsolete ref grammar description, no update is needed. | Resolved in this discussion (rev8)  | 2026-04-16 | SRC-0001 §7-8 ("変更条件付き": conditional update criterion)                                          |
