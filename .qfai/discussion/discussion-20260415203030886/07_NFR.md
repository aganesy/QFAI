# 07 Non-Functional Requirements

## NFR Register

| NFR-ID   | Title                                                         | Description                                                                                                                                                                   | Metric / Target                          | Source       | Priority | Status |
|----------|---------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------|--------------|----------|--------|
| NFR-0001 | All execution failure paths distinguishable by error type     | Each execution failure category (calibration, uiFidelity, specCoverage, L2, harness runtime, evidence write) must be distinguishable by the TypeScript error class thrown. Callers must be able to use `instanceof` checks to identify the failure domain. | 0 ambiguous failure categories; 6 distinct error classes present | SRC-0001 WS-5 | must     | draft  |
| NFR-0002 | Pack resolution failure latency                               | `CalibrationLoader` pack resolution failure (e.g., file not found) must propagate and throw within 100ms. No slow retry loops on resolution failure.                           | Failure propagation latency < 100ms      | SRC-0001 WS-1 | should   | draft  |
| NFR-0003 | Validator calibration metadata check overhead                 | The additional `packPath` / `packVersion` / `configPath` comparison introduced by WS-4 must not add more than 50ms to a validator run for a typical evidence bundle.           | Overhead < 50ms per validator run        | SRC-0001 WS-4 | should   | draft  |
| NFR-0004 | TypeScript strict compliance                                  | All modified and new TypeScript source files must compile with `strict: true` and produce 0 errors. No `@ts-ignore`, `@ts-nocheck`, or bare `as` type assertions may be added. | 0 TypeScript errors; 0 @ts-ignore uses   | QFAI repo convention | must | draft  |
| NFR-0005 | All vitest test suites pass                                   | After implementing all 7 workstreams, all `packages/qfai` vitest suites must pass with 0 failures. Test coverage must not regress from the pre-PR baseline.                    | 0 test suite failures                    | SRC-0001 §5  | must     | draft  |
| NFR-0006 | Shipped config template has 0 scalar calibration fields       | After WS-6, `packages/qfai/assets/init/root/qfai.config.yaml` must contain no instances of `thresholds`, `maxIterations`, `plateauDelta`, or `plateauLookback` under any calibration key. | 0 scalar calibration fields in template  | SRC-0001 WS-6 | must     | draft  |
