# 11 OQ Register

Open Questions register. All 5 OQs have Disposition = `resolved`.

| OQ-ID | Title | Gate | Disposition | Owner | Rationale | Options | Recommendation | Next-Decision-Point | Due | Evidence |
|-------|-------|------|-------------|-------|-----------|---------|----------------|---------------------|-----|---------|
| OQ-0001 | Exact members of PROTOTYPING_SUPPORTED_SURFACES | discussion | resolved | agent | Design doc states "at least exclude cli" but does not enumerate all members. An explicit, exhaustive list is needed to avoid ambiguity in tests and validator. Without it, implementors may differ on whether `mixed` is included, causing inconsistent behavior across test suites. | **Option A:** `[web, mobile, desktop, mixed]` — all UI-bearing visual surfaces including cross-platform; **Option B:** `[web, mobile, desktop]` — exclude `mixed` as potentially ambiguous | **Option A** adopted: `[web, mobile, desktop, mixed]`. `mixed` is a legitimate cross-platform UI surface per the QFAI surface taxonomy; excluding it without a documented technical reason would create an unintended gap in multi-surface prototyping scenarios. | Resolved in this discussion | 2026-04-15 | SRC-0001 §3-2; surface taxonomy in qfai-prototyping skill context; OQ-0001 resolution recorded in 12_OQ-Resolution-Log.md |
| OQ-0002 | surfacePolicy.ts as standalone file vs inline constants in mode.ts | discussion | resolved | agent | Design doc states "either is acceptable" but does not prescribe a location. The location affects Single Responsibility Principle adherence, independent testability, and the ability to import surface policy from non-mode contexts (e.g., directly from CLI and validator). | **Option A:** Standalone `surfacePolicy.ts` at `src/core/prototyping/surfacePolicy.ts` — SRP, independently testable, importable without pulling in mode.ts obligations logic; **Option B:** Inline constants inside `mode.ts` — simpler, fewer files, but couples surface policy to obligations computation | **Option A** adopted: standalone `surfacePolicy.ts`. `mode.ts` already owns obligations derivation logic; adding surface policy constants there would create dual responsibilities. Independent file enables isolated unit tests for surface allowlist and allows CLI layer to import without a transitive dependency on obligations logic. | Resolved in this discussion | 2026-04-15 | SRC-0001 §3-2; SRP principle from development-principles-checklist; OQ-0002 resolution recorded in 12_OQ-Resolution-Log.md |
| OQ-0003 | CalibrationLoader resolution failure behavior: throw vs return typed error | discussion | resolved | agent | Design doc says "pack resolution failure → 即 throw / fail" but leaves the throw mechanism unspecified. Two viable approaches exist, each with different implications for error handling patterns in the harness. | **Option A:** Throw `Error` immediately with `packPath` in the message — consistent with existing fail-fast patterns in the codebase; **Option B:** Return `{ error: string }` result object — allows caller to inspect and re-wrap; **Option C:** Throw typed `CalibrationPackError` subclass — enables instanceof checks but adds a new type to the public surface | **Option A** adopted: throw `Error` immediately. CalibrationLoader is a precondition step — the harness cannot run without a resolved pack. Precondition failures in packages/qfai use throw semantics. Typed errors (Option C) are unnecessary unless callers need to distinguish CalibrationPackError from other errors; no such caller exists. | Resolved in this discussion | 2026-04-15 | SRC-0001 §3-3; existing error handling patterns in packages/qfai harness code; OQ-0003 resolution recorded in 12_OQ-Resolution-Log.md |
| OQ-0004 | reviewerLogs.verdict: store mapped vocabulary vs original pre-mapping vocabulary | discussion | resolved | agent | Design doc section 3-5 defines both the pre-mapping values (e.g., `accept`) and the target vocabulary (`approve`). It is ambiguous whether `reviewerLogs[].verdict` should store the pre-mapping value (preserving trace fidelity) or the post-mapping value (vocabulary consistency). | **Option A:** Store mapped vocabulary (`approve`, `revise`, `reject`, `abandon`) — consistent with `reviewerSignoff.status` vocabulary; validator checks single vocabulary; **Option B:** Store original pre-mapping value (`accept`, `revise`, `reject`, `plateau-stop`) — preserves the original harness signal before mapping | **Option A** adopted: store mapped vocabulary. The validator checks `reviewerLogs[].verdict` against the mapped vocabulary. Storing original values would require a translation step in every consumer, increasing validator branching. The audit requirement is to see the final semantic verdict, not the raw harness signal. | Resolved in this discussion | 2026-04-15 | SRC-0001 §3-5; validator implementation context in prototypingEvidence.ts; OQ-0004 resolution recorded in 12_OQ-Resolution-Log.md |
| OQ-0005 | uiContractId in observation schema: hard-error vs silent ignore | discussion | resolved | agent | Removing `obs.screenId === screen.uiContractId` matching. The observation schema previously accepted `uiContractId` as a field. After the fix, observations that still use `uiContractId` will not be matched. The question is whether the schema validator should hard-error on records containing `uiContractId`, or silently ignore the field. | **Option A:** Hard-error on any observation record that contains a `uiContractId` field — strict, forces cleanup, consistent with "backward compat abandoned" principle; **Option B:** Silently ignore `uiContractId` field if present — safe and non-breaking, but hides stale fixtures in tests and user data | **Option A** adopted: hard-error. Backward compat is explicitly abandoned per design doc. Silently ignoring `uiContractId` would mask stale test fixtures that still use the old field, making it harder to identify and correct them. Hard-error surfaces the problem immediately and forces a clean migration. | Resolved in this discussion | 2026-04-15 | SRC-0001 §WS-6; "後方互換は完全に捨てる" principle from SRC-0001 §0; OQ-0005 resolution recorded in 12_OQ-Resolution-Log.md |
