{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__github__(create_pull_request|update_pull_request|issue_write|add_issue_comment|pull_request_review_write|add_comment_to_pending_review|add_reply_to_pull_request_comment)",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "QFAI documentation-clarity reminder",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:'QFAI documentation clarity (.agents/rules/documentation-clarity.md). Before this title or body ships: no issue or PR numbers, ticket IDs or team-local jargon in source and docs; no account of how the work went, only the current behaviour and why; cut self-evident, repeated and wordy text; ordinary words, one claim per sentence, short lines; bullet lists and tables for parallel items; then re-read every line and rewrite anything that reads as a literal translation.'}}))"
            ]
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: design artifact",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ],
            "if": "Write(**/.qfai/specs/**)"
          },
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: design artifact",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ],
            "if": "Edit(**/.qfai/specs/**)"
          },
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: design artifact",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ],
            "if": "Write(**/.qfai/discussion/**)"
          },
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: design artifact",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ],
            "if": "Edit(**/.qfai/discussion/**)"
          }
        ]
      },
      {
        "matcher": "Task|Agent",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: delegation",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ]
          }
        ]
      },
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "QFAI grilling reminder: plan",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',additionalContext:\"QFAI grilling (.agents/rules/grilling.md). Before this: is a decision about to be made that the repository cannot settle — a design, an approach, a scope boundary, a trade-off? Then a grilling session is declared and put in rounds: the whole frontier at once, each question numbered with its recommended answer, facts looked up rather than asked. A session ends on an empty frontier and the user's confirmation, never at a count. An agent that answers its own decisions has stopped following the rule.\"}}))"
            ]
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "if": "Write(**/*.md)",
            "statusMessage": "QFAI documentation-clarity reminder",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'QFAI documentation clarity (.agents/rules/documentation-clarity.md). For the Markdown just written, inside this change only: no issue or PR numbers, ticket IDs or team-local jargon; no account of how the work went, only the current behaviour and why; cut self-evident, repeated and wordy text; ordinary words, one claim per sentence, short lines; bullet lists and tables for parallel items; then re-read every line and rewrite anything that reads as a literal translation.'}}))"
            ]
          },
          {
            "type": "command",
            "if": "Edit(**/*.md)",
            "statusMessage": "QFAI documentation-clarity reminder",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'QFAI documentation clarity (.agents/rules/documentation-clarity.md). For the Markdown just edited, inside this change only: no issue or PR numbers, ticket IDs or team-local jargon; no account of how the work went, only the current behaviour and why; cut self-evident, repeated and wordy text; ordinary words, one claim per sentence, short lines; bullet lists and tables for parallel items; then re-read every line and rewrite anything that reads as a literal translation.'}}))"
            ]
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "QFAI minimal-implementation reminder",
            "command": "node",
            "args": [
              "-e",
              "console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'QFAI minimal implementation (.agents/rules/minimal-implementation.md). For the file just changed, if it is source: could it not exist at all, or does the standard library, the platform or an already-installed dependency do it? Otherwise the least code that works. Mark a deliberate shortcut with both its ceiling and the condition that lifts it. The ladder never removes validation at a trust boundary, error handling that prevents data loss, security, accessibility, or anything the spec asks for.'}}))"
            ]
          }
        ]
      }
    ]
  }
}
