/**
 * E2E acceptance skeleton for spec-0013 CHG-005 user story.
 *
 * `it.todo` Red skeleton; implementation lands in the follow-up
 * `/qfai-implement` micro cycle.
 */
// QFAI:SPEC-0013:US-0013-0011

import { describe, it } from "vitest";

describe("US-0013-0011: UI contract `primary_tasks` slot per screen + validate lane", () => {
  it.todo(
    "shipped ui-spec.yaml template carries primary_tasks: [] on every screens[] entry; QFAI-AUD-001 aligned lane blocks /qfai-prototyping preflight when any screen has empty primary_tasks",
  );
});
